# P3 交付说明：内容级锁定 · 工作区 · 撤销历史 · 现场恢复

> 对应第二批 10 项要求（①–⑩）。本文件只记录**可复现的实现与证据**，以及**明确的边界**；
> 不含"计划中""预计"这类话——没做到的直接写在"已知边界"里。

## 0. 验证总览（本轮实测）

| 项目 | 结果 | 命令 |
| --- | --- | --- |
| 类型检查 | 0 错误 | `node node_modules/typescript/bin/tsc --noEmit` |
| 单元测试 | **461 passed / 29 files**（~15–30 秒） | `node node_modules/vitest/vitest.mjs run` |
| 端到端测试 | **105 passed / 0 failed / 24 spec 文件**（全量 **~3–4 分钟**：并发主项目 91 例 + 串行敏感用例 14 例） | `npm run e2e`（= `--project=main --workers=6` 再 `--project=sensitive --workers=1`） |
| 格式兼容真值样本 | 20 个真 Excel 产出的 `.xls/.ods/.csv`（+2 个 UTF-8/GBK CSV） | `npm run fixtures:legacy`（需要本机 Excel） |
| 性能基准（Node） | 见 ⑩ 章节 | `npm run bench:parse` |
| 多标签内存 | 见 ⑧ 章节（第 4 个标签让堆从 347 → 194 MB） | `tests/e2e/tabs-memory.spec.ts` |
| 滚动/缩放 | 见缺陷 E、F、G2（滚动条拖拽由我们实现 + 命中容差） | `tests/e2e/scrollbar.spec.ts`、`tests/e2e/zoom.spec.ts` |
| 坐标一致性 | 见缺陷 G（真实夹具滚动后起点/落点仍准） | `tests/e2e/coordinates.spec.ts` |
| 提示统一 | 见缺陷 H | `tests/e2e/prompts.spec.ts` |

**测试规模是刻意收敛过的**（用户要求"只保留关键测试用例"）：单元 580 → **454**，e2e 52 → **95**。
（本轮为了三种新格式与两块新交互长了 95 条单元 + 12 条 e2e —— 都是"真值对拍"型用例：
拿真 Excel 产出的 `.xls/.ods/.csv` 与同内容的 `.xlsx` 逐格比对，不是重复覆盖。）
做法是删掉重复覆盖、探索性/诊断性文件、以及"一个行为拆成七八条"的穷举用例，
把同一场景族合并成单个 `it`（断言逐条保留），而不是删断言。
**关键不变量一条没少**：22 条被禁命令全部仍被断言（另加 3 条必须放行的视图命令：
缩放、滚轮滚动）、导出的字节级保留与瘦身等价、tokenizer 的 7 个回归（自闭合兄弟、命名空间前缀、
富文本降级、实体、脏 XML、夹具真值）、三种交互模式（含拖动落点高亮与互换后不留选中）、
工作区逐格拆分与跨表共享、右键复制/剪切/清空、持久化（含三种模式值）、撤销历史
（含"一次动作 = 一条历史"与"工作区动作按时间顺序撤销"）、性能护栏、
日期单元格搬家不退化（raw 序列号 + 悬停提醒已关）、缩放组件可用、
滚动可用（滚轮/滚动条不误触发搬运）、多标签常驻窗口与冷存重建、互换提醒框的几何与渐隐、
**工作区不放空内容条目**（空字符串/空格/全角空格都算空，含互换与会话恢复两条反向通道）、
**导入摘要不误报**（打印设置算"已原样保留"、未被引用的占位填充不算降级）、
**热更新后标签不多不少**（Fast Refresh 重跑引导 effect 也不会多出同名"灵魂标签"）、
**选区右下角不再画没用的"填充柄"**（自动填充被永久拒绝，拖它反而会换内容）、
**所有表格/工作区操作都在同一条可撤销通道上**（工作区条目拖回表格、面板剪切/复制、打开文件不留幽灵条目）。

单元测试文件分工：`parser*.test.ts`（解析器与真实夹具真值）、`importer-p1.test.ts`（适配）、
`export.test.ts`（保真导出）、`read-only-guard.test.ts`（只读约束，22 条循环未动）、
`click-swap.test.ts`/`interaction.test.ts`（交互原语）、`workspace-preview.test.ts`/`import-parse.test.ts`（工作区：
逐格拆分后的卡片契约、`cellSize` 夹取、搜索与来源筛选纯函数、清空二次确认、面板渲染契约与预览文案、
`resolveImportRanges` 的行/列收敛与多块展开）、
`snapshot-date.test.ts`（日期序列号与 raw 取值口径）、`swap-flash.test.ts`（提醒框坐标换算 + 浮层生命周期）、
`resident-tabs.test.ts`（常驻窗口淘汰决策）、`scrollbar-drag.test.ts`（滚动条拖拽换算）、
`cell-geometry.test.ts`（像素↔格的**自有**换算，含实测校准点）、`range-merge.test.ts`（选区与合并块对齐）、
`undo-shortcut.test.ts`（撤销/重做按键的归谁管，见缺陷 L）、
`persistence.test.ts`、`history-model.test.ts`、`context-menu-model.test.ts`、`fixture-coverage.test.ts`、`perf-pipeline.test.ts`。
e2e 文件：`import` / `readonly` / `export` / `modes` / `workspace` / `workspace-undo` / `context-menu` / `history` /
`persistence` / `perf` / `date-cells` / `zoom` / `scrollbar` / `coordinates` / `prompts` / `selection-merge` /
`selection-multi` / `formats` / `file-drop` / `tabs-memory` / `hot-reload`（+ 共享 `helpers.ts`）。

> 两条与测试驱动方式有关的说明（都不是产品行为，但会影响结论可信度，所以写在这里）：
> ① `workspace.spec.ts ⑩` 里的搜索框改成**真实按键**（`pressSequentially`）而不是 `fill()`：
> `fill()` 是"直接改 DOM 值 + 派发一个 input 事件"，长跑（多个 spec 连着跑）里偶发过一次 React 没处理该事件、
> 受控输入随即被重渲染回空值。排查记录：单独跑与整文件跑都复现不出来，失败 trace 里页面没有重载
> （`[vite] connecting` 只出现一次）、输入框节点也没被替换（Playwright 的 target 标记还在），
> 所以判定为驱动方式偶发，而不是应用丢了用户输入。真实按键每个字符都带完整文本，天然抗"丢一次事件"。
> ② `App.tsx` **只导出 `App` 这一个组件**（面板的范围换算因此单独放在 `src/workspace/import-target.ts`）：
> Vite 的 React Fast Refresh 一旦发现同文件里还有"非组件导出"，就会放弃热替换、改成整页刷新 ——
> `tests/e2e/hot-reload.spec.ts` 正是这条约束的守卫（实测：多出一个导出后该用例直接退化成 skip）。

## 1. 逐项落实

### ① 只允许改内容，样式一律不动 —— 三层防线

| 层 | 位置 | 作用 |
| --- | --- | --- |
| 命令门禁（默认拒绝） | `src/univer/read-only-guard.ts` | 猴补 `ICommandService.executeCommand` / `syncExecuteCommand`：只放行白名单（`univer.command.undo/redo`、`excel-preview.command.*`、`doc.command|operation|mutation`、`formula.*`、`sheet.operation.*` 及少量读操作），其余**一律拒绝**。 |
| 命令注销 | `src/univer/lock.ts` | 30 个格式/结构命令直接 unregister，点了是 no-op（连"无反应的按钮"都不会有反馈）。 |
| 数据层剥离 | `src/univer/lock.ts` `stripStyles` | 写入单元格时只保留 `v/f/si/p`，**剥离 `s`**。 |

**关键坑（务必不要在后续改动中退化）**：`stripStyles` 只能剥 `s`，**不能剥 `p`**。
Univer 把单元格超链接存成 `p` 里的 link mark，剥掉 `p` 会导致 `getHyperLinks()` 返回 `[]`——
即"计数说有 3 个超链接、实际一个都不显示"。

`SheetIntegration` 的权限点（`interceptCommand`）在 0.25.1 **不存在**，所以门禁只能做在命令服务上；
这是默认拒绝而非按命令逐个拦截，因此新增任何 Univer 命令都会被自动挡住（不会漏）。

### ② 图片/批注只读，导入导出原样保留

- 渲染：`src/importer/apply-features.ts`（条件格式 / 数据验证 / 超链接 / 批注 / 图片）。
- 导出：`src/exporter/export-xlsx.ts` 只重写 `xl/worksheets/sheetN.xml` 与 `xl/sharedStrings.xml`，
  其余 zip 条目**逐字节保留**。端到端已断言 `xl/tables`、`xl/drawings`、`xl/media`、`xl/comments`、
  `xl/_rels` 内容一致。

### ③ 工具栏图标化、无大标题、无子表创建、风格统一

- `src/shell/Toolbar.tsx` + `icons.tsx` + `toolbar.css`：纯图标（含 title/aria-label），无大标题。
- 子表创建关闭：`src/univer/setup.ts` 里 `footer.addSheetButtonConfig.show = false`。
- Univer 自带 ribbon / 格式工具栏一并关闭（`toolbar: false`）——留着会出现"按钮点了没反应"的矛盾体验。
- 工作区与编辑器统一配色变量（`src/shell/shell.css` 的 `--topbar-h` 等）。

### ④ 可切换的三种交互模式（选择 / 拖拽 / 点击互换）

工具栏上是三个**纯图标**分段按钮（悬停有整句说明，当前模式高亮、`aria-checked` 可断言），
模式随会话持久化，重开浏览器保持不变。

| 模式 | 单击 | 按住拖动 | 多选框 |
| --- | --- | --- | --- |
| **选择** | 选中单元格 | 原生框选 | 允许（原生行为） |
| **拖拽**（默认） | 选中单元格 | **直接搬运/互换内容**（无需长按） | **不出现** |
| **点击互换** | 选中一方；再点另一方即互换 | 不搬运 | **不出现** |

- **互换/搬运完成后清空选区**：不留着选中的单元格（用户要求）。
- **按住 Shift / Ctrl 时退回原生选择**（可以框选区域）。这条规则让"非选择模式下不出现多选框"与
  "区域互换"两个需求同时成立：不按修饰键就绝不会出现多选框；按了就明确表达"我要选一个区域"。
  实测（探针）：点击互换模式下 Shift+点选得到 `B3:C5` ✓，之后普通点击仍是"选边"，区域互换照常可用。
- 与工作区条目互相拖拽、与工作区条目点击互换都保留。
- 动画：`requestAnimationFrame` + CSS transform（`swap-animation.ts` + 幽灵/落点提示）。

**怎么做到"非选择模式下不出现多选框"**（这里踩过三个坑，都留了注释）：

1. **屏蔽原生扩选**：`pointerdown` 不拦（所以"点哪格选哪格"仍是原生），但从按下开始的
   `pointermove` 在非选择模式下用 `stopPropagation` 拦掉，Univer 就再也没有机会把选区扩成一片。
2. **代价与补偿**：拦掉 pointermove 后 Univer 的 `CellPointerMove` 不再派发，落点命中改由自建命中测试
   `ISheetSelectionRenderService.getCellWithCoordByOffset` 负责。取这个服务要经
   `IRenderManagerService.getRenderById(unitId).with(...)`——它按**工作簿渲染实例**注册，
   sheet 作用域与根作用域都取不到（两者都会抛 `Expect 1 dependency item(s) ... but get 0`）。
   注意返回的是 `ISingleCell`，字段是 **`actualRow`/`actualColumn`**，读成 `row/column` 会得到
   一个"假目标"导致拖放静默失效。
3. **清选区要延到下一帧**：清空动作发生在捕获阶段时，Univer 还没处理 pointerup，会把选区状态落回来
   覆盖掉我们的清空，所以用 `requestAnimationFrame` 延后一帧再清。

**历史缺陷（已修，留作回归）**：早先实现把"按住 + 移动"交给 Univer 当扩选处理，实测选区会从
`B3` 涨成 `B3:C5` 并在松手后保留——用户看到的就是"按住以后变成多选了"，完全不像在搬运。
回归护栏见 `tests/e2e/p4-modes.spec.ts`：拖动每一步都断言"要么是源单元格、要么已清空，绝不出现区域"。

### ⑤ 全部动作可撤销/重做 + 可还原的历史记录

- 自有命令：`src/univer/swap-command.ts` 的 `SwapRangesCommand`、`ClearRangeCommand`，
  **每个命令自己 `pushUndoRedo`**——Univer 的撤销不是自动记录的，`sequenceExecute` 不会产生撤销记录。
- 历史面板：`src/shell/HistoryPanel.tsx` + `history-model.ts`：列出动作，可回退/前进到任意一步。
- **账本是唯一顺序来源**（`entriesRef` 同步数组 + `historyIndexRef`；`historyEntries` state 只负责渲染）：
  每条记 `scope: 'sheet' | 'workspace'`。撤销/重做按账本时间顺序切换——
  上一步是工作区动作就用我们自己的 `{before, after}` 快照回放，否则交给 Univer 的撤销栈；
  工具栏按钮的可用态也严格取自账本下标（不再是"Univer 有栈就能点"）。
- **一次动作 = 一条历史**：我们自己的表格命令会同时被 Univer 的 `undos` 计数（订阅自动补一条）与
  `pushHistory` 抓到，曾因此多出一条"幽灵步骤"（撤销白走一步）。现在动作开始打标
  （`beginSheetAction`），结束时按 id 精确认领这段自动条目并合并成一条、用我们的措辞命名；
  我们自己的 `undo/redo` 引起的计数变化也不重复入账（`suppressAutoEntriesRef`）。
- **撤销/重做的按键归我们管**（缺陷 L 第 3 条）：在 Univer 引导**之前**注册第一顺位的 window 捕获监听，
  处理时 `stopImmediatePropagation()` 截下按键；只有焦点真在单元格编辑器/公式栏里才让路给 Univer。
- **Univer 的 `undoRedoStatus$` 会漏报手工 push 的记录**（实测：交换后 `undos` 仍为 2），
  所以历史用自有账本；`available` 只用于面板可达性提示，不再回写账本下标。

### ⑥ 误关闭后重开恢复现场

- `src/persistence/session.ts`：手写 IndexedDB 封装（不用 base64），800ms 防抖、写入串行化、
  `pagehide` / `visibilitychange` 强制 flush。
- 恢复内容：标签页（文件名 + 原始字节 + 解析结果）、编辑（脏单元格）、工作区条目、交互模式、活动标签。

### ⑦ 单选/多选 + 右键菜单

- 选区：选择模式下是 Univer 原生选区（含矩形多选）；拖拽/点击互换模式下只会有单个单元格选中，
  且动作完成后清空。
- 右键菜单 `src/shell/ContextMenu.tsx` + `context-menu-model.ts`，动作一律按**打开那一刻的选区快照**执行。
- 菜单项：**复制到工作区** / **剪切到工作区** / 与工作区互换 / 复制内容（**单块含格式、多块纯文本**）/ 清空内容（保留格式）/ 撤销 / 重做（复制出去的完整行为见「修复 Y」）。
  - 复制：内容进工作区，源区域不动；
  - 剪切：内容进工作区后把源区域清空（**只清内容、格式保留**），并记进历史，一次撤销即可还原。

### ⑧ 多标签页 + 工作区跨表共享

- `src/App.tsx` 的标签注册表（`tabsDataRef`：id/文件名/解析结果/原始字节），单一 Univer 实例切换标签。
- 工作区条目只在内存与 IndexedDB 里存内容快照，因此表1 → 工作区 → 表2 天然跨表可用。
- 标签**数量不设上限**（工具栏标签条，可横向滚动）；重复打开同一个文件会各开一个标签（不去重）。

**多标签的内存治理（本轮新增，用户要求"尽量复用资源、少占内存"）**

问题：Univer 每个**实体化**的工作簿都要建模型 + 渲染器 + 插件资源，百万格量级下每个标签
≈480–670 MB；开着 N 个就是 O(N)。而"打开的标签"与"当前实体化的标签"本来不必是一回事。

做法（`src/shell/resident-tabs.ts` 纯函数决策 + `src/App.tsx` 装配）：

| 机制 | 说明 |
| --- | --- |
| 常驻窗口 K=3 | 同时实体化的标签**最多 3 个**（含当前）。超出时按"最久未用"把非活动标签**冷存** |
| 冷存 | 先按 id 快照该标签的编辑 → `disposeUnit(id)` 释放工作簿与渲染器 → 字节/文件名/编辑桶都留着 |
| 切回重建 | 走与导入完全相同的管线：解析字节 → 适配 → 建簿 → 应用特性 → **回放编辑** |
| 撤销栈 | 重建后 `clearUndoRedo(unitId)` 并把我们的历史账本一起清零（Univer 的撤销栈不会随 dispose 清理，不清就会把旧 mutation 打到新模型上） |
| 启动恢复 | 以前是 for 循环把**所有**会话标签都解析建簿（这是最大的内存风险点）；现在只实体化要看的那个，其余先冷着 |
| 顺手清理 | 首个真实文件进来后释放启动示例簿；关标签时清编辑桶 / 记账 / 撤销栈 |

**实测（`tests/e2e/tabs-memory.spec.ts`，`performance.memory.usedJSHeapSize`，1.2 MB 中等夹具开 4 个标签）**：

| 步骤 | 堆（MB） | 实体化标签数 |
| --- | --- | --- |
| 启动 | 45 | 0 |
| 导入第 1 个 | 111 | 1 |
| 导入第 2 个 | 310 | 2 |
| 导入第 3 个 | 347 | 3 |
| **导入第 4 个** | **194** | **3** |

第 4 个标签进来时第 1 个被冷存，堆**从 347 MB 掉回 194 MB**——说明 `disposeUnit` 真的把内存还回去了
（这一点在 `M4-多标签内存分析.md` 里原本标着"未验证"，现已实测）。内存从 O(N) 变成 O(K)。
同一用例还断言了"改过的格子在冷存→重建后内容仍在""重建后撤销栈为空"。

### ⑨ 工作区 = 一份**独立单元格的集合**（快速导入、逐格拆分、搜索筛选、可选语义、条目剪贴板）

**工作区的数据模型（本轮重做）**：工作区不是"几个区域快照"，而是**一个个独立单元格**。
用户的原话是"工作区相当于一张小表格，放进去的单元格会变成一个个独立单元格，并保留它原本的尺寸"，
所以：

| 规则 | 实现 | 说明 |
| --- | --- | --- |
| **逐格拆分** | `src/workspace/snapshot.ts` 的 `extractCellItems(sheet, a1, max)` | **任何入口**（拖拽入侧边栏 / 右键「复制·剪切到工作区」/「+」导入 / 一键全搬 / 单元格↔条目互换 / 会话恢复）都走同一套判定，把区域拆成 **1×1 条目**；返回 `{ items, skippedEmpty, truncated, error? }` |
| **行优先** | 双重循环 `for r → for c` | 拆出来的条目按**行优先**顺序，新条目整批排在列表**最前** |
| **跳过空内容** | `isEmptyContent(value, formula)`（唯一口径） | **绝对跳过**：没有公式且文本 `trim()` 为空就算空 —— 覆盖 `null` / **空字符串（用户在编辑器里删光）** / 半角空格 / 全角空格 / 换行；`0`、`false`、公式**都算内容**。跳过数记在 `workspace:skip-empty`，一次操作产出的条目数 = 该区域**有内容的格子数**。见缺陷 M |
| **保留尺寸** | 条目带 `cellSize: { width, height }`（`readCellSize`） | 工作区按它展示，夹在 `tileMinWidth−16 … 200` × `18 … 64`；拿不到尺寸时回落 104×24 |
| **上限 500** | `MAX_WORKSPACE_ITEMS_PER_ACTION = 500` | 一次操作最多 500 个条目，超出部分不放入并记 `workspace:truncated`（防止"一键全搬"把超大表塞爆） |
| **一键全搬** | 面板里的 `data-testid="import-whole-sheet"`（文案「整个已用区域」） | 点一下把该表**已用区域**填进输入框（并立刻给出预览），再按确认键提交；App 通过 prop `usedRangeA1`（`sheet.getDataRange()`）传入；空表禁用 |

**「+」入口在工作区右上角**（从顶部工具栏挪过来，工具栏原先那个按钮已移除）。面板（`src/workspace/ImportDialog.tsx`）
只保留**三样有用的东西**：一个输入框（**自动识别**行/列/区域）、一块**实时预览**、一个**保留/剪切**选项。

| 输入 | 识别为 | 提交值 |
| --- | --- | --- |
| `3` / `3:5` | 行 | `3:5` |
| `B` / `B:D` | 列 | `B:D` |
| `A1` / `A1:B18` / `$A$1:$B$18` | 区域 | `A1:B18` |
| `A1:B2 D4:E5` | **多块区域**（与表格里 Ctrl+点选同款写法） | 原样空格分隔 |
| 其它（含内部空格、全角） | 非法 → 内联报错 + 禁用确认键 | — |

| 面板元素 | 行为 |
| --- | --- |
| 输入框 + 「识别为：…」 | 边打字边识别；非法输入**立刻**给出具体原因（哪种写法才对），确认键同步禁用 |
| 「当前选区 B2:D5」/「整个已用区域 A1:H21」 | **只填输入框**，不直接提交——先让用户看到预览再确认（以前"整个已用区域"是按下去就搬，用户不知道会搬多少） |
| **实时预览**（`import-preview`） | `将加入 12 个单元格 · 跳过 156 个空内容`，并列出前 6 格（`A1 张三`…）+ `…还有 N 格`；超上限时红字提示"只会加入前 500 格" |
| 预览的**只读安全阀** | 面板是边打字边预览的，所以预览只扫**与已用区域的交集**（`clipRect`）：写 `A1:A1048576` 也不会逐格扫百万行把页面钉死；"已用区域之外没有内容"是 `getDataRange()` 的定义本身，`将加入 N 格` 因此仍然是精确值（空内容的报数按原始选区面积算，上到 10 万就不再展示） |
| 确认键 | 文案带数字：**「加入 12 格」**（没算出预览时回落「加入工作区」） |
| **加入工作区后保留表格内容**（`import-keep-source`） | 勾选＝复制（表格不动）；取消＝**剪切**：内容先进工作区，再清空源格内容（保留格式）。与顶部工具栏「拖到工作区保留内容」是**同一个持久化设置**（`keepSourceOnDrop`），面板里改一下就会被记住 |

取证截图：`evidence/p3-panel-after.png`（默认复制语义：预览报"将加入 35 个单元格 · 跳过 133 个空内容"，
确认键写着「加入 35 格」）、`evidence/p3-panel-cut.png`（取消勾选后提示改口为剪切语义）。

**被删掉的无用元素**：原来那个 `import-skip-empty` 复选框（"跳过空单元格（固定行为，不可关闭）"）——
它不可点、容易被误读成"能关"，属于纯占位。跳过空内容本来就是唯一入口 `extractCellItems` 的固定行为，
现在改成在**预览里如实报数**（`跳过 156 个空内容`），用户看到的是结果而不是一句保证。

**撤销口径（用户重点要求）**：面板自己**不改任何状态**，只把 `{kind, value, keepSource}` 交给上层——
工作区那一半走**唯一提交入口** `commitWorkspace`（带 before/after 快照的历史条目），
剪切那一半走表格侧的统一通道（每块一条 `beginSheetAction` + `clearRange` + `pushHistory`）。
顺序是**先提交工作区、再清源**，于是撤销时**先还原表格内容、再撤工作区**，任何一步之后内容都在某一边，不会丢。
e2e 逐条守住：`workspace-undo.spec.ts` ⑧（剪切：一次撤销回内容、两次撤销回工作区）、
⑨（复制：一次撤销只撤工作区、表格分毫不动）、⑩（打开文件不留幽灵条目、之后的编辑照常可撤）。

**搜索与来源筛选**（`src/workspace/filter.ts`，纯函数）：
按**内容**搜（不区分大小写、忽略首尾空格）、也认**来源 A1**（搜 `b12` 直接定位 B12）；
下拉按**来源工作表**筛（第一项 `全部来源（N）`，其余 `表名（M）`）；筛选生效时出现 `M/N · 清除`，
Esc 清空搜索；一条都没有时显示 `没有匹配的单元格（共 N 个）`。

**外观**：面板与条目卡片都是圆角 + 边框 + 阴影，与表格区同款令牌。
条目 = **一格一个小方块**：正文里只有单元格本身（**没有来源角标、没有 × 按钮**），
来源信息在 `title` 悬浮提示里（`{内容} · 来源 {工作表}!{A1}（右键可复制/剪切/删除）`）；
**每行放几格由"面板宽度 ÷ 格宽"自动决定**（CSS `repeat(auto-fill, minmax(tileMinWidth, 1fr))`），
底部「格宽 N px」（`tileMinWidth`，80–220，默认 128）面板保持无 hooks、过滤状态在上层。

**宽度可拖**：表格区与工作区之间有一条分隔条（`data-testid="sidebar-splitter"`），
左右拖动即可调整工作区宽度（等价于调整表格区宽度），范围 200–640px，随会话持久化；
拉宽后每行自动多放几格。

**两个语义开关**（都持久化）：

| 位置 | 选项 | 默认 | 行为 |
| --- | --- | --- | --- |
| 表格顶部工具栏 | 拖到工作区保留内容 | 勾选 | 取消勾选时，拖到工作区会把源区域**清空内容、保留格式**（剪切语义），并记进历史可撤销 |
| 工作区底部 | 写回表格后移除条目 | 不勾选 | 勾选后，条目拖回/写回表格即从工作区移除 |

**清空要二次确认**（清空是不可逆的批量操作）：点右上角「清空」不会直接清，先出现确认条
（`role="alertdialog"`，`data-testid="workspace-clear-confirm"`）——无筛选时是
`确定清空全部 N 个单元格？`；**筛选生效时**多一个「清空显示的 M 个」按钮，文案变成
`清空显示的 M 个，还是全部 N 个？`；Esc 或「取消」都会原样保留条目。

**条目剪贴板**：在工作区条目上**右键**可「复制 / 剪切 / 删除」（卡片上没有 × 按钮，删除只走这里）；
复制或剪切后，在表格里右键选「粘贴「条目」（或按 Ctrl/Cmd+V）即可把内容写进目标位置——
**只写内容、目标格格式不变**，并记进历史（一次撤销即还原）。

**互换后的"落点提醒"**：互换完成**不再留下普通选中**（用户明确要求"互换完不要继续选中单元格"），
改用**黄色提醒框**把刚互换的两个区域标出来，**逐渐透明后自动消失**（约 1.6 s），
点击表格任何位置也会立刻收掉；提醒框**完全不吃鼠标事件**。

- 颜色 `#facc15`（亮黄描边 + 极淡黄填充），位置每帧跟随滚动/缩放；
- 实现是**自建 DOM 浮层**（`src/interaction/swap-flash.ts`）：CSS 透明度动画走合成器，
  不重绘 Univer 画布（大表不掉帧），`pointer-events: none` 让"不影响操作"成为结构性保证；
- 坐标换算公式取自上游命中测试链路（`getTransformOffsetX` 的反解），并且**自带自检**：
  每个框算完后拿中心点做一次命中测试，落不回原格就记 `swap:flash-rect-mismatch` 日志
  （e2e 断言这条日志必须为 0），宁可暴露也不画错位置的框；
- 选区清理补了两帧：Univer 的选区写入是异步命令，`pointerup` 之后的 `click` 有时会把选区写回来，
  只在当帧清一次会漏（用户看到的正是"互换后还选中着"）；
- 取证截图 `evidence/p3-swap-flash-color.png`（A3 是**合并单元格**，提醒框也正确覆盖整块）。

> 实现演进（两次失败都记下来，别再回去）：
> ①把自定义样式塞进 `SheetsSelectionsService.setSelections` 的 `style` 字段——**无效**，
> 选区渲染用的是自己 protected 的 `_selectionStyle`，看到的还是主题默认蓝框（用户反馈"看着像还选中着"）；
> ②改用标记图层 `fWorksheet.highlightRanges()`——颜色对了，但**做不了渐隐**，且每次刷新都要重绘画布。

### ⑩ 极致性能

**基准夹具**（`npm run bench:fixture`，不进仓库）：5 万行 × 20 列 = **100 万格**、6.1 MB，
混合共享字符串 / 数值 / 公式 / 样式 / 空单元格（纯数字灌水测不出真实开销）。

**Node 侧管线实测**（`npm run bench:parse`，含预热）：

| 阶段 | 优化前 | 优化后 | 吞吐 |
| --- | --- | --- | --- |
| 解压 + 解析 | 5501 ms | **1180 ms** | 5.1 MB/s · 848k 格/s |
| 适配 IWorkbookData | 142 ms | **88 ms** | 11.4M 格/s |
| 合计 | 5643 ms | **1268 ms** | — |
| 堆内存 | 337 MB | **237 MB** | — |

连续跑 3 次解析：5066 / 4974 / 4972 ms → 优化后无累积劣化（排除泄漏）。

**浏览器端实测**（`tests/e2e/perf.spec.ts`）：

| 指标 | 数值 |
| --- | --- |
| 端到端墙钟（选中文件 → 解析/适配/建簿完成） | **1439 ms**（解析 1177 / 适配 60 / 建簿 176 / 特性 11） |
| 拖动帧间隔 | p50 **4.2 ms**（≈238 FPS） · p95 **20.8 ms**（≈48 FPS） |
| 导入后 12 秒内 >32ms 的帧 | 7 帧（1515 帧中） |
| JS 堆 | 543 MB |

**做了什么（按收益排序，全部有 profile 支撑）**：

1. **消灭重复的全文档遍历**（最大头，占总耗时 ~65%）。
   `findFirstElement` 命中后只跳过该子树、**仍把整份文档走完**；一张表还要为
   条件格式 / 数据验证 / 超链接 / 合并区各走一遍。改成：命中即停（`stopOnFirstMatch`），
   并让这些"排在 `<sheetData>` 之后"的特性从尾段起步（`earliestNameAt` 会在元素真的
   出现在更前面时自动前移起点，因此不依赖 schema 顺序也不漏）。
2. **行 / 单元格内联扫描**：`parseSheetRows` 不再为每个 `<c>` 建 `ElementRange` +
   再调一次 `findCloseTag`，而是用 indexOf 直接取 `<v>/<f>` 的文本并顺带得到单元格结束位置。
   遇到自闭合、`<is>` 富文本、扩展元素或非无前缀写法一律**回退通用实现**（宁可慢也不丢数据）。
3. `childElements` 改为在原串上做"深度 1 扫描"，去掉每个子元素一次 `slice` + 偏移折算 + 展开对象。
4. `visitElements` 支持 `from/to`；CF / DV / 超链接扫描全部改为区间扫描。
5. 微优化：结束标签前缀缓存（省掉上百万次临时字符串）、标签名停止符内联比较（省掉 Set 哈希）。

**过程中的真实教训**：
- 用 `getImageData` 轮询画面测"首帧"会**污染被测对象**——超大 canvas 的像素回读本身就是
  几百毫秒到数秒的长任务。已改为 rAF 间隔判定"主线程静默"，不碰像素。
- 自闭合单元格 `<c r="A1" s="1"/>` 若按"有子元素"处理，会把**下一个单元格的内容吞掉**；
  这个 bug 是被现有单元测试（连续自闭合兄弟、真实夹具行列数）当场抓住的。

**尚未做**：多标签时的内存。
单标签的常驻内存问题已解决（见下节"内存优化"）：本工具自己不再驻留解析模型。
但**每个百万格标签页里 Univer 自己的工作簿**仍占很大一块（实测：1 个标签 459 MB、
2 个 1125 MB、3 个 1607 MB，未强制 GC 的口径）。这是 Univer 的内存结构决定的，不是本工具的解析模型。
若需要进一步压缩，方案是"标签较多时把非活动标签 `disposeUnit`，切回时从原始字节重建 + 回放编辑"，
代价是切回要付一次重解析（百万格约 1.2 s）且要处理每个标签的撤销栈——属于设计取舍，**本轮未做**。

### 内存优化（本轮追加）

**问题**：导入完成后，每个标签的 `tabsDataRef` 都保留着**完整** `ParsedWorkbook`（含上百万个
`ParsedCell` 对象、样式表、行/列信息、未解压的 zip 条目），而运行期只有"导出"会读它。

**做法**（`src/exporter/slim-source.ts`）：
1. 先把导出真正需要的东西压成最小形状——`sheet.id` + "每行第一个带样式的 s"（501 条），
   其余（`cells` / `rows` / 样式表 / 条件格式 / 数据验证…）一律丢弃。
2. zip 条目也不再常驻：改成只留**原始 xlsx 字节**（本来就要留给会话恢复），导出时**惰性解压**。
   代价是每次导出多一次解压（6 MB 文件实测约 0.2 s），换来几十 MB 常驻内存。
3. 导出签名改为结构化的 `ExportSource`（`raw.entries` 或 `bytes` 二选一），完整 `ParsedWorkbook`
   仍然满足该结构，所以既有调用与测试不用改。

**验证**：

| 指标 | 优化前 | 优化后 |
| --- | --- | --- |
| 浏览器 JS 堆（百万格导入后） | 543 MB | **446 MB** |
| Node 常驻工作集（管线跑完 + GC） | 模型 93 MB 仍在 | **90 MB**（只剩适配结果 + 瘦身数据） |
| 解析模型能否回收 | — | 可以：独立进程实测断裂引用 + GC 后回落到基线 7 MB（**无模块级泄漏**） |
| 导出产物 | — | **逐字节不变**（3 个夹具 × 完整模型 vs 瘦身来源对比，见 `tests/unit/export.test.ts`） |
| 瘦身耗时 | — | 10 ms |

另外说明两点口径问题，避免误读：
- 上表"446 MB"是 Chrome `performance.memory.usedJSHeapSize`，**不含 `Uint8Array` 的外部内存**；
  优化前那块"未解压的 zip 条目"也一并去掉了，所以真实收益比 −97 MB 更大。
- Node 侧的内存数字曾经量不准：**在同一个函数帧里把变量置 null 再 GC，堆水位经常不降**——
  死引用还留在寄存器/栈槽里，保守栈扫描把它当活对象。所以基准现在拆成两个进程
  （`tools/bench-parse.mjs` 先跑内存探针、再跑计时主进程），并在文档里标明口径。

### ⑩ 之外：性能回归护栏

- `tests/unit/perf-pipeline.test.ts`：百万格夹具上断言解析/适配耗时上限（阈值宽松到只拦
  数量级劣化），并跑 3 次检查不出现累积型劣化。夹具缺失时自动跳过（大文件不进仓库）。
- `tests/e2e/perf.spec.ts`：端到端墙钟、主线程静默、长任务、拖动帧率，阈值同样宽松。

**为什么阈值放得很松**：它们的用途是"抓复杂度写错导致的雪崩"，不是卡机器快慢；
卡太紧会在慢机/CI 上变成 flaky，反而让人开始忽略红灯。真实数字靠上面两张表人工复核。

## 2. 本轮修掉的真实缺陷（都有实测根因）

### 缺陷 A：自家右键菜单被 Univer 原生菜单盖住，用户点不动

- 现象：Playwright 报 `<button class="univer-...">` **intercepts pointer events**，且"元素不稳定"。
- 定位：诊断脚本在菜单项中心的 `document.elementFromPoint()` 上发现页面同时存在**两个**
  `SECTION.univer-popup`（z-index 都是 1070）——Univer 原生的那个 DOM 在后、盖住自家菜单。
  容器捕获阶段的 `preventDefault()` 拦不住它（它不是由 `contextmenu` 事件触发的）。
- 修复：`src/univer/setup.ts` 里 `UniverSheetsCorePreset({ contextMenu: false })`
  （`@univerjs/sheets-ui` 读 `config.contextMenu`，默认 true）。
- 结果：诊断复测 `hitIsInsideMenu: true`、`overlays: []`；三个菜单用例改用**真实坐标点击**后全部通过。

### 缺陷 B：清空内容调用了被自己门禁拒绝的命令 → 静默空操作

- 现象：`after.value === before.value`，日志显示 `menu:select`、`menu:clear` 都正常。
- 根因：原实现调 `sheet.command.clear-selection-content`——它不在守卫白名单里（默认拒绝），
  而且它作用于"当前选区"而不是右键的那个区域。
- 修复：新增 `ClearRangeCommand`（`excel-preview.command.clear-range`，守卫允许、自带撤销）。

### 缺陷 C：清空不可撤销

- 根因：最初的实现是"让区域与自己互换"（两个 range 相同、两个矩阵都空），
  undo 写回的自然也是空矩阵——**结构上不可能恢复**。
- 修复：`ClearRangeCommand` 在 redo 写空内容矩阵、undo 写回执行前抓取的原内容矩阵。

### 缺陷 D：拖动日期时弹出两个原始 i18n key（用户实测反馈）

- 现象：拖日期单元格时弹出标题 `sheets-ui.info.error`、正文 `sheets-ui.info.forceStringInfo`
  ——两串生 key，而不是人话。
- 查证（读 `@univerjs/sheets-ui@0.25.1` 源码 + 遍历全部语言包）：
  1. `src/controllers/force-string-alert-render.controller.ts` 订阅的是
     **鼠标悬停单元格**（`IHoverManagerService.currentCell$`，不是"选中"），
     当格子的 `t` 是 `STRING/FORCE_STRING` 且值看起来是数字、且数字格式又不是文本格式时弹提醒。
     拖动过程中指针会扫过一个个格子 → 于是"拖日期"时弹出来。
  2. 上游**漏配文案**：`t('sheets-ui.info.forceStringInfo')` 有调用，但 en-US / zh-CN
     等所有语言包里都**没有这个 key**，所以任何语言下都只会显示 key 本身。
- 我们这边的真元凶（值退化）：日期格的取值口径错了——Facade **单格** `getValue()` 返回的是
  格式化后的字符串 `"2025-01-01"`，而 `getValues()` 可能返回 `Date` 对象。两者一旦被
  `String()` 归一化，日期就永久变成文本（写回后不再是日期，也不再参与日期计算）。
- 修复（三处，全部有测试钉住）：
  1. `src/workspace/snapshot.ts` 全线改成**优先 raw 值**（`getRawValues()`）：
     `extractCellItems`（拖进工作区）、`extractSnapshot`、`buildValueMatrixFromRect`（互换/写回）。
     raw 值对日期格就是序列号 `45658`，配目标格自带的日期格式，写回仍是日期。
     顺带收益：一键盘点整块只取一次值，不再逐格调取值 API。
  2. `normalizeValue` 兜底：万一只拿到 `Date`，用 `dateToExcelSerial()` 转回序列号
     （1900 日期系统，1970-01-01 → 25569、2025-01-01 → 45658），绝不 `String(date)`。
  3. `src/univer/setup.ts`：给 `sheets-ui.config` 写 `disableForceStringAlert: true` 关掉这个弹窗，
     并补上中文文案兜底。理由：本产品**锁死格式**，提醒里说的"改成数字"用户根本做不到，
     弹窗纯噪音；学号/身份证/手机号这类"文本存数字"在班主任场景极常见，留着等于指针扫过就弹。
     **信息不丢**：只关弹窗，`disableForceStringMark` 保持默认，单元格左上角的绿色小三角照常显示
     ——取证见 `evidence/p3-forcestring-mark.png`（选中的 `20240101` 文本格角标在、悬停无弹窗）。
- 踩到的坑（值得记）：
  - 这条配置**写一次不生效**：`createUniver()` 返回后插件生命周期还没跑完，sheets-ui 插件随后用
    非 merge 的 `setConfig` 把整个 `sheets-ui.config` 覆盖掉（读回来只剩 `formulaBar`/`statusBarStatistic`）。
    现在引导时写一次 + **每次新建工作簿后再补一次**（`loadWorkbook`，幂等）。
  - 不能靠 `subscribeConfigValue$` 监听回写：上游那个实现用 `hasOwnProperty` 判断 `Map` 的键，永不触发。
  - 工作表 Facade 的 `getInject()` 是渲染/子表作用域，里面**没有** `IConfigService`（会抛
    "Expect 1 dependency item(s) … but get 0"），读配置必须走根 injector（`univer.__getInjector()`）。
- 验证：`tests/unit/snapshot-date.test.ts`（9 例：序列号锚点、本地日历语义、raw 优先、空格跳过、公式保留）
  + `tests/e2e/date-cells.spec.ts`（4 例）。e2e 第 ④ 例带**反向对照**：先用测试开关把配置改回 `false`，
  用真实鼠标悬停造出来的"文本格装数字"格子，确认**真的能抓到**那个弹窗；再恢复 `true`，同一格不再弹。
  这样"没有弹窗"才不是一句空断言。

### 缺陷 E：右下角缩放条拖不动（用户实测反馈）—— 元凶是我们自己的闸门

- 现象：浏览器缩放（Ctrl+滚轮）能用，但 footer 里那个缩放组件的**滑块拖动毫无反应**，
  百分比菜单点了也没用；而 −/+ 按钮正常。
- 定位：读 `@univerjs/sheets-ui` 的 `ZoomSlider` 组件——它拖动时发的是
  `SetZoomRatioCommand`（`sheet.command.set-zoom-ratio`），百分比菜单的 50%/75%/… 预设同样走这条；
  而 −/+ 按钮走的是 `ChangeZoomRatioCommand`（`sheet.command.change-zoom-ratio`）。
  我们的只读闸门是**默认拒绝**，白名单里只放了后者 → 滑块被静默拦掉。
- 修复：白名单补 `sheet.command.set-zoom-ratio`（它只写 `set-zoom-ratio` operation：纯视图、
  不进撤销栈、不改文档快照与样式，与已放行的 `change-zoom-ratio` 同类）。
- 验证：`tests/e2e/zoom.spec.ts` 两例——①真实鼠标拖滑块手柄（100% → 变小 → 拖回变大，
  百分比文字与模型值一致，且**被拦清单里不能出现缩放命令**）；②百分比菜单选 50% 生效 + −/+ 可用。
- 组件结构（实测，写测试要用）：footer 里 `div[role="track"]` + `button[role="handle"]`（12×12 圆点），
  旁边是百分比按钮（点开是 `div[role="menu"]` + `div[role="menuitemradio"]` 的 50%–400% 预设）
  与减/加号按钮；**只有拖手柄有效**，点轨道空白处不动（上游就没实现跳转）。

### 缺陷 F：滚动"无法正常使用、顿挫感"（用户实测反馈）—— 两处都是我们自己的问题

1. **滚轮完全滚不动**：Univer 的滚动条/滚轮链路里，滚轮走的是
   `SheetsScrollRenderController._wheelEventListener` → `SetScrollRelativeCommand`
   （`sheet.command.set-scroll-relative`），而我们的白名单里**漏了它** ——
   每滚一次被拦一次（实测日志：一次滚轮 = 一条 `sheet.command.set-scroll-relative` 拦截记录）。
   修复：白名单补这条命令（纯视图，只改视口滚动位置）。
   顺带补上 `focusSheetUnit()`：`FOCUSING_SHEET` 上下文标记只有 `IUniverInstanceService.focusUnit()`
   会置位，而滚轮处理第一行就要求它为真；编辑器/公式栏拿焦点会把它清掉，所以在表格里按下指针要重新置位。
2. **拖滚动条被当成内容拖动**：非选择模式下我们会在 `pointerdown` 后拦掉 `pointermove`
   （用来阻止 Univer 把拖动当扩选、画出多选框）。但 Univer 的滚动条**就画在主画布上**，
   于是拖滚动条时移动事件被我们在捕获阶段掐掉 → 拖不动、只在松手时跳一下（"顿挫感"），
   甚至可能误触发一次内容互换。
   修复：按下时先问引擎 `Viewport.getScrollBar().pick(coord)`（`src/App.tsx` 的 `isScrollbarHit`），
   命中滚动条就不按内容拖动处理、也不拦 pointermove，并记 `scrollbar:drag-start` 便于诊断。
- 验证：`tests/e2e/scrollbar.spec.ts` 7 例——⓪真实夹具下按正中/偏左 3px/偏右 3px 都算滚动条
  ①滚轮纵向/横向都能滚（且滚动命令不在被拦清单里）②编辑过单元格后滚轮仍可用
  ③**按住滑块拖动：不跳、跟手、原路拖回回到原处、两端夹住** ③b 横向同理
  ④拖滚动条不搬运内容 ⑤横向滚动条按偏也不误触发。
- **滚动条交互模型 = 普通滚动条**（用户要求"按住然后可以左右或者上下拖动"）：
  `src/interaction/scrollbar-drag.ts`（纯函数 + `tests/unit/scrollbar-drag.test.ts` 13 例）：
  - 滑块长度 = 视口/内容 × 轨道长；滑块位置 = 滚动/上限 × 可移动距离（互为逆运算）；
  - 按住**滑块** → 记抓取偏移，拖动时**严格跟手**（指针位移多少滑块走多少，不跳也不飘）；
  - 按住**轨道** → 滑块先跳到指针处，随后继续拖同样跟手；拖出两端自然夹住；
  - 拖动期间在滚动条上盖一条**抓取高亮**（`.scrollbar-grab`，穿透浮层），松手即收；
  - **悬停也在条带里给一条淡虚线提示**（不按下也看得见"这儿能抓"）。
- **"抓不住"是怎么修的（第三次修订，用户反馈"完全拖不动"）**——三个叠加原因：
  1. **按精确命中判定**：早先要求引擎 `pick()` 在指针处命中滚动条。实测真实夹具几何：
     **主画布 1110px 宽、主视口只有 933px**，滚动条画在视口右缘、**只有 5~6px 宽**，
     它右边还有 170 多像素"死区"（那里没有单元格）。指针偏一两像素 → 命中失败 → 既不滚也不拖，
     就是"完全没反应"。
     现在改为**条带判定**：`isInBarStrip()`——视口右缘/下缘往里 14px，外加**视口外到画布边缘的整段死区**
     都算滚动条；死区本来没内容，划进来只赚不亏（取 14px 是为了不抢"最后一列单元格"的拖拽）。
  2. **几何依赖 `pick` 的命中矩形**：轴向、滑块长度全靠 `pick()` 返回的矩形推断，一旦没命中就全盘失效。
     现在轴向由"离哪条条带更近"决定；滑块长度/滚动上限优先用引擎给的数，拿不到就用
     "内容尺寸 − 视口尺寸"推算（任何一项缺失都不会退化成"什么都不发生"）。
  3. **拿错了画布**：页面里每个工作簿一块 `univer-sheet-main-canvas*`，`querySelector` 取到的是
     **DOM 里第一块**（可能是别的/已隐藏的工作簿），坐标与视口几何就对不上。
     现在统一走 `activeCanvas()`：先按活动 unit 的 id 精确定位，再退回第一块。
- 验证：`tests/e2e/scrollbar.spec.ts` 9 例，其中新增 **⑥ 条带内任意位置（距右边缘 4/12/60/120px）
  按下并拖动都要滚动、都不能变成内容拖动，而网格内部仍应正常拖单元格**；
  **⑥b 悬停条带显示"可抓"提示、离开即收**；另有 ③ 按住滑块"不跳 + 跟手 + 原路拖回 + 两端夹住"、
  ③b 横向同理、⓪ 真实夹具按偏也不误触发、①②滚轮、④不搬运内容、⑤横向条带。

### 缺陷 G：滚动之后提醒框画错位置 / **拖动起点与落点严重偏移**（用户实测反馈）
- 现象 A：用过滚动条再互换，黄色提醒框出现在**没滚动时**的位置（甚至跑到画布外被裁掉）。
- 现象 B（更严重）：**表格滚动之后，拖动的落点解析成"没滚动时"的格子**——实测指针停在 S74，
  app 认成 D13，于是互换发生在完全不相干的格子上。起点看着正常只是因为起点取自**选区**而不是命中测试。
- 同根因：两处都用了上游那套滚动换算 `content = (css − scroll)/scale + scroll − header`，
  它在 scale=1 时**把滚动量消掉了**。
- 修法：
  1. 正向（格 → 像素，提醒框/落点高亮）：`css = (content − scroll) × scale`（`swap-flash.ts`）。
  2. 反向（像素 → 格，命中测试）：**不再用渲染服务的 `getCellWithCoordByOffset`**，改自建
     `src/interaction/cell-geometry.ts`：内容坐标 = `css / scale + scroll − 表头`，再在行/列累计偏移里二分查找。
- **实测校准**（这是关键，别再凭直觉改）：
  - `columnWidthAccumulation[i]` / `rowHeightAccumulation[i]` 是**累计边界**（含第 i 列/行、不含表头），
    所以查找必须是"**第一个 > 坐标**的下标"（写成"最后一个 ≤"会少一格——第一版就是这么错的）。
  - 用真实夹具（**带冻结窗格**）对照：点像素 → 读 Univer 选中的格（真值） → 反推公式；
    校准后命中测试与真值**完全一致**（S74 → S74、O68 → O68）。
- 验证：`tests/e2e/coordinates.spec.ts` 新增用例 ⓪——真实夹具纵横滚动后，
  拖动起点/落点必须等于"点该像素 Univer 认的那一格"，且提醒框覆盖这两格；
  另有 ①②③ 覆盖未滚动/滚动/横向滚动/150% 缩放下的"矩形包住像素"与"提醒框位置误差 < 2px"。
  单测 `tests/unit/cell-geometry.test.ts`（10 例，含实测校准点与"边界压在格线上属于下一格"）。

### 缺陷 G2：拖滚动条变成内容拖拽（用户实测反馈）

- 现象：拖表格滚动条时，反而触发了单元格拖拽/互换。
- 定位：滚动条只有 6~10px 宽，命中判定用的是引擎 `pick()` **精确命中**——用户（或指针）偏一两像素
  就落到画布上，于是走了"内容拖动"分支。
- 修法：`getScrollDragGeometry` 加**容差**：`pick` 没命中时沿轴扫出滚动条带子，
  落在带子 ±8px 内也算按在滚动条上（按偏了按"点轨道"处理：先跳到该位置再拖）；
  另外 `limit` / 滑块长度拿不到时用"内容尺寸 − 视口尺寸"与比例兜底。
- 验证：`tests/e2e/scrollbar.spec.ts` 新增用例 ⓪——真实夹具下按正中、偏左 3px、偏右 3px
  都必须记 `scrollbar:drag-start`、都**不能**出现 `app:drag-start` / 互换 / 搬运，且确实滚动了；
  横向滚动条按偏 3px 同理。

### 缺陷 J：把 xlsx 拖进窗口打不开（用户实测："无法打开 2502班座位表 单座 2026.9.7.xlsx"）

- 定位：先排除了文件本身与解析链路——用我们的解析器/适配器跑这个文件**都成功**（178 个单元格、
  适配出 1 张表、25 个样式），在浏览器里用"打开 xlsx"按钮导入也成功、内容渲染正常
  （实测读到 51 个非空格子：座位表标题与"曹煜涵/刘杰/郭嘉诚…"等姓名）。
  真正的问题是：应用自己的提示写着"**拖入 xlsx** 或使用左侧示例开始"，
  但**根本没有接文件拖放**——拖进来毫无反应（`dom:drop` 只有个诊断探针在记日志，没人处理），
  浏览器还可能直接跳转去打开那个文件。用户"拖进去打不开"就是这个。
- 修法（窗口级接管，`src/App.tsx` 引导 effect）：
  1. 只处理带 `Files` 的拖放（内部内容拖拽不带 `Files`，互不干扰）；
  2. `dragover` 里 `preventDefault()`（否则浏览器不会派发 `drop`，还会自己接管）；
  3. `drop` 时过滤 `.xlsx` 并**走与"打开 xlsx"按钮完全相同的导入路径**（`fileOpenerRef` → `handleFile`），
     所以标签页、只读锁、特性应用、工作区等行为完全一致；
  4. **支持一次拖多个**：串行导入，各自开成标签页；
  5. 非 `.xlsx`（如 `.xls`/`.csv`）给明确提示「只支持 .xlsx 文件（.xls 请先另存为 .xlsx）：xxx」且不导入；
  6. 拖到窗口上方时显示「松开即打开这个表格」的覆盖提示（`.file-drop-overlay`，穿透、不吃事件），
     离开窗口即收起。
- 验证：`tests/e2e/file-drop.spec.ts` 4 例——①用**用户真实文件**拖入后确实导入并渲染出 >20 个非空格子
  ②拖入时出现提示、离开后收起 ③拖入 `.xls` 只提示不导入 ④一次拖两个文件各开一个标签页。

### 缺陷 K：多选后"剪切到工作区"剪的不是选中的那片（用户用真实课程表实测反馈）

- 现象：在 `（小波）课程表（修订版）0908.xlsx` 里选多格 → 右键「剪切到工作区」，
  剪切/清空的**不是**用户以为的那一片。
- 定位（用用户文件实测）：课程表里满是**合并单元格**。拖选 `B4:F8` 时，Univer 会把**模型选区自动扩成整块**
  ——实测得到 `B4:F11`（第 9~11 行是竖向合并），于是剪切自然剪走 8 行 × 5 列，
  而不是用户拖过的 5 行 × 3 列。
  另外发现**状态栏的"选区 …"一直是旧的**（拖选后仍显示"选区 A1"），
  用户因此看不到自己实际选了什么，更容易觉得"剪错了"。
- 修法（原则：**高亮看到的 = 实际被操作的**）：
  1. 新增 `src/interaction/range-merge.ts`（`expandRectToMerges` + `rectsIntersect`，纯函数，9 例单测）：
     右键时把范围**补齐到不与任何合并块相交**（阶梯状合并不停扩，直到收敛）。
  2. 若真的补齐过：记 `menu:range-expanded-for-merge` 日志、弹提示
     「选区包含合并单元格，已按整块 B4:F11 处理」，并**把选区同步成补齐后的范围**（高亮跟着变）。
  3. 状态栏"选区 …"改为**每 250ms 跟随真实选区**（只读 `getActiveRange()`；页面不可见时不跑），
     拖选后立刻能看到真实范围（实测：拖 B4→F8 后显示"选区 B4:F11"）。
  4. 菜单项本来就带范围（「剪切到工作区（B4:F11）」），现在与状态栏、高亮三者一致。
- 同类问题一起排查（都按"快照即所操作"处理）：复制/剪切到工作区、复制内容、清空内容、
  与工作区条目互换、粘贴，全部走右键那一刻的**快照范围**；工作区"+"导入对话框按用户填的范围，
  跳过合并块里的附属格（无值）✓。
- 验证：`tests/e2e/selection-merge.spec.ts` 3 例（用**用户真实文件**，不存在则跳过）：
  ①拖选被合并块撑大后，菜单范围 == 真实选区，且补齐时有日志/提示；
  ②「剪切到工作区」后**条目全部落在选区内、选区内被清空、选区之外一字未改**；
  ③对照（夹具无合并）拖选多少就是多少，不无谓扩大。
  取证截图 `evidence/p3-merge-range.png`。

### 缺陷 H：提示不统一、位置被遮挡、拖到工作区却说"不能放置"（用户实测反馈）

三件事一起改：
1. **拖到工作区提示错了**：命中测试只认表格画布，指针到侧边栏上时落点为 null → 提示走到 reject 分支。
   现在先按 **DOM 命中**判断（`elementFromPoint().closest('.workspace-host, .ws-panel, .body-splitter')`，
   能覆盖面板表头/表尾/滚动条与分隔条），命中就是"松开即暂存到工作区（可随时拖回表格）"，
   同时收掉单元格高亮（工作区上方没有网格落点）。
2. **不再用否定式措辞**：`此处不能放置` → `这里放不了：拖到单元格上互换，或拖到右侧工作区暂存`
   （用户明确要求"应该有其他对应提示"）。e2e 会**全程采样**提示文案，断言任何时刻都不出现"不能放置"。
3. **提示统一成一个层**：新增 `prompt-layer`（`data-testid="prompt-layer"`），
   拖动提示与轻提示**都**渲染在里面，位置统一（表格区域底部居中，`left:0 / right: var(--sidebar-w)`），
   样式统一（同一 `.prompt` 卡片：同 padding/圆角/字号/阴影，语义色只用左侧 3px 描边：
   互换=绿、写入=黄、落到工作区=主色、无效=红）。
4. **层级压过 Univer**：引入 `--z-prompt: 3000` / `--z-menu: 3200` / `--z-dialog: 3400`
   （上游弹层 z-index 到 1070）。提示层 3000、提醒框 3001、右键菜单 3200、导入对话框 3400、
   历史抽屉 2900；e2e 直接断言"提示层级 > Univer 容器内最高层级"。
5. 验证：`tests/e2e/prompts.spec.ts` 3 例（①拖到工作区提示"可放置"且松手真的进工作区、
   工作区上方无单元格高亮；①b 全程不出现否定式提示；②提示层 fixed / `pointer-events: none` /
   层级高于 Univer / 轻提示也在同一层里）。
   取证截图 `evidence/p3-prompt-layer.png`。

### 缺陷 I：互换后仍选中最开始的单元格 + 拖动时看不到落点（用户实测反馈）

- **互换后残留选中**：收尾顺序错了——`onPointerUp` 里先 `finish()`（互换，内部清了一次选区），
  紧接着 `restorePinnedSelection()` 又把**源单元格**钉回选中（`dragSourceA1Ref` 那时还没清），
  而 Univer 的选区写入是异步命令，偶尔还会落在我们清完之后。
  修复：先 `stopPinLoop()` + 清 `dragSourceA1Ref`，**再** `finish()`；删掉松手时的"再钉一次"；
  收尾改为"连清 3 帧 + 一次 160ms 兜底"，且**用户一按下指针就取消**（绝不跟用户抢选区）。
  兜底那一次还要**分清是谁的选区**：只收拾"这次互换涉及的那两格"，选区已经变成别处就不动——
  否则互换后 160ms 内用户（或程序）新选的区域会被我们抹掉（这是本轮自己踩到并修掉的回归：
  工作区用例里程序化选区被这一下清掉，条目只剩 A1）。
  验证：`tests/e2e/modes.spec.ts` 的拖拽用例在 50/300/600ms 三个时间点采样断言选区为空。
- **拖动时高亮即将被交换的单元格**（用户要求）：拖动中把落点那一格用**蓝色虚线框 + 淡蓝填充**标出来，
  落点无效（重叠/越界）时变红色；松手/取消/切模式即收掉。
  实现复用互换提醒框那套定位与穿透浮层（`createDragTargetHighlight`），位置每帧跟随滚动/缩放；
  取证截图 `evidence/p3-drag-target-highlight.png`。
- 同一套浮层机制现在有两个用途：**拖动中常亮**的落点高亮（蓝）与**互换后渐隐**的提醒框（黄）。

### 缺陷 L：工作区的操作撤不掉 / 点了工作区之后快捷键失效（用户实测反馈，两条一起报的）

用户原话：①"无法撤回对工作区的操作（撤回时，工作区未进行改变，或是工作区的操作未被记录）"；
②"鼠标点击工作区后，撤回重做这类快捷键失效"。这两条其实是**四层**问题叠在一起，逐层查出来的：

1. **工作区的改动根本没进撤销体系**：条目增删只改我们自己的 React state，Univer 的撤销栈完全不知情，
   Ctrl+Z 只会去撤表格内容。
   修法：`commitWorkspace(next, label, kind)` 收口成**唯一**的工作区改动入口
   （同步 `itemsStateRef` / `snapshotStoreRef` / `setItems`），每条历史附带 `{before, after}` 快照；
   撤销/重做按账本顺序在"表格动作"与"工作区动作"之间切换（`stepBack`/`stepForward`）。
2. **账本曾用 React state，撤销紧接着就来时会读到旧值**（实测：加入工作区后立刻 Ctrl+Z 毫无反应）。
   修法：账本改成**同步 ref**（`entriesRef` + `historyIndexRef`），`historyEntries` state 只负责渲染。
3. **Ctrl+Z 的第一顺位被 Univer 占着**（这条最隐蔽，也是"点了工作区之后失效"的真身）：
   Univer 的 `ShortcutService` 在引导时用 `window.addEventListener('keydown', …, { capture: true })`
   注册了 `UndoShortcutItem`/`RedoShortcutItem`；我们的监听是 React effect 里注册的，**必然排在它后面**。
   同一个 target + 同一个阶段按**注册顺序**执行，所以 `stopPropagation()` 同节点无效、
   `preventDefault()` 也拦不住它。实测到的现象就是"按一次 Ctrl+Z，画布变了、账本没动"：
   Univer 先撤掉表格，等我们执行 `api.undo()` 时栈已经空了、命令返回 `false`，账本索引原地不动——
   于是账本与撤销栈**差了一步**，接着再按就"什么都没发生"。
   修法：新增 `src/shell/undo-shortcut.ts`，在 `main.tsx` 里**引导之前**注册第一顺位的转发器，
   处理时用 `stopImmediatePropagation()` 把这次按键彻底截下来；只有焦点真在单元格编辑器/公式栏里
   （`EDITOR_ACTIVATED` / `FOCUSING_FX_BAR_EDITOR`）才让路给 Univer，输入框里让给浏览器原生撤销。
   顺带修掉一个旧坑：`<input type="file">`、勾选框这类"不是文本框的 input"不再被当成文本编辑
   （否则选完文件后 Ctrl+Z 也失效）。
4. **一次表格动作被记了两条历史**（幽灵步骤）：我们自己的互换/写回/清空既被 Univer 的 `undos` 计数
   抓到（订阅自动补一条"编辑内容"），又被 `pushHistory` 记了一条，撤销时白白多走一步。
   修法：动作开始打标（`beginSheetAction`），结束时按 **id 精确认领**这段自动条目并合并成一条、
   用我们自己的措辞命名（`history:push-merge` 日志）；我们自己的 `undo/redo` 引起的计数变化
   （重做就是 `undos +1`）也不重复入账，否则刚重做出来的那一步会被"编辑内容"顶掉标签。

- 验证：`tests/e2e/workspace-undo.spec.ts` **6 例**——①加入工作区后 Ctrl+Z/Ctrl+Y（含连续撤/重做不乱）
  ②删除与清空（含二次确认）都能撤销回来 ③点过工作区卡片与面板空白后快捷键仍有效
  ④表格动作与工作区动作**按时间顺序交替**撤销、并两次重做按原顺序回来
  ⑤工具栏按钮不再灰（可用态取自账本）⑥一次表格动作只记一条历史（面板里恰好一行、标签是"互换 …"）。
  另加 `tests/unit/undo-shortcut.test.ts` 6 例（哪些按键组合归我们管、挂载/卸载状态）。
  **口径变化（诚实说明）**：`tests/e2e/history.spec.ts` ② 原来点面板第 1 项就断言"回到最初内容"，
  那其实是**靠幽灵步骤凑出来的**（多出来的那条让"第 1 项"落在了最初状态）。现在账本准确了，
  面板第 `index` 项 = 步下标 `index + 1`（0 = 最初状态，只有撤销能走到），用例改为：
  点第 1 项 → 第 1 步的内容，再按一次 Ctrl+Z → 最初内容，然后从最初一口气重做到最新。

### 缺陷 M：多选复制/剪切到工作区时，空格子又混进去了（用户实测反馈）

- 现象：用户原话——"现在多选后复制/剪切到工作区以后，不会默认跳过空单元格了，请默认跳过空单元格，
  并检查所有的转移单元格到工作区的渠道，确保**绝对**跳过空内容的单元格"。
- 定位（在浏览器里造出四种"看着是空的"格子后逐一探针）：
  旧口径只判 `rawValue === null && !formula`，而 Univer 里"空"有好几种形态：

  | 现场 | 真实值 | 旧口径 | 现在 |
  | --- | --- | --- | --- |
  | 用户在编辑器里把内容删光 | **空字符串 `''`**（不是 null！） | ✗ 漏掉 → 白卡片 | ✓ 跳过 |
  | 只打了空格 | `' '` | ✗ 漏掉 | ✓ 跳过 |
  | 只有全角空格（中文表格常见） | `'　'`（U+3000） | ✗ 漏掉 | ✓ 跳过 |
  | 只有换行 | `'\n'` | ✗ 漏掉 | ✓ 跳过 |
  | 从没写过 | `null` | ✓ 跳过 | ✓ 跳过 |
  | 数字 0 / 布尔 false / 公式（哪怕算出来是空串） | `0` / `false` / `=<f>` | ✓ 保留 | ✓ 保留 |

  这就是"以前偶尔看着是会跳过的、现在不跳了"的来源：删过内容的格子从 `null` 变成了 `''`。
- 修法：
  1. 新增**唯一判定** `isEmptyContent(value, formula)`（`src/workspace/snapshot.ts`）：
     有公式 → 有内容；`null`/`undefined` → 空；数字/布尔 → 有内容（`0`、`false` 都是答案）；
     其余 `String(value).trim() === ''` 即为空。
  2. `extractCellItems` 用它做跳过判定（这是**所有**"搬进工作区"渠道的唯一底层入口）。
  3. 逐条查渠道，把两处不一致补上：
     | 渠道 | 处理 |
     | --- | --- |
     | 拖区域到侧边栏 | `addWorkspaceItem` → `extractCellItems` ✓（本就统一） |
     | 右键「复制/剪切到工作区」 | 同上 ✓ |
     | 「+」导入对话框（行/列/区域/整个已用区域） | 同上 ✓；顺手把那个**点了没作用的**"跳过空单元格"勾选框改成只读说明（它以前是死控件：`skipEmpty` 根本没被使用） |
     | **单元格 ↔ 工作区条目互换** | 以前无条件把换出来的内容写回条目 → 空格子会变成**白卡片**。现在 `absorbSwappedCell`：换出来是空的就**移除条目**（内容没丢，已在单元格里；一次 Ctrl+Z 可还回来），并记 `workspace:remove-empty-after-swap` |
     | **恢复上次会话** | 旧版本可能把空条目存进了会话 → 恢复时用 `isSnapshotEmpty` 过滤掉（记 `workspace:skip-empty-on-restore`），规则跨会话也成立 |
- 验证：`tests/e2e/workspace.spec.ts` ⑬（真实右键「复制到工作区」+ 底层入口读数 + 面板卡片数 + 空格子互换移除条目）
  与 `tests/unit/snapshot-date.test.ts` 的 4 例（三种空形态 / `0` 与 `false` 算内容 / 公式算内容 / `isSnapshotEmpty`）。
  复测用户座位表：`C1:N22` → 51 个条目、跳过 213 个空格子，面板里零白卡片。

### 缺陷 N：导入座位表时状态栏显示「未支持 1 项　降级 1 项」（用户实测反馈）

- 用户原话："导入 2502班座位表 单座 2026.9.7.xlsx 时下方状态栏提示：未支持 1 项降级 1 项，请问是什么原因，能修复吗？"
- 查因（直接读那份文件的 XML 真值，不猜）：

  | 提示 | 文件里的真身 | 是不是缺陷 |
  | --- | --- | --- |
  | 未支持 1 项 = `[Sheet1] 打印设置(pageSetup/pageMargins/headerFooter) · 2 处（未解析）` | 只有一个 `<pageMargins left="0.75" right="0.75" top="1" bottom="1" header="0.5" footer="0.5"/>` 和一个**空的** `<headerFooter/>`；**没有** `pageSetup` | **不是**：只影响打印，屏幕预览与内容编辑完全不受影响，导出走字节级保真本来就原样保留 |
  | 降级 1 项 = `patternFill patternType="gray125" 暂不支持` | `fills[1]` 是规范占位的 gray125，而**全表 25 个 cellXfs 没有一个引用它**（实测 fillId 全是 0） | **不是**：这格填充我们根本不会去画 |

- 修法（两条都改成"按事实说话"）：
  1. **填充降级按引用关系上报**（`src/parser/styles.ts`）：`parseFill` 只**记录**画不出来的原因，
     等 `cellXfs` 解析完，只对**真被某个单元格样式引用**的填充记 warning（并写明"该格按无填充显示"）。
     Excel/WPS 几乎每个文件都会写的 `fills[1] = gray125` 从此不再是无脑噪音；真用了非纯色填充的文件仍会如实报警。
  2. **打印设置改为"已解析 + 已原样保留"**（`src/parser/worksheet.ts` + `types.ts`）：
     解析 `pageSetup`（纸型/方向/缩放/适应页数）、`pageMargins`、`headerFooter`（页眉页脚文字）、
     `printOptions`、`rowBreaks/colBreaks` → `ParsedSheet.print`；同时移出 `unsupported`，
     改写进新增的 **`report.preserved`**（"已原样保留，不影响预览"）。
     状态栏只为真正的缺陷报警；日志抽屉里新增「已原样保留（不影响预览）」一节，写清解析到的真值。
- 验证：`tests/e2e/import.spec.ts` ⑦（用**用户真实文件**：状态栏不再出现"未支持/降级"胶囊、
  `preserved` 里有打印设置与页边距真值、抽屉里能看到该节、内容照常渲染 >20 个非空格子）；
  `tests/unit/parser-p1.test.ts` 新增 5 例（打印设置解析真值/空占位/无打印设置、
  未被引用的 gray125 不报警、被引用的非纯色填充才报警）。

### 缺陷 O：改一次代码，标签栏就多一个同名"灵魂标签"（用户实测反馈）

- 用户原话："每次你更新代码后，标签页都会出现一个名字相同的'灵魂'标签页，这可能会影响到正常使用吗，有修复方案吗？"
- 复现（**在真实 HMR 下复现出来，不靠猜**）：页面里导入一个 xlsx → 用 Node 往 `src/App.tsx`
  末尾追加一行注释（触发 Vite HMR / React Fast Refresh）→ 读标签列表，得到 **两个同 id 同名的标签**，
  控制台同时刷出十几条 React `Encountered two children with the same key`；日志给出完整链路：

  ```
  app:read-only-guard-installed      ← 引导 effect 又完整跑了一遍
  session:restore-start {tabs:1}
  session:tabs-registered {tabs:2}   ← 会话里的 1 个标签被**追加**到已有的 1 个上
  tab:switch-error {message: "no document with unitId imported-…"}   ← 新实例里没有这个 unit
  session:restore-done {tabs:2}
  ```
- 根因：Fast Refresh 会把 App 的引导 effect **重跑一遍**，而 `refs`/`state` 是**保留**的；
  旧 Univer 实例已经在 cleanup 里 `dispose()` 了。于是
  ① 内存里的标签在新实例中**没有对应 unit**（点它报 `no document with unitId …`，是个点不动的死标签）；
  ② 启动恢复把会话里的标签**再追加一遍**（同 id、同名）→ 标签栏多出"灵魂标签"；
  ③ 用户若继续操作，会话会把重复条目一并存下来，重开页面也带着它。
- 修法（三处，缺一不可）：
  1. **识别热更新重启**（`app:hot-reboot`）：effect 重跑时把所有标签标成**未实体化**
     （切回时按字节重建，与冷标签同一条路径）——死标签变成能用的标签；
  2. **恢复按 id 去重**（`session:restore-skip-registered`）：只补会话里缺的标签 —— 从根上不可能出现同名重复，
     并且**顺带自我修复**：旧版本已经写进会话的重复条目，下次读取时就被丢掉；
  3. cleanup 里**先把现场同步写库**（`collectSessionState()` → `sessionStore.save()`）再拆监听/定时器：
     热更新时页面并不卸载、没有 `pagehide`，原来"最近 1~2 秒的编辑"会被热更新吞掉（顺带修掉的数据丢失）。
- 对正常使用的影响（回答用户的疑问）：**不会损坏数据**（两条记录同 id、同字节，关掉重复那个即可继续用），
  但确实是缺陷 —— 死标签点不动、标签栏混淆、重复 key 警告，且重复条目可能被持久化。
  另外它只在"改代码触发 Fast Refresh"时出现（生产构建的整页刷新路径本来就没问题），
  而本工具日常就是跑在 `run.cmd` 起的 vite dev 上，所以必须修。
- 验证：新增 `tests/e2e/hot-reload.spec.ts` **2 例**——
  ①真实触发两次 Fast Refresh：标签数恒为 1、id 不重复、该标签已实体化、**热更新前刚敲进去的内容还在**、
  没有"重复 key"报错、日志里有 `app:hot-reboot` 与 `session:restore-skip-registered`；
  ②把会话写成"同一标签两遍"再刷新：恢复后仍只剩一个（自我修复）。
  （用例会往 `App.tsx` 末尾追加一行注释来触发 HMR，`finally` 里必然还原；环境不支持 HMR 时自动跳过。）

### 缺陷 P：选区右下角的"填充柄"小方块没用（用户实测反馈）

- 用户原话："选中单元格的时候，选中的单元格右下角会有个角标，但是这个角标毫无用处，
  能否利用起来或者直接不要显示这个角标？"
- 查因（先验行为，再决定"利用"还是"去掉"）：那个小方块是 **Univer 的填充柄**（auto-fill handle），
  服务于"拖拽自动填充序列/批量复制"。
  1. 本产品**只允许逐格改内容**，自动填充会一次改写一片单元格（还可能把格式一起铺开），
     命令闸门里 `/auto-fill|refill/i` 是**永久拒绝**的 —— 所以它永远不会成功，是纯粹的假入口；
  2. 更糟的是实测发现：默认的**拖拽模式**下捏着它一拖，会被我们自己的内容搬运接管，
     日志是 `app:swap`「已互换 A12 ⇄ A15」—— 用户以为在"填充"，实际**把两格内容换了**。
- 结论与修法：**不利用，直接不画**（若改成"拖它 = 搬内容"，只会和拖拽模式里"按住格子就能拖"重复且更难抓）。
  新增 `src/univer/fill-handle.ts`：
  ① 把选区控件工厂 `newSelectionControl` 包一层（幂等），保证**以后新建的**控件都不画填充柄；
  ② 顺手把**已存在**的控件关掉（切换工作表、多选都会新建控件，所以每次 `attachSheetDeps()` 都扣一次闸）；
  ③ 用的是 Univer 自己的开关 `SelectionControl._enableAutoFill`（它就是 `_updateControl()` 里
     `_autoFillControl.hide()` 的判据），所以不是"用浮层盖住"，而是真的不画。
  闸门（`read-only-guard`）继续拦命令：**就算有人绕过 UI 也改不动**，这里只负责不给假入口。
- 实测效果：`getFillHandleState()` = `{ available: true, controls: 1, enabled: [false], visible: [false] }`；
  角上起拖在拖拽模式 = 正常互换、在选择模式 = 正常框选（死区从"可见的 8px"缩到"右边界 1px"，
  那 1px 是 Univer 自己的"拖选区边框移动区域"热区，本来就被我们的闸门拒绝，属于既有行为）。
- 验证：`tests/e2e/modes.spec.ts` 新增 1 例（选中/多选/切工作表三种情形下填充柄都不启用不可见；
  角上起拖走的是我们自己的互换；选择模式角上起拖正常框选到 `A12:A14`）。

### 缺陷 P2：那个小方块**依旧**在画（用户第二次反馈，第一版修法只覆盖了一半）

- 用户原话："选区右下角那个小方块目前依旧绘制，请确保不再绘制。"
- 复现（**像素级**，不靠肉眼猜）：选中示例工作簿的 A3 后截图选区右下角 40×40 区域——
  蓝色小方块清晰可见；同时读 `getFillHandleState()` 得到 `{ enabled: [true], visible: [true] }`。
  而**导入任何文件之后**同一读数才是 `[false]/[false]`。也就是说：漏的正是"启动后第一眼看到的示例表"。
- 根因：第一版的关法是"**按渲染单元**处理"（`renderManager.getRenderById(unitId).with(ISheetSelectionRenderService)`），
  前提是该单元的渲染服务已经建出来。而启动流程是
  `loadWorkbook(示例) → focusSheetUnit() → attachSheetDeps()` 连着同步跑完的 —— 那一刻渲染单元还没创建，
  关闸直接落空（`{ ok: false }`），示例表的选区控件是用**原版工厂**造出来的。
  导入路径之所以没问题，是因为 `handleFile` 里有 `await parseXlsx(...)`、`await applyWorkbookFeatures(...)`，
  等它调 `attachSheetDeps()` 时渲染单元早就绪了。
- 修法：补一层**类级**关闸 `installFillHandleOff()`（`src/univer/fill-handle.ts`）：
  直接包 `SelectionControl.prototype.updateRangeBySelectionWithCoord` —— 选区控件**每次更新选区都会走它**
  （构造时也会走一次），在委托之前把 `_enableAutoFill` / `_showAutoFill` 按死。
  它在 `bootUniver()` **之前**安装（不依赖任何单元、任何注入器），因此覆盖"现在与将来、所有单元、所有选区"。
  按单元的那一层保留：它负责把**已经画出来**的那一帧立刻 `hide()` 掉，两层互补。
- 验证（两条独立证据）：
  ① 同一段探针再跑一遍：示例表也变成 `{ enabled: [false], visible: [false] }`，截图里小方块消失
     （前后对比图存 `evidence/p3-fill-handle-before.png` / `evidence/p3-fill-handle-after.png`）；
  ② `tests/e2e/modes.spec.ts` 的那条用例（选中/多选/切工作表三种情形）继续通过。

### 缺陷 R2：多区域选区（Ctrl+点选）落地后的两条真实回归

多区域本身按缺陷 R 实现；随后跑全量回归时被自己写的用例抓住两处**因为"多块"而暴露的问题**，一并记录：

1. **工作区导入日志丢了**：为了让多块"一次提交"，右键菜单与「+」导入改成了直接调
   `extractCellItems` + `addWorkspaceItems`，结果**绕过了** `addWorkspaceItem` 里的
   `workspace:skip-empty` / `workspace:truncated` 日志（`workspace.spec.ts` ⑦ 与 ⑬ 当场变红）。
   修法：抽出**唯一实现** `addWorkspaceFromRanges(a1List, pointer?)`（多块 + 单块共用）：
   逐块拆格 → 汇总跳过/截断 → 一条历史 → 一条 toast → 同一套日志 → 飞入动画；
   `addWorkspaceItem` 退化成"单块转发"，所有入口（拖拽/右键/「+」/一键全搬/测试钩子）口径完全一致。
2. **点两次同一个条目不取消**：面板里"待互换"高亮是靠 `data-pending-swap` 标记的，
   而我最初的用例在两次互换之后才点它，**第一次点击是"选中"而不是"取消"** —— 用例逻辑写错了，
   改成"点一下应高亮、再点一下应取消"（与点格子两次的行为一致），产品代码没动。

### 缺陷 Q：按钮为什么写着"xlsx"，其它格式呢？（用户提问）

- 用户原话："为什么打开 xlsx 按钮名称为 xlsx，对其他格式的兼容性呢？"
- 查因（读代码得到的事实）：
  1. **解析器其实不看扩展名**：它打开的是 **OOXML 工作簿包**（OPC = zip + XML），
     工作簿部件是按 `_rels/.rels` 里的 `officeDocument` 关系找的（`findWorkbookPath`），
     与文件叫什么无关 —— 也就是说 `.xlsm`/`.xltx`/`.xltm` 本来就能读，只是**被入口的
     `accept=".xlsx"` 与拖放过滤 `/\.xlsx$/i` 挡在门外**了，属于白丢的兼容性；
  2. 名不副实：按钮/提示/导出名字全写死 `.xlsx`，用户自然以为"只能开这一种"。
- 修法：
  1. 新增 `src/importer/file-kinds.ts` 作为**唯一口径**：支持 `.xlsx` / `.xlsm` / `.xltx` / `.xltm`；
     工具栏 `accept`、拖放过滤、`handleFile` 校验、状态栏与拖放提示文案全部读它；
  2. **打开路径统一校验**：`handleFile` 开头就判格式（用户把文件选择框改成"所有文件"也拦得住），
     拖放与选择框共用同一句话；
  3. **打不开的格式按类给"为什么 + 怎么办"**（不再是"只支持 .xlsx"一句打发）：
     `.xls` → 说明它是 Excel 97–2003 的**二进制**格式（不是 zip/XML），请另存为 `.xlsx`；
     `.csv/.tsv/.txt` → 说明它**只有内容没有格式**，而本工具是"保格式预览与编辑"；
     `.ods/.numbers/.et` → 说明是别家格式（需要各自解析器）；其它 → 列清支持范围；
  4. **宏不再误报**：`.xlsm` 里的 `xl/vbaProject.bin` 从"未支持"改为
     `report.preserved`：「宏(VBA) · N 个部件已原样保留（导出逐字节不变；不执行宏）」——
     导出是外科式修补，宏部件一个字节都不动，说"未支持"会让用户以为宏丢了；
  5. **导出保留原扩展名**：`座位表.xlsm` → `座位表-已编辑.xlsm`（导出的字节里仍是
     macroEnabled 内容类型，硬写成 `.xlsx` 会让 Excel 认为"格式与扩展名不符"）。
- 明确不做的（诚实边界）：`.xls`（BIFF8 二进制）要另写二进制解析器；`.csv` 没有格式信息，
  打开它等于放弃"保格式"这条产品前提；`.ods`/`.numbers` 是别家 schema。三者都**明确拒绝 + 指路**，
  不做"半支持"（读进来丢格式比读不了更糟）。
- 验证：`tests/unit/file-kinds.test.ts` 15 例（分类/提示文案/导出名/accept 常量）、
  `tests/unit/parser-p1.test.ts` 新增 2 例（`.xlsm` 走同一条解析路径、宏记进 preserved 而非 unsupported）、
  `tests/unit/export.test.ts` 新增 1 例（编辑后导出：`vbaProject.bin` 与 `[Content_Types].xml` 逐字节不变）、
  `tests/e2e/file-drop.spec.ts` ③ 改为逐类提示（`.xls` 说 97-2003、`.csv` 说没有格式，都不导入）、
  ④ 新增"打开 `.xlsm`：正常预览、宏在 preserved 里、accept 属性含 `.xlsm`"、
  `fixtures/fixture-macro.xlsm` 由 `npm run fixtures` 生成（改包：塞入占位 `vbaProject.bin` + 改成 macroEnabled）。

### 缺陷 R：不连续多区域（Ctrl+点选）—— 新能力

- 用户要求："请实现不连续多区域（Ctrl+点选）"。
- 先查事实（不猜）：Univer 的选区模型**本来就支持多块** —— `SheetsSelectionsService.getCurrentSelections()`
  返回数组；UI 层 `_checkClearPreviousControls(evt)` 在按住 Ctrl/Shift 时**不清掉**已有选区框，
  随后 `newSelectionControl(...)` 会**再建一个**选区控件；门面也提供
  `FWorksheet.getSelection().getActiveRangeList()` 读全部区域。
  也就是说"能不能选"不用造轮子，缺的是**我们自己的功能只读 `getActiveRange()` 这一块**。
- 修法：
  1. 新增 `src/interaction/selection-ranges.ts`（纯函数，22 例单测）：区域规范化（解析失败丢弃、
     **被包含的小块丢掉**、完全相同的去重、按行优先排序）、面积/包围盒、状态栏文案、
     多块输入的解析（`A1:B2 D4:E5`，分隔符支持空格/逗号/顿号/分号）；
  2. `readSelectionA1List()`：读**全部**选区并规范化（读不到退回单块），状态栏 250ms 轮询改写它 ——
     多块时显示 `B2 + D4（2 块 / 2 格）`，单块保持原来的 `B2:D5`；
  3. **右键菜单按全部块执行**：复制/剪切到工作区（一次提交、跳过空内容、来源只来自这些块）、
     复制内容（多块之间空一行分隔，与 Excel 习惯一致）、清空（逐块清空、每块一条历史，
     撤销就是逐块回来）；
  4. **天然只有一方的操作在多块下禁用并说明原因**（不是随便挑一块执行）：
     「与工作区互换」「粘贴」在多块时置灰，`title`/`hint` 写明
     「已选 N 块区域：… 一次只能处理一块，请先只选一块（Ctrl+点选可再去掉多选）」；
  5. 工作区「+」导入对话框的输入框**也认多块写法**（`A1:B2 D4:E5`），识别行显示
     「区域 A1:B2 + D4:E5（2 块 / 8 格）」，一次把几块内容搬进工作区；
     合并单元格补齐改成**逐块**处理（以前只补主区域）。
- 边界（诚实）：拖动搬运/点击互换仍以"指针下的那一格/主区域"为准（它们天然是单方动作）；
  冷启动时若选区被别的程序改回单块，状态栏会立刻如实回落。
- 验证：`tests/unit/selection-ranges.test.ts` 22 例（规范化/去重/包含/文案/多块解析）、
  `tests/e2e/selection-multi.spec.ts` 4 例（Ctrl+点选累积、Ctrl+框选追加、右键菜单按全部块执行且
  互换/粘贴禁用、导入对话框多块输入）——都以**真实 Ctrl+点击/拖选**驱动，并同时校验
  Univer 选区模型（`getActiveRangeList()`）与状态栏文案。

### 缺陷 S：点击互换支持"先点工作区、再点表格"—— 新能力

- 用户要求："点击互换模式请支持点击工作区选中后再点击表格进行互换（支持与工作区进行互换）"。
- 根因：工作区条目上只接了**拖拽**（按住拖动 = 拖回表格写回），单击既不高亮也不登记，
  于是"先点条目 → 再点格子"完全没反应（只有反过来才行）。
- 修法：
  1. 条目上"按下 → 没拖动就松手"识别为一次**点击**（`pendingWorkspaceClickRef`），
     在点击互换模式下把它登记为待互换的一方（日志 `swap:select-workspace-item`）；
  2. 面板把这一方**高亮**出来（`is-pending-swap` + 「待互换 · 再点表格里的一格」角标 + `data-pending-swap`），
     表格那边本来就有 Univer 的选区框做反馈；
  3. 再点同一个条目 → 状态机判为"同一方"→ 取消选中（与点格子两次的行为一致）；
  4. 两个顺序都走同一条互换路径（`swapHandlerRef`），所以"条目 ↔ 单元格"的行为完全一致，
     包括既有的两条规则：换出来的内容是空格子 → 条目被移除（工作区不留白卡片）、一次 Ctrl+Z 可还原。
- 验证：`tests/e2e/workspace.spec.ts` ⑭（两个顺序都互换成功、`data-pending-swap` 标记出现与消失、
  条目内容随互换更新、日志留痕）。

### 缺陷 Q2：把常见表格格式**真的**加上去（用户第二次要求）
- 用户原话："我希望能支持常见表格格式的兼容，我看过 Univer 不只支持 xlsx，请把其他表格格式都加上去，
  并确保按钮名称为打开表格，鼠标移上去的时候要变手形。"
- 事实校正（诚实说明）：**Univer 开源包里没有任何格式解析器** —— 它的 `.xlsx` 导入/导出属于
  **Univer Pro（付费 + 需要服务端）**，我们这套工具从一开始就是自研 OOXML 解析 + 外科式回写。
  所以"支持更多格式"= 自己写解析器，而不是打开某个开关。这一轮把三件事做了：
- **① 分隔文本（`.csv` / `.tsv` / `.txt`）** —— `src/parser/csv.ts`：
  - 编码：BOM（UTF-8 / UTF-16LE/BE）→ 严格 UTF-8 试解 → **退回 GBK**（中文 Windows 的 Excel「另存为 CSV」
    写的就是 ANSI/GBK，直接按 UTF-8 解会满屏乱码；实测夹具 `fixture-styles-gbk.csv` 正是这种）；
  - 分隔符：`.tsv` 直接制表符；否则在前 20 个非空行里挑"每行出现次数一致且最多"的候选
    （逗号/分号/制表符/竖线），**引号内的分隔符不算数**（否则含逗号的备注列会把制表符表格带偏）；
  - 引号：完整 RFC 4180 状态机（`""` 转义、字段内换行、字段首字符才是包裹引号）；
  - 类型：纯数字转数值，**带前导零的留作文本**（`007`/`0912` 这类学号、电话的零不能被吃掉），
    日期/百分比/布尔一律按文本（CSV 本来没有类型，不替用户猜）。
- **② `.ods`（OpenDocument 表格）** —— `src/parser/ods.ts`：zip + XML 自研解析：
  重复行列展开（Excel 导出 ODS 会写 `number-columns-repeated="16383"` 把表补满，必须**截断而不是跳过**，
  否则后面的行坐标会整体前移）、合并区（只认"声明者"，不反推 `covered-table-cell`）、
  `text:p`/`text:s`/`text:line-break` 文本还原、OpenFormula → Excel 公式（看不懂的丢掉公式保留值）、
  ISO 日期/时长 → Excel 序列号（`PT36H` 是 1.5 天，不取模）、自动样式与命名样式、列宽/行高/冻结/隐藏表。
- **③ `.xls`（Excel 97–2003 / BIFF8 二进制）** —— `src/parser/xls.ts`：
  手写 **CFB（OLE2 复合文档）**（512 字节头 → FAT/DIFAT → 目录项 → `Workbook` 流，含迷你流分支）
  + **BIFF8 记录流**（`BOF`/`BOUNDSHEET`/`SST`+`CONTINUE`/`LABELSST`/`RK`/`MULRK`/`NUMBER`/`BOOLERR`/
  `FORMULA`+`STRING`/`MERGEDCELLS`/`ROW`/`COLINFO`/`XF`/`FONT`/`FORMAT`/`PALETTE`/`PANE`/`WINDOW2`）。
  实测踩到的坑都写进了注释与用例：CFB 头里首个目录扇区在偏移 **48**（44 是恒为 0 的 `_csectDir`）、
  FAT 链上的 `FATSECT/DIFSECT` **不是**链结束标志、SST 跨 `CONTINUE` 时新段第一个字节是"压缩标志位"而非字符。
- **统一做法（关键设计）**：这三种格式都先被翻译成中性的 `WorkbookInput`
  （`src/importer/synth-xlsx.ts` 的 `writeWorkbookPackage` 打出一份**规范 xlsx**），再走既有全链路 ——
  所以预览、编辑、工作区、撤销、多标签、会话恢复**一个能力都不缺**，也没有第二套导入管线要维护。
  代价是导入时多一次"写包 + 解析"（毫秒级），换来的是"新格式自动拥有全部功能"。
- **导入摘要说清楚**：转换来的文件在"说明（不影响预览）"里写明
  「由 CSV/ODS/XLS 转换导入：编码 GBK、分隔符逗号、共 12 行 × 8 列」这类事实，
  以及「导出会另存为 .xlsx（原文件一个字节都不动）」—— 不藏着。
- **按钮与指针**：按钮文案改成「**打开表格**」（tooltip 列出支持的扩展名），
  `accept` 由 `SUPPORTED_EXTENSIONS` 生成（`.xlsx/.xlsm/.xltx/.xltm/.csv/.tsv/.txt/.ods/.xls`）；
  悬停手形：以前"忙时"给按钮加了 `pointer-events: none`，鼠标会**穿过**按钮落到工具栏背景上、
  指针退回箭头（用户看到的就是"移上去没有手形"），现在改成 `cursor: not-allowed` +
  内部 `<input>` 的 `disabled` 拦点击，并把按钮及其子节点的 `cursor` 一次性钉死。
- **导出与指纹**：转换源（csv/ods/xls）导出为 `原名-已编辑.xlsx`（标签页里存的就是我们合成的 xlsx）；
  OOXML 源继续保留原扩展名（`.xlsm` 改名会让 Excel 认为"格式与扩展名不符"）。
- **明确不做**：`.xlsb`（Excel 二进制 OOXML：zip 里是 binary parts）、`.numbers`/`.et`（iWork/WPS 私有）——
  都给出"为什么 + 请另存为 .xlsx"的提示，不做半支持。
- **真值夹具（关键证据来源）**：`tools/make-legacy-fixtures.ps1`（`npm run fixtures:legacy`）用
  **本机 Excel COM** 把 `fixtures/*.xlsx` 另存为真实的 `.xls` / `.ods` / `.csv`（ANSI=GBK 与 UTF-8 各一份），
  于是"同内容的 .xlsx"就是**真值**：ODS/XLS 的单元与端到端用例都是"与同一份 xlsx 逐格对拍"，
  而不是手写死数字。没有装 Excel 的机器上脚本会跳过，相关用例自动 skip。
- 验证：`tests/unit/synth-xlsx.test.ts` 9 例（写入器闭环：值/样式/合并/尺寸/冻结/隐藏表/表名清洗）、
  `tests/unit/csv.test.ts` 23 例（编码/分隔符/RFC4180/类型 + 真夹具对拍）、
  `tests/unit/ods.test.ts` 与 `tests/unit/xls.test.ts`（真夹具对拍 + 合成边界 + 安全阀 + 端到端）、
  `tests/e2e/formats.spec.ts` 7 例（按钮名与手形、GBK/UTF-8 CSV、ODS/XLS 与 xlsx 逐格一致、
  转换源导出名、打不开的格式给提示）。

### 缺陷 T：撤销通道的**全面收口**（用户："确保把所有对工作区、表格的操作都统一走可撤销的通道"）

用户要求重构「+」面板时顺带点名："该面板添加的数据不会被历史记录所记录而导致无法撤销，请务必注意"。
把面板本身复测了一遍：**加入 → Ctrl+Z → 0 → Ctrl+Y → 3** 是通的（日志 `history:push` /
`workspace:commit` / `history:undo-workspace` 齐全）。于是把**所有**会改表格或工作区的代码路径拉出来审，
查出两处真会让撤销**永久失灵**的作用域错误，以及一处幽灵条目：

| 路径 | 症状（根因） | 修法 |
| --- | --- | --- |
| 工作区条目**拖回表格**（`dropHandler` 的 `workspace-item` 分支） | 记的是 `pushHistory(label, 'workspace')`：账本里是"工作区条目"，但工作区快照只有 `commitWorkspace` 会建 → `stepBack` 找不到快照直接 `return false`，**账本索引卡死，此后 Ctrl+Z 全都失灵** | 写进表格就是**表格侧**的一步：`beginSheetAction` + `applySnapshot` + `pushHistory(..., 'sheet')` |
| 条目剪贴板的**粘贴到表格**（`pasteClipboardAt`） | 同上（同一个写法） | 同上 |
| 打开文件时**应用特性**（条件格式/数据验证/超链接/批注/图片） | 一串 Univer 命令让 `undos` 分多次增长，订阅把它们当成"用户编辑"记进账本 → 刚打开文件就躺着几条"编辑内容"幽灵条目，Ctrl+Z 撤不动任何看得见的东西（实测 `fixture-extras.xlsx` 多 1 条） | 新增**装载窗口** `pauseAutoEntriesRef`：应用特性期间整个窗口不记账（只更新计数）；窗口结束后顺手清掉新单元的撤销栈，让"账本 0 条 / 栈 0 步"一致 |

审计口径（列在这里方便下次照做）：**所有** `.setValue/.setFormula/clearRange/applySnapshot/moveRange/swapRanges`
调用点逐个过一遍，确认三件事 —— ① 表格改动前有 `beginSheetAction(措辞)`；② 改动后有 `pushHistory(措辞, 'sheet')`；
③ 工作区改动只经过 `commitWorkspace`（它自己带快照 + `pushHistory(...,'workspace', entryId)`）。
唯一允许"不走账本"的两处是**装载类**操作：`replayEditsFor`（冷标签重建回放，紧接着 `clearUndoRedoFor` 清栈）
与 `setValues/setValue` 测试钩子（模拟"用户打字"，靠 `undos` 订阅自动入账）。
两个测试钩子 `pasteWorkspaceItem` / `move` 以前也是"悄悄改内容不记账"，现已改成走**与真实交互完全相同**的通道，
免得测试测出来的是"钩子行为"而不是用户看到的行为。

守卫用例（`tests/e2e/workspace-undo.spec.ts`）：⑦ 真实鼠标把条目拖到 D22 → 写入 → Ctrl+Z 撤掉 → Ctrl+Y 重做；
⑧⑨ 面板的剪切/复制两条语义；⑩ 打开文件后历史面板为空、`import:undo-stack-cleared` 有日志、之后的编辑照常可撤。

### 缺陷 U：拖放的"误操作"两连（用户："工作区拖动会自我复制""点工作区空白会把单元格加进去"）

两条反馈是**同一处落点逻辑**的两个面，根因都是"按下的手抖被当成了拖动"：

| 现象 | 根因 | 修法 |
| --- | --- | --- |
| ① 在工作区里把条目拖到面板空白处 → **自我复制** | `dropHandlerRef` 的落点判断**不看拖拽来源**：`payload.kind === 'workspace-item'` 落到侧边栏时，被当成"从表格拖进来"，于是把同一个快照又收了一份（新 id、同来源 A1） | 落点在工作区内且来源就是工作区 → **什么都不做**（toast「这个单元格已经在工作区里了；拖到表格里才会写回去」+ 日志 `workspace:drop-self-ignored`） |
| ② "选中单元格后点工作区空白会把单元格加进去" | 当时**没有任何代码**在做这件事：真相是拖动阈值只有 **4px**，按下时手抖 5px 就算"拖动开始"，松手落在工作区上就执行了落点动作 | 阈值提到 **8px**（`MOVE_TOLERANCE_PX`）：点选/点条目仍然算点击，真正的拖动照样起得来 |

> **后续澄清（重要）**：② 那条反馈的真实意图是**"我希望有这个功能"**。所以在这一轮把它**正式实现**了
> （见下一节「功能 V」），而"手抖不算拖动"的 8px 阈值也保留 —— 两者并不冲突：
> 现在是**明确的点击**才触发快速加入，拖动仍然只做搬运。

顺手补的三道同类防线（用户要求"检查是否存在类似其他问题"）：

1. **入口级去重**（`addWorkspaceItems`）：同一个快照 id 只能在工作区里存在一份；从表格再拖一次同一格会生成**新 id**，
   所以正常重复不受影响。日志 `workspace:duplicate-ignored` / `workspace:skip-duplicate`。
2. **会话恢复也去重**（`restoreSession`）：万一会话里同 id 存了两遍（历史版本写坏过），恢复时按 id 去重 ——
   否则会出现两张同 id 卡片（React key 冲突、删一张另一张也没了），看起来同样是"自我复制"。
3. **"没放进去就不动源内容"**（剪切语义）：拖到工作区且开关是"不保留"时，只有**真的放进了条目**才清空源格；
   以前选中区域全空（底层整块跳过）也会照清，用户看到的是"东西没进来，格子却空了"。日志 `workspace:cut-skipped`。

守卫用例（`tests/e2e/workspace-dnd-guards.spec.ts`）：① 条目拖到面板空白 → 数量不变 + 有提示 + 无 `workspace:add`；
② 条目拖到另一个条目上 → 数量不变；③ 选区**过期**后点空白不动手、有 1 条时点空白也不会多出第二条；
④ 反向确认正常功能没被修坏：条目拖到表格 D22 仍然写回（`app:workspace-paste`）。

### 功能 V：**快速加入** —— 选择模式选中单元格后，点工作区空白就收进来

用户要求："选择模式下，选中单元格后在三秒内点击工作区空白区域时，会把单元格加到空白区域"。

实现（`src/App.tsx` 的 `handleWorkspaceBlankClick` + 面板根节点的 `onPointerDown/onClick`）：

| 规则 | 取值 | 为什么 |
| --- | --- | --- |
| 时间窗 | **3 秒**（`QUICK_ADD_WINDOW_MS`） | "刚选完"的直觉窗口；超时后点空白只是点空白 |
| 什么算"刚动手" | 表格里**没拖动的按下**（点选 / Ctrl+点选）或**焦点在表格时的按键** | 只认用户真实操作，不认程序化选区（否则撤销/互换改选区后也会被误触发） |
| 什么算"点击" | 面板内按下与松开位移 **< 8px** | 拖到面板里松手是"拖入工作区"，不该同时触发快速加入 |
| 什么算"空白" | 不在条目、按钮、输入框、下拉、标签、面板头/脚上 | 点搜索框、点「+」、点条目都保持原行为 |
| 哪个模式 | **选择模式 + 点击互换模式**（用户后续要求"点击交换模式也把这个功能加上去"）；**拖拽模式刻意不参与** | 拖拽模式的手势语言就是"按住拖"，在那里点空白容易被理解成"放下/取消"，少一个隐式动作更不容易误触（日志 `workspace:quick-add-skipped` reason=mode） |
| 重复点击 | **同一次选区只加一次**（3 秒内） | 连点两下不会收两遍，日志 `workspace:quick-add-duplicate` |
| 落地 | 仍走唯一入口 `addWorkspaceFromRanges` | 逐格拆分、跳过空内容、按 id 去重、**可撤销**（Ctrl+Z 一次撤掉） |
| 发现性 | 空态多一行小字"选择模式下：选中单元格后点这片空白也能加入" + 飞入动画 | 不靠猜 |

用例（`tests/e2e/workspace-quick-add.spec.ts`）：① 选中一格 → 点空白 → 加入且可撤销/重做；② Ctrl+点选多块 → 一次全进；
③ 选区含空格子 → 只收有内容的（不放空条目）；④ 连点两下只加一次；⑤ 点搜索框/条目/「+」不触发；
⑥ 拖到面板里松手走拖拽（不会被记成快速加入）；⑦ 拖拽模式下点空白不动手，**点击互换模式下点空白会加入**。

### 修复 W：导出面板只留能用的项 + 「打开表格」的光标修到底

用户三条反馈一起处理：

| 反馈 | 处理 |
| --- | --- |
| "导出按钮的面板进行优化，无法使用的功能直接隐藏" | 菜单改成**按可用性决定放不放**（不是置灰+说明）：`hasFile` 才有「导出 .xlsx（保真）」；有工作表就有「导出 .csv」；`经本机 Excel` 的 `.ods/.xls/.xlsb` 只在**桥可用**时出现。标签也改短（说明放右侧提示位）。菜单一项都没有时导出按钮自己置灰（不会点开一个空菜单） |
| "打开文件按钮鼠标移上去时不会变成手形" | 三层保险：① 样式表 `.tb-file-btn *`（原有）；② React **内联样式**（优先级最高，样式表顺序/浏览器缓存都盖不掉）；③ **补满圆角外的命中区** —— 按钮是 7px 圆角的 28×28 方块，四个角的像素不属于绘制区，`elementFromPoint` 会穿透到 `.tb-toolbar`（实测 `(+26,+2)`、`(+27,+27)` 命中工具栏、光标 `auto`），于是加了铺满整格的透明 `::after`，**并删掉 `.tb-file-btn` 的 `overflow: hidden`**（它会把伪元素也裁成圆角，命中区又漏回去）。用例把"按钮中心 + 四角 + 内联样式"全钉住了（`formats.spec.ts` ①） |
| （顺带）骨架屏的光标 | 加载阶段那排灰块看起来像按钮却点不动：骨架整体给 `cursor: progress`，表示"在忙"而不是"死的" |

### 修复 X：打开文件后白屏，刷新又好了（用户实测反馈）

用户原话："打开文件后，页面是空白的，但是网页刷新后又会出现"。

**第一次交付时的诚实说明**：这条一开始**没复现出来**（夹具/CSV/6MB/真实座位表/引导中导入/四标签轮切/
恢复中导入/竞态导入都试过，画布都正常）。所以第一版是把这一类现象的**三条可能成因**分别封死
（见下面 ①②③），并让故障不再表现为白屏。

**随后用户给出了确切步骤，一次就复现了**：

> 打开一个文件 → 等备份进行存储 → 过几秒后刷新页面 → **把那个还原的页面关掉** → 再打开文件 ⇒ 白屏

复现出来的机制（一条一条打日志量出来的，对应成因 ④）：

1. 关掉**最后一个**标签时，`disposeUnit` 释放的是 Univer 里**唯一**的活动单元；
2. 于是"当前单元"变成 `null`，容器里的 canvas 被一起摘掉 —— 实测 `canvasCount: 0`、`canvasWidth: 0`，
   舞台立刻全白；同时 Univer 的公式栏开始抛 `Cannot read properties of null (reading 'activeSheet$')`；
3. 此后再打开文件，**模型全对**（状态栏连"解析 3ms / 渲染 15ms"都打出来了，`unit === 活动标签`），
   但画布回不来：自愈先记 `render:rebind`，紧接着记 `render:rebind-failed`；
4. **关键**：这个状态下**新建工作簿也救不回来**（实测新建之后 canvas 仍然是 0）——
   渲染根一旦被回收，JS 这边只能靠重新引导，也就是刷新。这正是用户"刷新又好了"的原因；
5. 启动时之所以没这个问题：那时摆着"示例工作簿"（`p0-workbook`）这个**占位单元**。

**修法**：不让单元数走到 0 —— 关最后一个标签时**先摆占位单元、再释放**（顺序反了就是白屏）：

```ts
const isLastTab = tabsDataRef.current.length === 1;
if (isLastTab) restorePlaceholderRef.current('last-tab-closing'); // 先建占位并切成当前单元
apiRef.current?.disposeUnit(id);                                   // 再释放
```

摆上之后舞台回到"刚打开工具"的样子（示例表 + 「拖入表格文件…」），也就能继续开新文件；
状态栏同时清掉上一份文件的摘要。另外留了两个兜底：标签从"有"变"没有"之后再由 effect 确认一次
（只在**真的开过标签**之后才动手，避免启动/恢复期间多摆一份谁也管不着的工作簿）；
真到了"渲染根没了"那一步就如实提示「画面没能恢复，请按 F5 刷新（数据不会丢）」，而不是让人对着白舞台反复点。

**下面 ①②③ 是第一版按"可能性"封死的三条成因**，仍然保留（它们各自都有独立价值）：

| 成因 | 为什么会出现 | 处理 |
| --- | --- | --- |
| ① **活动单元与画布绑定错位** | 三条路径都会"建簿 + 重挂依赖"：打开文件、冷标签重建（大文件要几秒）、会话恢复。以前它们能**交错**执行 → 两次 `createWorkbook` 互相穿插，应用以为在看 A、画布画的是 B（或什么都没画）→ 舞台空白；刷新只走恢复这一条干净路径，于是"自己好了" | 三条路径排进**同一条 Promise 串行闸**（`unitOpsRef` / `runUnitOp`，日志 `unit-op:done` / `unit-op:error`），一次只跑一个，后到的排队而不是插队 |
| ①的兜底 | 触发条件未必只有交错这一种（浏览器冻结/丢弃标签后再激活、渲染单元被提前释放…） | 每次"会动工作簿"的操作结束后**自检并自愈**（`ensureActiveUnitRendered`）：先**等首帧**（最多 600ms 逐帧轮询，绘制是异步的，立刻判会误报）→ 真不对就重绑（`setCurrentUnitForType` + 聚焦 + 重挂依赖）并 `dispatchEvent(new Event('resize'))` **催一次布局** → 再复验 1200ms，仍不对记 `render:rebind-failed`。**健康路径零开销**：第一次测量就通过，不 await、不打日志 |
| ② **渲染期抛异常** | React 18 没有错误边界时，渲染期异常会**卸载整棵树** → `#root` 变空 → 整页白；刷新走恢复路径又正常，看起来像随机故障 | `AppErrorBoundary` + `ErrorFallback`（`src/shell/ErrorBoundary.tsx`）：兜成一张可读卡片（错误摘要 + 「重新加载」），并记 `app:render-error`。数据在 IndexedDB 里，刷新不丢 |
| ③ **分块/入口脚本根本没下下来**（静态托管上"刷新就好"的典型） | 每次发布都换分块 hash，浏览器里可能还留着**旧的 index.html**（弱网/代理截断也一样）：旧 HTML 指向的 `App-<旧hash>.js` 已不存在 → 动态 import reject。以前**没有任何兜底**，页面永远停在骨架屏；按 F5 拿到新 HTML 就好了 | ① `main.tsx`：捕获动态 import 失败 → **自动重来一次**（标记写 `sessionStorage`、起来后清掉；存储不可用则**不**自动重试，绝不死循环）→ 还不行摆可读卡片（`boot-failed`）；② `index.html`：入口脚本自己就没跑起来时，**看门狗** 20 秒后摆出「重新加载」；③ 卡片样式**静态引入**（`ErrorBoundary` 模块可能正是没下来的那一块） |

**验证不是"只写了防御代码"** —— 四条成因各有对应用例，其中 ③④ 都能**稳定复现**（把请求打断 / 走用户给的确切步骤）：

- `tests/e2e/blank-page-guard.spec.ts`
  ① 冷标签重建进行中导入；② 会话恢复进行中导入；③ 正常导入不出现兜底卡；
  ④ **人工造出绑定错位**（`createHandsOnSheet()` → 实测 `unit=hands-on-…` / `tab=imported-…`）再调
  `healActiveUnit` 断言绑定真的接回来，并钉住健康状态**不许误报**；
  ⑤ **用户给的确切复现路径**（打开 → 等备份落盘 → 刷新 → 关掉还原的标签 → 再打开文件）：
  断言关掉后画布仍在（`tab:placeholder-created`）、再打开文件画布仍 > 200px、绑定一致、
  没有 `render:rebind-failed`、**没有页面级异常**（以前这里就是公式栏的 `activeSheet$` 空指针）。
- `tests/e2e/boot-failure.spec.ts`：① 打断分块一次 → **自动重来**后应用正常起来（实测导航 2 次、重试标记清空）；
  ② 一直打断 → 摆出失败卡（实测打断 2 次后出卡，不是无限重试）；③ 打断入口脚本（JS 完全没跑）→ 看门狗摆出「重新加载」；
  ④ 正常启动**不误报**（看门狗即使先响了，也会随应用挂载一起消失）。
- 兜底卡本身另有 4 条单测（`tests/unit/app-crash.test.ts`）。

顺带修掉一条**卡在边界上的测试**（排查过程中撞上的）：`hot-reload.spec.ts` ② 原来写死
`waitForTimeout(2600)` 等"自动保存落盘"，而自动保存是"每 2000ms 查一次指纹 + 变化了再去抖 800ms"，
最坏 2800ms 才进库 —— 相位偏 200ms 就会在"还没写"的时候去读，读到 `null` 直接假红（实测同一份代码连着红两次）。
现在改成**轮询"库里真的有这份会话了"**，末尾"等恢复跑完"也改成等 `session:restore-done` 日志：
断言其实**更严**（真的在验证自动保存会写库，而不是用 `flush` 把行为绕过去），而且更快（9.0s → 6.1s）。

**如果还是遇到**，现在不会白屏了，请把看到的那张卡片上的文字（或控制台里 `app:render-error` / `unit-op:error` /
`render:rebind-failed` / `boot-failed` 的内容）发我们 —— 有了它就能定位到具体那一行。

### 修复 Y：Ctrl+C 复制**完全没有反应**（用户提问"能不能复制到本地表格软件"时查出来的）

用户原话："目前是否能做到复制网页表格的内容后可以粘贴到本地的表格软件或者其他表格软件里？"

**查出来的结论**：右键菜单的「复制内容」一直是好的，但 **Ctrl+C 一直是不工作的** ——
按下去剪贴板纹丝不动，而且**没有任何提示**（所以既没人报障、也没人发现）。

**根因不在浏览器、也不在"没绑快捷键"，而在我们自己的只读闸门**：白名单里的命令 id 写错了。

| | |
| --- | --- |
| 真实 id | `univer.command.copy`（`@univerjs/ui` 的 `CopyCommandName`） |
| 白名单里写的 | `sheet.command.copy` —— 这是 `@univerjs/sheets-ui` 里 `SheetCopyCommand` 的 **`name`**，而命令注册表认的是 **`id`** |
| 后果 | 那条"放行"是**空放行**（该 id 在 0.25.1 里根本不存在，派发会报 `command is not registered`），而真命令落进"默认拒绝"被拦掉 |
| 为什么一直没露 | 右键菜单的复制是我们**自己实现**的（`navigator.clipboard.writeText`），走的不是 Univer 命令，所以看起来"复制是好的" |

定位手法（可复用）：给表格选区后按 Ctrl+C，读 `getBlockedCommands()` —— 里面 `univer.command.copy` 的计数 +1；
再用对照组排除假象（往 DOM 塞一个 textarea 全选后按 Ctrl+C，剪贴板拿到了文本 → 说明键盘链路是通的，
问题在我们的闸门而不是浏览器/CDP）。

**修复**：白名单补上真实 id `/^univer\.command\.copy$/`（保留 `sheet.command.copy` 只作将来改名的兼容别名）。
**`cut` 故意不放行**：`univer.command.cut` 只做标记，真正清空源发生在粘贴时，而那条路径可能落到被本闸门拦掉的
`move-range` / `reorder-range` 上 → 会变成"剪切看着成功、却粘不出来"的半坏状态；产品里的剪切由「剪切到工作区」承担。

**顺手把"带格式复制"做实**（用户接着问"对于格式能否处理"）：原生复制一次写**两种口味**——

- `text/plain`：TSV（Tab 分列 / 换行分行，Excel、WPS、腾讯文档、Google Sheets 都靠它分格），内容仍是**显示值**；
- `text/html`：`<table>` + 内联样式。实测带出**字体、字号、加粗、字色、底色、四边框、水平/垂直对齐、
  合并（rowspan/colspan）、列宽（`<colgroup>`）、行高**，外层还带 `<google-sheets-html-origin>` 包裹
  （Excel / 在线表格认这个标记）。所以粘到 Excel/WPS 是**带格式**的。

于是右键菜单的「复制内容」也一并收口：

| 选区 | 行为 | 菜单标签（如实写明） |
| --- | --- | --- |
| 单块 | 派发 `univer.command.copy`（与 Ctrl+C 同一条路，含格式）；命令失败时退回纯文本 | `复制内容（3×3，含格式）` + 提示 `Ctrl+C` |
| 多块 | **只能纯文本**：原生复制只取"最后一个选区"（源码 `getCurrentLastSelection()`），会把其它块丢掉，所以多块仍由我们自己拼 TSV、块间空一行 | `复制内容（2 块 / N 格，纯文本）` |

**同时修掉一条静默失败**：新增 `src/shell/clipboard-write.ts` —— `navigator.clipboard` 只在**安全上下文**
（HTTPS / localhost）存在，用 `http://局域网IP` 发给同事时它是 `undefined`，而原来的
`navigator.clipboard?.writeText(text).then(...)` 会**整条短路**：不写剪贴板、也不提示（用户看到"点了没反应"）。
现在退回 `textarea + document.execCommand('copy')`（Univer 自己也这么做），并把**真实结果**返回给调用方 ——
真失败就弹「复制失败：浏览器/系统拒绝了剪贴板写入（可用 HTTPS 或 localhost 打开后重试）」。

**粘贴方向（用户第二个问题："会不会覆盖格式？"）—— 实测不会，而且是双层保证**：

| 测试 | 结果 |
| --- | --- |
| 目标格 A2（蓝底 `#2563EB` + 白字 + 四边框），从外部粘一份 `<td style="background:#ff0000;font-weight:bold;text-decoration:underline;color:#0000ff">` 的 HTML | **值写进去了**（`外部粗体下划线`），`getCellStyleData()` 与粘贴前**逐字段全等**，`getValue(true)` 仍是纯文本（**没有**变成富文本），列宽/行高不变 |

原因是两层：命令层的只读闸门（默认拒绝）+ mutation 层把 `cellValue` 里的 `s`（样式）剥掉
（`src/univer/lock.ts` 的 `stripStyles`）—— 粘贴带进来的底色/粗体/边框到不了单元格。
（粘贴 HTML 时上游还会发一条 `sheet.mutation.set-worksheet-row-auto-height`，同样被闸门拒掉，所以行高也不会变。）

**回归用例**（`tests/e2e/clipboard.spec.ts`，5 例，归到 `sensitive`（串行）项目 —— 它读写的是**真实系统剪贴板**，
并发跑会被别人的复制盖掉哨兵值）：

1. Ctrl+C 写出的两种口味：纯文本是 TSV（3 列）且含表格真实内容；HTML 含 `font-family`/`font-weight`/`color:`/
   `background`/`border`/`text-align`/`colspan`/`<colgroup>`；**被拦清单里不得再出现 copy 命令**；
2. 右键单块复制同样带 HTML 口味，标签含"含格式"；
3. 多块复制只写 `text/plain`，块间空一行，标签含"纯文本"；
4. 遮掉 `navigator.clipboard` 模拟 http 局域网 → 降级路径仍返回成功，且**内容真的进了系统剪贴板**；
5. 外部带格式 HTML 粘进来：值写入 + 目标格式一格不变 + 不引入富文本。

单测侧：`tests/unit/read-only-guard.test.ts` 补 `univer.command.copy` 必须在白名单、`univer.command.cut` 必须被拒
（原来那几条断言用的正是**不存在**的 id，所以闸门错了它照样绿 —— 这也是这次能潜伏这么久的原因）。

### 修复 Z：内存泄漏专项审计（用户要求"检查代码中可能造成内存泄露的情况并修复"）

**审计怎么做的**：按目录分区**通读**（不是 grep 猜）——`App.tsx` 全部 5100+ 行、`interaction/`+`univer/` 18 个文件、
`workspace/`+`persistence/`+`parser/`+`exporter/`+`importer/` 35 个文件；每条结论都要求给出机制、代码证据、
增长频率与调用点，**我自己逐条复核后才动手**（子代理只做审计，不改文件）。

**结论先说**：这个代码库的"成对释放"纪律总体是好的 —— 14 对 window/document 监听两端引用与 capture 标志
全部对称、4 处 rAF 全部受控、3 个 interval 全部清理、Univer 订阅成对 dispose、
`p0log`/历史账本/工作区快照/`CLOSE_TAG_CACHE` 都有显式上限。**真正修掉的是下面 9 处**（1 高 5 中 3 低）。

| # | 问题 | 级别 | 机制 | 修法 |
| --- | --- | --- | --- | --- |
| 1 | **插图 blob url 页面生命周期内永不回收** | 高 | `apply-features.ts` 原来是模块级 `Set`，只登记不回收。每个 url 钉着一份图片字节；导入 N 份带图的表、标签来回切 M 次 → 累积 O(N+M)×图片数 份 Blob，**关标签、冷存都不还**，只有整页刷新才回收 | 按工作簿分桶 `objectUrlsByWorkbook`；关标签 / 冷存 / 整实例拆卸**三处**释放；单张插入失败只作废那一张（不误伤同簿其它图）。安全前提：冷存切回会重走 `applyWorkbookFeatures` 重新插图 —— 有回归用例盯着 |
| 2 | 冷存标签**不清 Univer 撤销栈** | 中 | 撤销栈不随 `disposeUnit` 清理（代码自己在 `clearUndoRedoFor` 注释里实测确认过）。冷存的标签在"切回重建"之前一直占着每次编辑的 mutation 快照 | 冷存时也清（`clearUndoRedo(id)`）。重建本来就会清，所以提前清**不损失任何可撤销范围**；注意不能用 `clearUndoRedoFor`（那会连全局账本一起重置） |
| 3 | 渲染单元缓存**按住已 dispose 的 scene/skeleton** | 中 | `renderCacheRef` 原来是引导 effect 里的局部对象，`coldStoreTab`/`closeTab` 够不到 → `disposeUnit` 后缓存仍指着已释放的渲染单元（大表这份对象不小），要等下一次命中测试才换掉 | 提升为组件级 `useRef`，在统一释放入口里清 |
| 4 | 工作区快照表**只增不减**（外加一个功能缺陷） | 中 | 只有 `commitWorkspace` 同步它；`stepBack`/`stepForward`/会话恢复三条路径直接改 items。于是"加入工作区 → Ctrl+Z"留下的快照**永远删不掉**（快照 id 是 `snap-${Date.now()}-${seq}`，永不复用）；反向"撤销移除条目"时 store 里又没有 → 拖拽/粘贴按 id 取到 `undefined`，表现成"工作区条目不存在" | 抽出 `syncSnapshotStore(next)`（多删少补），三条写 items 的路径共用 |
| 5 | **示例/占位簿的脏格账本跨实例存活** | 中 | `dirtyByWorkbook` 是模块级 Map，以前只有 `closeTab` 清；示例/占位簿用**固定 id** `p0-workbook`，释放后账本留着 → 同 id 重建的新实例**继承上一轮的脏格计数**（标签红点 + 外科式导出的回写范围都会错） | 把"释放一个单元的记账清理"收口成 `releaseUnitBookkeeping(id, {keepTabState})`：关标签 / 冷存 / 拆示例簿三条路径都走它。以后再加一项清理不会再漏路径 |
| 6 | `window.__p0` / `__app` 钩子**从不摘除** | 中 | 钩子闭包持有 `tabsDataRef`（**每个标签的整份原始字节**）与工作区快照表；挂在 window 上 → 真卸载（非 HMR）路径上 boot.dispose() 之后这些 MB 级对象仍可达 | 拆卸时 `delete window.__p0 / __app`（HMR 先删后装，e2e 不受影响） |
| 7 | 冷存标签的插图 url 与**模块级 config 引用** | 低 | `setup.ts` 的 `configServiceRef` 是模块级的，`dispose()` 不清 → 留着已 dispose 实例的 `IConfigService`（配置 Map + Subject 及订阅者闭包） | 按**身份比对**后置空（避免误清下一个实例刚写进去的引用） |
| 8 | 分隔条拖动的 window 监听可能漏摘 | 低 | 不在任何 effect 里：拖动进行中卸载（HMR）就会永久留下两个监听（闭包抓着旧 setState）；且只监听 `pointerup`，**手势被 `pointercancel` 取消**时监听会赖着不走 | 补 `pointercancel`；把"摘监听"存进 ref，卸载时兜底调用 |
| 9 | 滚动条"抓取高亮"节点卸载时留在 body | 低 | 节点直接 append 到 `document.body`；卸载瞬间若指针正悬停在条带上（hover 态不会自己消失）就永久游离 | 在引导 effect 的 cleanup 里兜底 `hideScrollbarGrab()` |

顺带清理：`fillHandleLoggedRef`（按 unitId 只增不减）也在统一释放入口里 `delete`；
`resetAllDirty()` 以前全仓没有调用点，现在在整实例拆卸时调用。

**明确判定"不是泄漏"的（记下来免得下次重复怀疑）**：`toast` 的 3.2s 定时器（最多 3 个待触发、不抓大对象）、
`downloadBytes` 的 10s `revokeObjectURL`（有界延迟回收，是必要设计）、
`lock.ts` 的 30 个命令注册 + `beforeCommandExecuted`（全部进 `disposables`，`attachSheetDeps` 每次重装前先
`restore()`）、`ensureSwapCommandRegistered`（`hasCommand` 真幂等）、`fill-handle` 的 prototype/服务补丁
（双重标记守卫，每个实例只包一次）、`parser/xml.ts` 的 `CLOSE_TAG_CACHE`（上限 256）、
Univer 自己的撤销栈（上游 `STACK_CAPACITY = 20`）。

**仍然存在、但属于设计取舍（未改，写清楚）**：

1. **每个"打开过的"标签常驻一份原始字节**（`tabsDataRef[].bytes`，外科式导出的依据）。冷存只放模型、不放字节，
   所以常驻内存是 O(打开的标签数 × 文件大小)，关标签才释放。这是既定设计（见 `M4-多标签内存分析.md`）。
2. **自动保存的写放大**：会话把**所有标签的字节**整体结构化克隆进 IndexedDB，而指纹里含 `dirtyCellCount`，
   所以"每改到一个新单元格"就会触发一次全量克隆 → 长会话下表现为 GC 抖动/卡顿。
   **这不是泄漏**（单键覆盖、关标签即消失），要根治得改成"每标签一条记录 + 惰性字节句柄"。
3. `src/univer/drag-probe.ts` 是 P0 探测遗留、**全仓无调用点**；它的 `dispose()` 不摘"手势进行中"注册的
   window 监听（模式本身是真漏，但今天跑不到）。留着不动，接进 App 前必须按同款写法收口。

**验证**：

- 新增 `tests/e2e/memory-leaks.spec.ts` —— 3 例，断言的是 `__p0.retainedImageObjectUrls()` 这个**确定性计数**
  （不是堆采样，所以没有"GC 没跑"的噪声）：① 关标签后该簿 url 归零；② 被冷存的标签 url 归零、**切回重建后
  重新登记**（证明回收没把"以后还能插图"堵死）；③ 关掉全部标签后 url 与脏格账本都回空。
- `tsc --noEmit` 0 错；单测 **467/467**；主项目全量 + 串行项目 **19/19**（含 `tabs-memory` 的冷存/重建/关标签三例，
  正是本次改动最集中的路径）。

**顺带发现（不是内存问题，未改）**：导入带特性的表（图片/批注/超链接）之后，标签立刻显示"未保存"——
因为应用特性时会写单元格，脏格账本照实记账。写这条回归用例时撞上的（关标签会先弹确认，Playwright 默认
取消对话框，所以用例里必须显式接受）。

## 3. 已知边界（诚实清单）

**保真度（P1 遗留，未变）**
- **能打开的格式**：`.xlsx` / `.xlsm`（启用宏：宏原样保留、**不执行**）/ `.xltx` / `.xltm`（OOXML 包，原字节直用）；
  `.csv` / `.tsv` / `.txt`（分隔文本，含 GBK 识别）、`.ods`（OpenDocument）、`.xls`（Excel 97–2003 的 BIFF8 二进制）
  —— 后三种自研解析后**翻译成一份规范 xlsx** 再走全链路，**导出会另存为 .xlsx**（原文件一个字节都不动）。
  **明确不支持**：`.xlsb`（二进制 OOXML，zip 里是 binary parts）、`.numbers` / `.et`（iWork / WPS 私有）。
  不支持的格式会给"为什么 + 请另存为 .xlsx"的提示。
- `.xls` 的**已知边界**：加密工作簿（`FILEPASS`）、BIFF5/BIFF7 及更早版本、图表工作表/宏工作表/对话框工作表
  不解析（检测到就明确报告，返回空表而不是乱码）；公式只搬 `FORMULA` 的**缓存值**（不反编译 RPN），
  错误值（`#DIV/0!` 等）如实丢弃并计数；`.xls` 里的条件格式/数据验证/批注/超链接/图片不解析。
- `.ods` 的**已知边界**：无法可靠翻译的 OpenFormula 公式**丢掉公式保留值**（并计数）；
  复杂样式（背景图、条件格式）不解析；重复行列的超长空段按安全阀截断并报告。
- `.csv` 的**已知边界**：日期/百分比/布尔一律按**文本**导入（CSV 本身没有类型，不替用户猜）；
  带前导零的数字保留为文本；多表不存在（一张表）。
- 表格配色是按主题色的算法近似；行高/列宽为估算（`LINE_HEIGHT_FACTOR = 1.36`）。
- 不支持：dxf 边框、超链接 tooltip、批注作者、`top10` / `containsErrors` 规则、
  >3 色色阶、数据条渐变与负值配色、`absoluteAnchor` 图片、iconSet 的 `gte` 边界。
- 图表 / 透视表 / 迷你图 / 形状 / VBA **不解析**，但会在 `report.unsupported` 里列出，导出时原样保留字节。
- **打印设置**（`pageSetup`/`pageMargins`/`headerFooter`/`printOptions`/分页符）现在**会解析**成真值
  并列入 `report.preserved`：它们只影响打印，预览与编辑不受影响，导出原样保留 ——
  本工具**不做打印预览/打印输出**（不是"漏了"，是屏幕预览工具）。
- **填充画法**只有纯色（solid）：`patternFill` 的其他图案与 `gradientFill` 一律按"无填充"渲染，
  但**只有当某个单元格真的引用了它**时才会记一条降级 warning（未引用的不打扰用户）。

**交互与结构**
- 多选：**矩形选区 + 不连续多区域（Ctrl+点选 / Ctrl+框选）都已支持**（见缺陷 R）。
  多块下"追加类"操作（复制/剪切到工作区、复制内容、清空）作用于**全部块**；
  "两方类"操作（与工作区互换、粘贴）**只支持单块**并会说明原因。
  拖动搬运与点击互换仍以"指针下的那一格 / 主区域"为准（天然是单方动作）。
- 属性面板类能力（字体、边框等）按约束**永久不提供**，不是"没做完"。
- **自动填充（填充柄）永久不提供**：选区右下角那个小方块已**不再绘制**（见缺陷 P）。
  残留一处 1px 的既有热区：选区**右边界**那 1 像素宽的位置被 Univer 当作"拖动边框移动区域"，
  而移动区域属于被闸门拒绝的结构改动，所以在那里按下拖动不会有反应（不可见、不影响正常点选与框选；
  要一并去掉需要动 Univer 选区控件内部，收益极小，故保留并记录在此）。

**性能（⑩）**
- 已有实测数字（见 ⑩ 章节）：百万格 6.1 MB 文件解析 1180 ms / 端到端 1439 ms / 拖动 p50 4.2 ms。
- 单标签内存已优化（见"内存优化"）：模型不再常驻，浏览器堆 543 → 446 MB，导出产物逐字节不变。
- 多标签内存已做**常驻窗口 K=3**（见 ⑧ 章节）：非活动标签冷存、切回重建，内存从 O(N) 变 O(K)，
  实测第 4 个标签让堆从 347 → 194 MB。**未做/边界**：
  - 冷标签切回要**重解析**（百万格 ≈1.2 s），期间只显示"正在重建标签…"（未做骨架屏/进度条）；
  - 每个标签仍各存一份原始字节（6.1 MB/百万格文件），这部分**故意不省**——导出与会话恢复都要它；
  - 撤销历史是**全局一条**（不是每标签独立），冷存重建会把它清零（栈清了账本必须一起清）；
  - 同一文件开两个标签仍是两份模型（不做样式表共享：收益小、且触碰"格式不可改"的硬约束）。
- **未做**：极端规模（如 100 万行 × 50 列）未测；需要的话可用 `BENCH_ROWS`/`BENCH_COLS` 生成更大夹具。
- **未测**：Chrome 单 renderer 的堆上限（"最多能开几个标签"因此只有估算：常驻窗口下不再随标签数线性增长）。

## 4. 运行方式

**一键运行（Windows，双击即可）**：`run.cmd` —— 只负责把工具跑起来：检查环境 → 首次装依赖 →
起服务 → 打开浏览器。关掉窗口（或 Ctrl+C）服务即结束、端口自动释放；窗口被强杀时由
脱离控制台的看门狗（`tools/run-helper.ps1`）兜底清理，不留后台 node。
可选参数：`-Port 5300`、`-NoBrowser`、`-SkipInstall`。
测试与基准**不放在这个启动器里**，用下面的 npm 脚本。

```bash
cd excel-preview
npm install
npm run fixtures        # 生成 fixtures/*.xlsx + fixture-macro.xlsm（功能夹具，共 7 个样本）
npm run bench:fixture   # 生成 fixtures/bench-large.xlsx（性能夹具，50k×20）
npm run dev             # 开发预览（等价于启动器做的事，但需自己开浏览器）
node node_modules/typescript/bin/tsc --noEmit
node node_modules/vitest/vitest.mjs run
node node_modules/@playwright/test/cli.js test
npm run bench:parse     # Node 侧性能基准（打印指标表）
```

性能剖析（V8 CPU 剖面，会打印自身耗时 Top）：

```powershell
$env:BENCH_PROFILE='1'; node tools/bench-parse.mjs
```

自定义规模 / 只测某一份文件：

```powershell
$env:BENCH_ROWS='100000'; $env:BENCH_COLS='30'; node tools/make-benchmark.mjs
$env:PERF_FIXTURE='bench-medium.xlsx'; node node_modules/@playwright/test/cli.js test tests/e2e/perf.spec.ts
```

并发跑多份端到端时给独立输出目录，否则会互相删 `test-results`：
`$env:PW_OUTPUT_DIR='test-results-x'`（PowerShell）。
