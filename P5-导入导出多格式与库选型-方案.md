# P5 方案（第 4 版）：多格式导入导出 —— 本地转换桥 + 轮子兜底

> 版本沿革：v1 提出"引入 SheetJS 当广度引擎"；v2 你定了①依赖走 `@e965/xlsx@0.20.3`（npm）、②`.ods`/`.xls` 导出必须带样式；
> v3 你选了 `.xls` 走"借本机 Excel 转换"（路线 B），方案整并成"桥负责保真、轮子负责兜底"；
> **v4（本版）：桥与双形态已经**落地实现**（不再是"计划"），并补上"只保留 zh-CN / 静态+本地双形态 / 加载速度优化"三件事的实测数字。
> 见 §9（静态托管）与 §10（本轮已实现 vs 仍待做）。

---

## 0. 结论先说

| 项 | 结论 |
| --- | --- |
| **主干不动** | OOXML 解析、外科式导出、synth-xlsx、内容锁定、撤销账本 —— 一行不改 |
| **保真的旧格式导出** | ✅ **已实现**：**本地转换桥**：先把"用户当前看到的内容"导出成 xlsx 字节，交本机 Excel（COM）另存为 `.xls`(56) / `.ods`(60) / `.xlsb`(50) → **100% 保真**（样式/公式/CF/批注/图片都带）。实测：`fixture-styles.xlsx → .ods` 产出 5205 字节的合法 OpenDocument 包（Excel 16.0） |
| **顺带解决 .xlsb 导入** | ✅ **已实现**：`.xlsb` → Excel 另存 `.xlsx` → 走自研解析器 → **预览里样式/CF/DV 全在**（比轮子读出来的"只有值"强得多）；无 Excel 时按"打不开"处理并说明原因 |
| **双形态** | ✅ **已实现**：`npm run build:local`（`base: '/'`，带桥）/ `npm run build:static`（`base: './'`，无桥、桥入口置灰说明"本地版可用"）。形态经 `--mode` 编译进产物（`__APP_FORM__`），跨平台无需 cross-env |
| **轮子（`@e965/xlsx`）保留两处** | ⏳ 待接入：① 无 Excel 机器上的导入兜底（值 + 数字格式）；② 导出**不承载样式**的格式：`.txt/.dif/.sylk/.rtf/.prn/.dbf` |
| **导出第一批** | ✅ 已上：`.xlsx/.xlsm` 保真导出（菜单默认项）、`.csv`（当前表，UTF-8 BOM）、`.ods/.xls/.xlsb`（本地版经 Excel）；⏳ 待上：`.xlsx` 干净重建、全表 CSV/TSV、`.txt/.dif/.sylk/.rtf/.prn/.dbf` |
| **加载速度** | ✅ **已实测**：首屏首次内容绘制 **556 ms → 44 ms**（骨架 79 ms 可见）；入口 chunk **6792 KB → 1.4 KB**；产物 **11.17 MB → 6.79 MB**；断词词典分块 **69 → 0** |

---

## 1. 为什么这条桥能保真，而轮子不能

（这一节回答你上一轮的提问"为什么另存为会丢格式"，也是选择桥的依据。）

| | 轮子（SheetJS CE） | 本地转换桥（本机 Excel） |
| --- | --- | --- |
| 样式信息来源 | **读的时候就没读进来**：CE 只取"填充色 + 数字格式 + 行列尺寸 + 合并"，字体/边框/对齐/CF/DV/批注/图片都不解析（实测见 §4.2） | Excel 打开的就是**完整文件**，它自己知道全部格式 |
| 写出能力 | CE 的写入器只序列化值/数字格式/行列/合并；**完整样式是 Pro 付费能力**（官方文档：*"SheetJS Pro offers cell / text styling, conditional formatting…"*） | Excel 自己另存：格式、公式、CF、批注、图片由 Excel 序列化，与"人工打开再另存"**完全等价** |
| 文件其余部分 | 整本重写，图表/透视/VBA/主题都会丢 | Excel 决定目标格式支持什么、如何降级，**不是你我来猜** |
| 代价 | 纯前端、无外部依赖、快 | **必须有本机 Excel**（Windows）；启动 Excel 约 1–3 秒；需要一个本地桥 |

一句话：**轮子的模型是"值"，Excel 的模型是"文件"**。要"另存为别的格式还长一样"，就得让 Excel 干这活。

---

## 2. 架构：一条桥 + 一个轮子

```
                  浏览器（我们的应用）
   ┌───────────────────────────────────────────────────────────────┐
   │ 导入 openWorkbookBytes(bytes, name)                            │
   │   .xlsx/.xlsm/.xltx/.xltm → 自研 OOXML（不变）                 │
   │   .csv/.tsv/.txt          → 自研分隔文本（不变）               │
   │   .ods / .xls             → 自研（不变）                       │
   │   .xlsb/.xlml/.wk*/.dbf   → ① 本机有 Excel：桥转成 xlsx（保真）│
   │                             ② 没有 Excel：轮子读（值+数字格式）│
   │   自研解析抛错            → 轮子兜底重试（标注"已降级"）        │
   └───────────────────────────────────────────────────────────────┘
   ┌───────────────────────────────────────────────────────────────┐
   │ 导出「导出」菜单                                                │
   │   .xlsx/.xlsm 保真（外科式，默认）        ← 自研，不变          │
   │   .xlsx 干净重建（值+样式）               ← 自研 synth-xlsx      │
   │   .csv/.tsv                               ← 自研（BOM/编码可控）│
   │   .html/.pdf/.json（后续批次）            ← 自研                │
   │   .ods/.xls/.xlsb（带样式）               ← 桥：xlsx → Excel    │
   │   .txt/.dif/.sylk/.rtf/.prn/.dbf          ← 轮子（无样式语义）  │
   └───────────────────────────────────────────────────────────────┘
                              │ 127.0.0.1 本地调用
                              ▼
   ┌───────────────────────────────────────────────────────────────┐
   │ 本地转换桥（Node 中间件 + PowerShell COM helper）              │
   │   POST /api/convert  { bytes(≤), to: 'xls'|'ods'|'xlsb'|'xlsx' }│
   │   ① 写入临时文件（路径由服务端生成，客户端只能传字节 + 格式枚举）│
   │   ② 调 Excel COM：Open → SaveAs(FileFormat) → Quit              │
   │   ③ 回传目标字节；清理临时目录与残留 EXCEL.exe；超时保护        │
   │   DisplayAlerts=false / Visible=false / AutomationSecurity      │
   └───────────────────────────────────────────────────────────────┘
```

**为什么桥的输入要用"外科式补丁后的 xlsx"而不是重建的 xlsx**：
外科式导出的产物 = 原文件 + 脏格回写 = **用户当前看到的内容**，且保留了图表/透视/VBA/图片等全部部件；
交给 Excel 另存时，Excel 会把这些部件按目标格式的能力**自己**转换或降级。这样连"条件格式、图片、图表"都能带到 `.ods/.xls` 里（能带多少由 Excel 说了算，不由我们猜）。
（若原文件本身就是转换源，例如 csv 导入的，则用 synth-xlsx 的产物作为桥的输入。）

---

## 3. 桥的设计细节（要在方案里定死，避免后面返工）

| 关注点 | 设计 |
| --- | --- |
| 形态（待你选） | **a. Vite 中间件（推荐）**：项目本来就是本地服务（`run.cmd`/`npm run dev`），加一个 `/api/convert` 路由即可，零额外进程；`vite preview` 用 `configurePreviewServer` 同样支持。<br>**b. 独立 helper**：常驻小进程（Node 或 PowerShell HttpListener），与 dev server 解耦，但多一个进程要管（run.cmd 需一并启停）。 |
| API 约定 | `POST /api/convert`，body 为二进制 + 查询参数 `to=xls|ods|xlsb|xlsx`（**枚举白名单**）；返回目标字节流 + `X-Convert-Warnings` 头（Excel 自己的告警文案）。 |
| 安全 | 只监听 `127.0.0.1`；**客户端不能传路径**（临时文件由服务端生成并回收）；格式白名单；单请求体积上限；同一时刻只跑一个转换（串行队列）；临时目录在 `%TEMP%` 下按请求建、结束即删。 |
| Excel 进程 | `New-Object -ComObject Excel.Application` + `Visible=false` + `DisplayAlerts=false` + 打开前设 `AutomationSecurity=ForceDisable`（宏不执行，沿用我们"宏只保留不执行"的策略）；`finally` 里 `Quit()` + 释放 COM；超时（如 30 s）后结束我们启动的 EXCEL.exe；日志记录耗时与告警。 |
| 无 Excel 时 | 启动探测一次（`Test-Path` Excel 可执行文件或试起 COM）；不可用时：导出菜单里 `.ods/.xls/.xlsb` 置灰并注明"需要本机安装 Excel"；导入 `.xlsb` 自动走轮子（值+数字格式），摘要写明"已降级"。 |
| 目标格式映射 | `.xls` 56 / `.ods` 60 / `.xlsb` 50 / `.xlsx` 51→（用 51 会丢宏，宏文件用 52 `.xlsm`）；CSV 不走桥（我们自研更可控）。 |
| 降级与告知 | 桥返回 Excel 的告警（例如"另存为 ODS 可能丢失某些格式"）→ 原样显示给用户，不粉饰。 |

---

## 4. 实测证据（本机跑过，可复现）

### 4.1 性能（同一台机器、同一份夹具）

夹具 `fixtures/bench-large.xlsx`：6.08 MB、5 万行 × 20 列 = **1,000,020 格**。

| 引擎 | 最好用时 | 峰值堆 | 说明 |
| --- | --- | --- | --- |
| 自研 `parseXlsx`（`npm run bench:parse`，Node） | **2853 ms** | 93 MB 模型 | 含样式/CF/DV/批注/图片等完整模型 |
| SheetJS CE（Node，`dense+cellStyles+cellNF`，遍历全表） | **2268 ms** | ~178 MB | 只要值 2195 ms；稀疏 3276–3332 ms；`sheetRows:1000` 609 ms |
| ExcelJS（Node，`load`+遍历） | 2082 ms | — | 只 xlsx |

→ "轮子=更快"不成立（同一量级）。轮子的价值在**格式广度与兜底**，不在速度。
→ 顺带发现文档口径问题：`P3`/`README` 的"百万格解析 **1180 ms**"是浏览器侧旧记录，与当前 Node 侧 `bench:parse` 2853 ms 对不上，P0 复测后统一（Node 侧 / 浏览器侧分开报）。

### 4.2 轮子的样式上限（决定"哪些格式不能交给它"）

CE 读我们的 `fixture-styles.xlsx`（`cellStyles:true, cellNF:true`）：

```
A1   {v:"样式保真样本（P0 fixture）", z:"General", s:{patternType:"solid", fgColor:{rgb:"DBEAFE"}}}
E3   {v:"粗体",                        s:{patternType:"none"}}   ← 粗体没读进来
F3   {v:"粗蓝 + 斜体 + 下划线绿",       s:{patternType:"none"}}   ← 字色/斜体/下划线全丢
D3   {v:"自动换行的长文本…",            s:{patternType:"none"}}   ← 换行/对齐全丢
fixture-rules.xlsx  → 只有 !ref/!margins/!cols：条件格式、数据验证没有
fixture-extras.xlsx → 只有 !ref/!margins/!cols/!legrel/!legdrawel：批注、图片、表格没有
```

---

## 5. 分工矩阵（最终版）

### 5.1 导入
| 格式 | 由谁负责 | 保真度 |
| --- | --- | --- |
| `.xlsx/.xlsm/.xltx/.xltm` | 自研 OOXML | 完整（样式/CF/DV/批注/图片/链接/表格/打印设置） |
| `.csv/.tsv/.txt` | 自研分隔文本 | 值（编码识别 + 前导零保文本） |
| `.ods` / `.xls` | 自研 | 样式/合并/行列/冻结/数字格式 |
| **`.xlsb`** | **桥（有 Excel）→ 转 xlsx → 自研** | **完整** |
| `.xlsb`（无 Excel 时） | 轮子 | 值 + 数字格式 |
| `.xlml/.wk1-3/.sylk/.dif/.prn/.rtf/.dbf/.html` | 轮子（有 Excel 时可走桥，按需） | 值（Excel 路线则完整） |
| 自研解析失败 | 轮子兜底 | 值 + 数字格式，摘要标注"已降级" |

### 5.2 导出（第一批 = 你选的四项 + 建议追加的三项）
| 格式 | 引擎 | 带什么 | 备注 |
| --- | --- | --- | --- |
| `.xlsx/.xlsm`（原名） | 自研外科式 | 全部（未改部件字节保留） | 默认项，现有能力 |
| `.xlsx` 新文件 | 自研 synth-xlsx | 值/公式/样式/合并/行列/冻结 | 去掉图表/透视/VBA，换来"干净的一份" |
| `.csv / .tsv` | 自研 | 当前表或全部表的显示值；UTF-8 BOM（可选 GBK） | 给其它系统导入用 |
| `.txt/.dif/.sylk/.rtf/.prn/.dbf` | 轮子 | 值 | 这些格式本就不存样式，无损失 |
| **`.ods`（建议纳入）** | **桥** | **完整（由 Excel 决定降级）** | 用户明确要求带样式 |
| **`.xls`（建议纳入）** | **桥** | **完整** | 同上 |
| **`.xlsb`（建议纳入）** | **桥** | **完整** | 体积小、Excel 常用 |
| `.html` / `.pdf` / `.json` | 自研（后续批次） | HTML：内联样式 + 浏览器打印成 PDF；JSON：值/公式/样式对象 | 这一批不做 |

---

## 6. 依赖与合规

| 项 | 方案 |
| --- | --- |
| 依赖 | `@e965/xlsx@0.20.3`（SheetJS CE 官方构建的 npm 转发，Apache-2.0，零传递依赖，自带 TS 类型） |
| 加载 | `await import()` **懒加载**：主 chunk 不变；只有"导入非自研格式（无 Excel 兜底）"或"导出无样式格式"时才拉（`xlsx.mjs` 930 KB，gzip ≈ 300 KB；纯 csv 场景可用 `xlsx.mini.min.js` 273 KB） |
| 许可 | 保留 Apache-2.0 LICENSE 与版权声明；README 增加"第三方许可"小节 |
| 桥的合规 | 桥只用**用户本机已安装的 Excel** 做格式转换，不打包/不分发 Office；不联网 |
| 护栏 | e2e：打开 `.xls` 时不得请求轮子分块；桥不可用时导出菜单正确置灰；桥的临时目录与 EXCEL.exe 无残留 |
| 不做 | 不引入 ExcelJS 作为运行时依赖（仓库里保留 devDependency 用于夹具）；不支持加密文件（AES/RC4）；不做 `.numbers` 导出 |

---

## 7. 分阶段落地与验收

### P0 桥（1–1.5 天）
- Node 中间件 `/api/convert` + `tools/convert-via-excel.ps1`（COM：Open → SaveAs → Quit，含超时/清理/告警回传）；
- 探测 Excel 是否可用并在 UI 上暴露（可用性 + 版本）；
- 验收：单测（格式枚举/体积上限/临时目录清理）+ e2e（导出 .xls/.ods/.xlsb 后**用 Excel COM 回读**，与导出前的模型逐格对拍；无 Excel 环境分支单独测"置灰 + 提示"）。

### P1 导出菜单（1 天）
- 工具栏「导出」→ 菜单：保真 `.xlsx`（默认）/ `.xlsx` 重建 / `.csv` / `.tsv` / `.ods` / `.xls` / `.xlsb` / `.txt`… 每个格式一句"带什么、丢什么"；
- 范围选择（当前表 / 全部表）；文件名规则（`原名-已编辑.xlsx` → `原名.ods` 等）；
- 验收：e2e 逐个格式导出 + 回读对拍；文件名与编码用例；桥失败时的错误提示（Excel 未装 / 被占用 / 超时）。

### P2 导入补广度（0.5–1 天）
- `.xlsb` 走桥（有 Excel）/ 走轮子（无 Excel）两条路，都汇入 `openWorkbookBytes` → `WorkbookInput` → 规范 xlsx → 现有全链路；
- 摘要写明"由 XLSB 转换导入"与降级信息；`accept`/格式表/README 同步；`tools/make-legacy-fixtures.ps1` 增加 `.xlsb` 真值（`SaveAs(…, 50)`）。

### P3 轮子收尾（0.5 天）
- 装轮子、写 `tests/unit/lib-parity.test.ts`（20 份真值夹具上比较"轮子值/数字格式"与自研模型，输出差异报告）；
- 导出 `.txt/.dif/.sylk/.rtf/.prn/.dbf`；懒加载护栏。

### P4 文档与口径（0.5 天）
- README 格式表（`.xlsb` ❌ → ✅，注明"有 Excel 时完整保真、无 Excel 时仅内容与数字格式"）；
- 修复"1180 ms vs 2853 ms"的口径问题（Node 侧 / 浏览器侧分开写）；交付说明新增一章；许可清单。

---

## 8. 已定 / 待定

**已定**：① 依赖用 `@e965/xlsx@0.20.3`（懒加载）；② `.ods`/`.xls` 导出必须带样式；③ `.xls` 走"借本机 Excel 转换"（路线 B）；④ 导出第一批含 `.xlsx` 保真、`.xlsx` 重建、`.csv/.tsv`、`.txt/.dif/.sylk/.rtf/.prn/.dbf`。

**待定**：
- ⑤ 桥的形态：**a. Vite 中间件（推荐）** / b. 独立 helper 进程；
- ⑥ 是否把 `.ods`/`.xls`/`.xlsb` 导出与 `.xlsb` 导入一并放进第一批（我建议放，因为桥已经在了，边际成本≈0；不放的话，这些格式要等下一轮）；
- ⑦ 是否要"静态版 + 本地版"双形态（见 §9：静态托管可行但桥不可用）。

---

## 9. 静态托管（GitHub / Gitee Pages）—— 实测结论与对方案的影响

**结论：应用本体可以当纯静态站点托管，我已经实测通过；但"本地转换桥"在静态托管上不可能存在，所以导出能力要分两档。**

### 9.1 实测（本机，2026-xx）

```
node node_modules/vite/bin/vite.js build --base=./      # 11.2 MB / 82 个文件
# 把 dist 挂在子路径 /repo/ 下当静态站点（模拟 GitHub Pages 项目站点），再跑真实浏览器：
[static-smoke] 下载文件: fixture-styles-已编辑.xlsx 8320 bytes
[static-smoke] 请求失败: []      ← 无任何资源加载失败
[static-smoke] 4xx/5xx: []       ← 无 404/5xx
```
断言过的三件事：① 引导成功（`window.__p0` 就绪）；② 「打开表格」走真实文件输入能导入并渲染（A2 = "区域"）；③ 导出触发真实下载（纯前端 Blob，不需要任何后端）。
源码里也没有任何运行时 `fetch`、`Worker`、绝对资源路径（已全仓 grep 确认），所以静态托管不会缺接口。

### 9.2 要改的 3 处（都很小）

| # | 改动 | 原因 |
| --- | --- | --- |
| 1 | `vite.config.ts` 加 `base: './'` | 项目站点在 `https://<user>.github.io/<repo>/` 子路径下，默认的 `/assets/...` 会 404（实测用 `--base=./` 通过） |
| 2 | 产物加 `.nojekyll` | 让 GitHub Pages 跳过 Jekyll 处理（Vite 产物本没有下划线目录，加上更保险） |
| 3 | CI：GitHub Actions `npm ci → npm run build → actions/deploy-pages` | 用自定义工作流还能避开"每小时 10 次构建"的软限制 |

### 9.3 静态托管下**会失效**的能力（必须如实告知用户）

| 能力 | 静态版 | 本地版（`run.cmd`） |
| --- | --- | --- |
| 打开/编辑/工作区/撤销/会话恢复/导出 xlsx 保真 | ✅ | ✅ |
| 导出 `.csv/.tsv`、未来的 `.html/.pdf/.json` | ✅（自研） | ✅ |
| 导出 `.txt/.dif/.sylk/.rtf/.prn/.dbf` | ✅（轮子） | ✅ |
| 导入 `.xlsb` 等新格式 | ⚠️ 只能走轮子（**值 + 数字格式，样式/CF/批注不进来**） | ✅ 走桥，**完整保真** |
| 导出 `.ods/.xls/.xlsb`（带样式） | ❌ 除非再写自研写入器 | ✅ 走桥，**完整保真** |

→ **建议双形态**：静态版给所有老师"免安装、打开就能用"；本地版（`run.cmd`）给需要"另存为 .ods/.xls 还要保样式"的场合。
静态版里对桥相关功能做**能力探测 + 置灰 + 一句"本地版可用"**，不要给一个点了报错的按钮。

### 9.4 托管选型（含国内访问）

| 平台 | 能不能用 | 备注 |
| --- | --- | --- |
| **GitHub Pages** | ✅ 可以 | 公开仓库在 Free 计划即可；**私有仓库需要 Pro/Team/Enterprise**；站点 ≤ 1 GB、月流量软上限 100 GB、单次部署 ≤ 10 分钟（[官方限制](https://docs.github.com/en/pages/getting-started-with-github-pages/github-pages-limits)）。国内访问偏慢/不稳定 |
| **Gitee Pages** | ⚠️ 不要当主方案 | 社区多处反馈**已下架/无法新开**（Gitee 帮助入口也返回 404）；若你账号里还有入口可以试，但不建议押注 |
| 腾讯云 **EdgeOne Pages** | ✅ 值得先试 | 被称为"国内版 Vercel"，有免费套餐，国内访问快（[介绍](https://cloud.tencent.cn/developer/article/2565878)）；自定义域名要注意备案 |
| 腾讯云 COS / 阿里云 OSS 静态网站 | ✅ 便宜可控 | 几元/月，可绑自有域名；用平台默认域名一般无需备案，绑自有域名需备案 |
| Cloudflare Pages / Vercel / Netlify | ⚠️ 国内常打不开 | 部署最省事，但有实测反馈"国内基本打不开"（[例子](https://haoyelaiga.com/%E4%BD%BF%E7%94%A8cloudflare-page%E6%9E%84%E5%BB%BA%E7%9A%84%E9%9D%99%E6%80%81%E7%AB%99%E7%82%B9-%E5%9B%BD%E5%86%85%E5%9F%BA%E6%9C%AC%E6%89%93%E4%B8%8D%E5%BC%80/)），不适合给老师用 |

### 9.5 顺带三条注意

1. **包体（已优化）**：优化前是"一个 6.8 MB 的入口 chunk + 69 个断词词典分块"；现在拆成 `入口 1.4 KB / App / vendor-react / vendor / vendor-univer` 并行下载，产物 11.17 MB → **6.79 MB**，首屏首次内容绘制 **556 ms → 44 ms**。剩余的大头仍是 Univer 引擎本身（gzip 约 1.4 MB），要再降只能裁剪 preset（会牺牲功能，暂不做）。
2. **剪贴板**：右键「复制」用 `navigator.clipboard`，需要 **HTTPS 或 localhost**。静态托管是 HTTPS，没问题；但用 `http://局域网IP` 直连给同事用时复制会失效（浏览器限制），这一点在"怎么分享给别人用"的说明里要写清。
3. **仓库卫生（重要）**：**不要**把真实学生/班级文件提交到公开仓库（座位表、成绩表里有姓名学号）。发布前确认 `.gitignore` 排除 `*.xlsx`/`*.xls`/`*.csv`（仓库里只保留 `fixtures/` 下由脚本合成的样本）。学生数据始终只在浏览器本地（IndexedDB），不上传任何服务器 —— 这是公开托管的最大加分项，也意味着"公开托管 = 任何拿到链接的人都能用这个工具"，可以接受，但你要知道。

---

## 10. 本轮已实现 / 仍待做（照着改就行，不用再翻设计）

### ✅ 本轮已实现（有实测与用例守着）

| 能力 | 落点 | 证据 |
| --- | --- | --- |
| 只保留中文 | `build/vite-plugin-drop-hyphenation.ts` 清掉 engine-render 的 69 个断词词典分块 | `npm run measure:bundle` 输出"断词词典分块 0 个 ✓"；产物里无 `"de-DE"`/`Arbeitsblätter` 等字面量 |
| 加载速度 | `index.html` 内联骨架 + `src/main.tsx` 懒加载 App + `manualChunks` 分包 + `performance.mark` 打点 | `tests/e2e/static-form.spec.ts` ② 打印并断言：骨架 62–106ms、FCP 40–44ms、表格可用 382–462ms；实测对比：FCP 556→44ms、入口 6792KB→1.4KB、产物 11.17→6.79MB |
| 双形态 | `vite.config.ts`（`--mode static` → `base:'./'` + `__APP_FORM__`）、`src/shell/app-form.ts`、`npm run build:static/local` | `tests/e2e/static-form.spec.ts` ③：静态版 `/api/bridge/health` 返回 404，导出菜单里 `.ods/.xls/.xlsb` 置灰且写明"本地版可用" |
| 本地转换桥 | `build/vite-plugin-excel-bridge.ts`（dev+preview 中间件）+ `tools/excel-bridge.ps1`（Excel COM） | `tests/e2e/export-formats.spec.ts` ③：经本机 Excel 真导出 `.ods` 5205 字节，且校验为合法 OpenDocument 包（PK 头 + mimetype） |
| 导出菜单 | `src/shell/Toolbar.tsx` 的「导出」→ 菜单；`.xlsx` 保真（默认）/ `.csv`（自研，UTF-8 BOM）/ `.ods/.xls/.xlsb`（桥） | `tests/e2e/export-formats.spec.ts` ①②；`tests/unit/app-form-export.test.ts` 钉住 CSV 引号/BOM/文件名规则 |
| `.xlsb` 导入 | `src/App.tsx` 的 `handleFile`（桥可用→先转 xlsx）、拖放过滤同步放开、`file-kinds.ts` 区分"桥专属格式" | `tests/e2e/formats.spec.ts` ⑦ 与 `file-drop.spec.ts` ③：桥可用时 `.xlsb` 经 Excel 转换后照常打开（日志 `import:bridge-converted`），不可用时给"需要本机 Excel + 另存为 .xlsx" |
| 测试与部署 | `tests/e2e/static-form.spec.ts`、`tests/e2e/export-formats.spec.ts`、`.github/workflows/static-pages.yml` | 全量：`tsc` 0 错、单元 **461**、e2e **101** 全绿 |

### ⏳ 仍待做（都是"加菜"，不影响现在能用）

1. **轮子接入**（`@e965/xlsx@0.20.3`）：无 Excel 机器上的导入兜底 + 导出 `.txt/.dif/.sylk/.rtf/.prn/.dbf`；接入后补"值对拍"单测。
2. **`.xlsx` 干净重建导出**：从当前模型（值+样式+合并+行列+冻结）重建一份新 xlsx（与"保真导出"并列成第二项）。
3. **CSV 范围选择**：当前只有"当前工作表"，可加"全部工作表（打包 zip）"与 GBK 编码选项。
4. **HTML/PDF/JSON 导出**：HTML 用内联样式（也是 PDF 的打印源），JSON 给程序用。
5. **桥的批量与进度**：目前一次一个文件（Excel COM 串行）；多文件导出可加队列与进度提示。
6. **骨架屏细化**：目前是静态灰块（够用）；若要更像"真界面"，可在骨架里画工具栏图标占位。

---

## 附：证据复现方式
```powershell
# 轮子（评估用；正式接入走 npm 的 @e965/xlsx@0.20.3）
Invoke-WebRequest https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz -OutFile xlsx-0.20.3.tgz
tar -xzf xlsx-0.20.3.tgz     # → package/xlsx.mjs（1.0MB）、package/dist/*、package/types/

# 自研基线（Node 侧）：6.1MB / 100 万格 → 解压+解析 2853 ms、适配 95 ms
npm run bench:parse

# 样式保真对照：CE 读我们的样式夹具，看它给到什么
node -e "const X=require('./package/xlsx.js');const wb=X.read(require('fs').readFileSync('fixtures/fixture-styles.xlsx'),{cellStyles:true,cellNF:true});console.log(wb.Sheets[wb.SheetNames[0]]['E3'])"

# 本机 Excel COM 可用性（桥的前提；夹具脚本已在用同一套）
pwsh -NoProfile -File tools/make-legacy-fixtures.ps1

# 双形态构建 + 产物 / 启动体检
npm run build:static                 # base './'，无桥；丢到 Pages 用这一份
npm run build:local                  # base '/'，带桥
npm run measure:bundle               # 体积/分块/断词词典数量（应输出"断词词典分块 0 个 ✓"）
npm run serve:dist                   # 把 dist 挂在 /repo/ 子路径下（模拟 Pages 项目站点）
node node_modules/@playwright/test/cli.js test tests/e2e/static-form.spec.ts   # 启动预算 + 降级断言
node node_modules/@playwright/test/cli.js test tests/e2e/export-formats.spec.ts # CSV / 桥导出 .ods
```

外部依据：[SheetJS CE 安装与 vendoring](https://docs.sheetjs.com/docs/getting-started/installation/nodejs)、
[写出格式与选项（CE 写样式属 Pro）](https://docs.sheetjs.com/docs/api/write-options)、
[读入格式与 cellStyles 说明](https://docs.sheetjs.com/docs/api/parse-options)、
[ExcelJS 写大文件 OOM 记录](https://github.com/exceljs/exceljs/issues/2953)。
