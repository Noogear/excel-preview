# P2 交互与 UI — 交付说明

> **历史文档提示（后续已变更）**：本文记录的是 P2 阶段的实现快照。此后交互层做了两项调整，**以最新文档为准**：
> ① 「长按拖动」已改为**按住即可拖动**（长按判定模块已移除）；
> ② 交互模式从两种扩展为**三种（选择 / 拖拽 / 点击互换）**，且非选择模式下不再出现多选框。
> 现状见 [`P3-内容锁定与工作区-交付说明.md`](./P3-内容锁定与工作区-交付说明.md) 的 ④ 一节。

> 阶段目标：把 P0 验证台变成**能用的产品雏形**——正式界面、只改内容的编辑锁定、长按拖动互换、
> 侧边栏工作区（拖入暂存 / 拖回写入）、动画与视觉打磨。
> 结果：**19 个端到端用例 + 107 个单元测试全部通过**，`tsc --noEmit` 零报错。

---

## 一、这一阶段做了什么

### 1. 正式界面外壳（替换 P0 验证台）
- 顶栏：品牌标识、`打开 xlsx`、撤销/重做、运行日志开关
- 预览区：Univer 网格 + 公式栏（**关闭了 ribbon 与格式工具栏**——本产品不允许改格式，留着格式按钮会出现"点了没反应"的矛盾体验）
- 右侧工作区：暂存卡片列表、空状态引导、清空、卡片内缩略图
- 底部状态栏：活动工作表、选区、导入耗时（解析/转换/渲染三段）、未支持项计数、FPS、状态文案
- 运行日志抽屉：事件流 + 导入报告（未支持项 / 降级与警告）
- 视觉：统一设计令牌（色板/圆角/阴影/动效曲线）、浅色与深色跟随系统、
  `prefers-reduced-motion` 全量降级、Toast 提示、拖动时舞台高亮与落点提示胶囊

### 2. "只允许编辑内容"在正式 UI 下生效
沿用 P0 验证过的两层防线（命令层 no-op 替换 + mutation 层剥离样式 `s`），
并在每次活动工作表变化（引导 / 导入 / 切换标签）后自动重装。

### 3. 长按拖动互换（旗舰交互）
- 400ms 长按判定 + 4px 容差（与框选/滚动互不误伤）
- 拖动时：DOM 幽灵跟手（rAF 合并 + `translate3d`）、底部提示胶囊、舞台高亮
- 落点命中：复用 Univer 在按住拖动时持续派发的 `CellPointerMove`（P0 已证实），
  并用 `setActiveSelection` 让 Univer 自己绘制目标框（观感与原生一致、零额外绘制成本）
- 落点决策：同尺寸 → **互换**；尺寸不同 → **移动**（源清空）；与源重叠 → 拒绝并提示
- 拖到工作区 → 生成暂存条目（带飞入动画）；从工作区拖回 → **只写内容**，目标格式不动

### 4. 撤销原子性（不是想当然就有的）
"一次互换 = 一次撤销"。见下方"坑 ②"。

---

## 二、P2 阶段新踩到的 4 个坑（都已修，且都有测试兜底）

### 坑 ①：`StrictMode` 之上的**第二个**实例陷阱——`canvas` 选择器
Univer 内部还会创建一个 **0×0 的 docs 编辑器画布**（`id="univer-doc-main-canvas"`）。
一旦布局变化（例如关掉 ribbon），它会排到 DOM 最前面，
于是 `page.locator('canvas').first()` 命中的是它 → 所有基于 canvas 坐标的手势测试全线错位。
**对策**：测试里一律用 `canvas[id^="univer-sheet-main-canvas"]` 精确定位工作表画布。

### 坑 ②：Univer 的撤销**不是自动记录的**
`SetRangeValuesMutation` 只是改数据；**撤销记录要由命令自己在成功后调用
`undoRedoService.pushUndoRedo({ unitID, undoMutations, redoMutations })` 推入**。
我最初直接 `sequenceExecute` 两条 mutation，结果撤销完全无效（按一次 Ctrl+Z 什么都不回退）。
**对策**：互换命令自己构造 undo/redo 对（redo = 写入新值；undo = 写回原值），
于是"一次互换 = 一条撤销记录"成为**结构性保证**，不依赖任何批处理 API。

### 坑 ③：传 `null` 是"整格删除"，会连样式一起删掉
Univer 的 `SetRangeValuesMutation` 对 `null` 的处理是 `realDeleteValue(row, col)`——
**整格（含 `s` 样式）从模型里删掉**。用它来"清空源单元格内容"会让源单元格**掉格式**，
直接违反产品的核心承诺。
**对策**：清空内容必须传"只把内容字段置空"的对象：`{ v: null, f: null, si: null, p: null }`，
让已有的 `s` 保留下来。已封装为 `emptyContentCell()`，并有 e2e 断言"移动后源位置格式不变"。

### 坑 ④：React `setState` 是异步的，程序化流程别指望立刻读回
"把选区存入工作区 → 立刻按 id 写回"这类流程里，`items` state 还没更新，
按 id 查找必然失败。**对策**：加一个同步的 `Map` 快照存储，state 只负责渲染。

---

## 三、测试覆盖（P2 新增部分）

| 用例 | 断言要点 |
| --- | --- |
| 长按后拖到另一处 | 长按必须触发拖动；结束必须产生互换/移动动作；**源与相邻单元格格式一字不变** |
| 拖到侧边栏 | 条目生成、空状态消失、飞入动画的临时元素最终被清理（不残留） |
| 条目写回表格 | 内容被写入，**目标单元格格式一字不变** |
| 撤销原子性 | 互换后按**一次**撤销，两个单元格都还原 |
| 视觉基线 | 外壳整体 + 拖动中反馈（幽灵/提示/高亮）两张基线截图，越界即失败 |

单元测试新增：工作区样式映射与适配缩放（46 个）、交互原语与长按状态机（17 个）。

---

## 四、明确留到后续的项

| 项 | 属于 | 说明 |
| --- | --- | --- |
| 行高自适应 | P1 保真 | 自动换行的长文本目前按默认行高显示会被裁切（截图 row 5 可见），需按换行结果计算行高或开启 Univer 的 auto-height |
| 条件格式 / 数据验证 / 批注 / 超链接 / 图片 / Excel 表格斑马纹 | P1 保真 | 解析器已能识别并记账，渲染尚未接入 |
| 外科式修补导出 | P1 导出 | 依赖 `ParsedWorkbook.raw.entries`（已保留全部 zip 条目） |
| 自定义公式栏/单元格编辑体验 | P2.5 | 目前用 Univer 自带公式栏 |
| 工作区条目持久化 | P2.5 | 目前仅本次会话内存态（可加 localStorage / 导出） |
| 性能面板（接 `SheetRenderController.renderMetric$`） | P3 | 状态栏已显示 FPS，官方渲染指标管道已确认可用 |
