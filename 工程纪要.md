# 工程纪要

> 这份文件是**唯一的工程记录**：技术选型结论、关键设计决策与理由、实测数据、已知边界、测试与部署方式。
> 面向"要改这个项目的人"。用户视角的使用说明看 [`README.md`](./README.md)。
> 以前的 P0/P1/P2/P3/P5/M4 等分阶段文档已合并到这里（历史版本仍在 git 记录里）。

---

## 1. 技术选型（结论 + 为什么）

| 选择 | 结论 |
| --- | --- |
| 前端 | React 18 + Vite + TypeScript |
| 表格内核 | **Univer 0.25.1**（canvas 渲染 + 虚拟化，Apache-2.0 可商用）。所有 `@univerjs/*` 必须**同版本**；它是 `0.x`，命令 id / 权限点 / 事件会随版本变，升级要跑 Playwright 回归 |
| 导入 | **自研 OOXML 解析**（`src/parser/`）。**不用** `@univerjs-pro/*`：那是服务端转换 + 商业授权 + 水印/大小限制，与"纯前端、离线、免费"冲突 |
| 导出 | **外科式修补**（见 §4），不是"重新生成一个 xlsx" |
| 测试 | Vitest（纯逻辑）+ Playwright（canvas 只能端到端；断言走 `window.__p0` 钩子与真实键鼠） |

性能瓶颈排序：**解压/XML 解析 > 转 IWorkbookData > Univer 数据模型 > 渲染**。所以解析在 Worker 化的边界上、渲染反而不是问题。

---

## 2. 目录地图

```
src/
  App.tsx            主组件：引导、多标签、导入导出、工作区、交互装配（全项目最大的文件）
  main.tsx           入口：骨架 → 懒加载 App → 失败兜底卡
  parser/            自研解析：xlsx(xml/worksheet/styles/table/drawing/CF/DV/comment) + xls(BIFF8) + ods + csv
  importer/          解析结果 → Univer：to-univer（数据）/ apply-features（CF/DV/链接/批注/图片）/ synth-xlsx（翻译成规范 xlsx）
  exporter/          export-xlsx（外科式回写）/ csv-export / slim-source（瘦身导出源）
  univer/            setup（引导）/ read-only-guard（命令闸门，默认拒绝）/ lock（样式命令注销 + mutation 剥 s）
                     dirty-tracker（脏格账本：导出与"未保存"都靠它）/ fill-handle（去掉填充柄）/ swap-command（互换/移动）
  interaction/       三种模式的手势：drag-controller / click-swap / selection-ranges / scrollbar-drag / 动画
  workspace/         侧边栏"工作区"（逐格快照、导入面板、预览渲染）
  persistence/       IndexedDB 会话（自动保存 + 误关闭恢复）
  shell/             工具栏 / 标签条 / 右键菜单 / 历史面板 / 主题
tests/unit（纯逻辑）/ tests/e2e（真实浏览器，分段见 §6）
tools/               夹具生成、基准、启动器、Excel 桥、残留清理
fixtures/            脚本合成的样本（**不含任何真实学生数据**）
```

---

## 3. 三条硬约束与它们的设计

### 3.1 只编辑内容，绝不改格式（三层防线）

1. **命令闸门（默认拒绝）**：`read-only-guard.ts` 猴补 `ICommandService.executeCommand/syncExecuteCommand`，
   **只放行白名单**，其余一律拦掉（枚举"坏命令"必漏，所以反过来做）。
2. **样式命令注销 + no-op 替换**：`lock.ts` 把 30 个样式/结构命令注销，UI 点了也没反应。
3. **mutation 层兜底**：包装 `SetRangeValuesMutation`，把 `cellValue` 里的 `s`（样式）**剥掉**——
   无论打字/粘贴/填充/拖拽哪条路径，样式都写不进去。`p`（富文本）**必须保留**（超链接存在里面）。

踩过的坑：命令 id 的 `name` 与 `id` 不是一回事（如 `sheet.command.copy` 只是 name，真 id 是 `univer.command.copy`），
白名单写错会"空放行 + 真命令被拦"。

### 3.2 复制出去 / 粘进来

- **复制**：`Ctrl+C` 与右键「复制内容」单块都走 Univer 原生复制 → 一次写 `text/plain`（TSV，**显示值**）
  + `text/html`（内联样式：字体/字号/加粗/字色/底色/四边框/对齐/合并/列宽/行高，外层 `google-sheets-html-origin`），
  粘到 Excel/WPS/在线表格**带格式**。多块选区只能纯文本（原生复制只取"最后一个选区"），菜单标签如实写明。
- **兜底**：`navigator.clipboard` 只在安全上下文存在，`http://局域网IP` 下要退回
  `textarea + execCommand('copy')`（见 `src/shell/clipboard-write.ts`），否则是**静默失败**。
- **粘贴**：外部带格式内容**只写值**，目标格格式逐字段不变、也不会引入单元格内富文本（§3.1 第 3 层）。

### 3.3 多标签与内存纪律

- 打开的标签**不设上限**，但**同时实体化的只保留最近用过的 3 个**（`resident-tabs.ts`）；
  超出的冷存（`disposeUnit`）、切回时按需重建（重解析 → 建簿 → 应用特性 → 回放编辑）。
- **释放一个工作簿时必须一起清的账**（统一入口 `releaseUnitBookkeeping`，缺一项就是泄漏）：
  Univer 撤销栈（不随 dispose 清）、插图 blob url、渲染单元缓存、编辑桶、脏格账本、填充柄日志去重表。
- **应用特性期间暂停脏格记账**（`pauseDirtyTracking`）：装载不是"用户改动"，否则刚打开的文件立刻"未保存"、
  导出还会把本可原样保留的 XML 当脏格回写。
- 已知的**故意保留**：每个打开过的标签常驻一份原始字节（导出与会话恢复都要它，关标签即释放）。
- 已知的**写放大**（未修）：自动保存把全部标签字节整体结构化克隆进 IndexedDB，指纹含脏格数 →
  每改到一个新格子就全量克隆一次。要根治得改成"每标签一条记录 + 惰性字节句柄"。

---

## 4. 导出：外科式修补（保真的关键）

编辑被限制为"只改单元格内容"，所以原文件里**没动过的部件一个字节都不用重写**：

```
原 .xlsx (zip)
  ├─ xl/worksheets/sheetN.xml   ← 只重写**被编辑过的**工作表（按坐标补写 <c>）
  ├─ xl/sharedStrings.xml       ← 只**追加**新字符串（不改已有索引）
  ├─ xl/workbook.xml            ← 只为写入 <calcPr fullCalcOnLoad="1"/>（让 Excel 打开时重算）
  └─ 其余一切（styles/theme/图表/透视表/切片器/VBA/媒体…）← **原样字节保留**
```

**实测结论**（`tests/e2e/export-fidelity.spec.ts`）：

| 情形 | 结果 |
| --- | --- |
| 一次都不改就导出 | 除 `xl/workbook.xml`（calcPr）外，**全部 zip 部件逐字节一致** |
| 改了某个格子的值再导出 | 白名单外一切部件逐字节一致；白名单内**没被编辑**的部件也逐字节一致；**被重写的工作表 XML 里** `<hyperlink`/`<drawing`/`<conditionalFormatting`/`<dataValidation`/`<tableParts` 出现次数与原文完全相同；导出文件重新解析后特性计数与原文相等 |
| Excel 能否打开 | 经本机桥另存为 `.ods` 成功 ⇒ 解析无误、无修复提示 |

细节：改一个带超链接的格子时，`<hyperlink ref>` 与单元格样式索引都保留 —— **链接仍有效、格式不变**，只有显示文字变成你输入的内容。

---

## 5. 本地转换桥（借本机 Excel 转 .ods/.xls/.xlsb）

- 形态：Vite 中间件（`vite-plugins/vite-plugin-excel-bridge.ts`）+ PowerShell 脚本（`tools/excel-bridge.ps1`），
  `GET /api/bridge/health`、`POST /api/bridge/convert?to=…`。静态形态（`--mode static`）**不挂**它，前端据此置灰。
- 安全边界：只收"字节流 + 格式枚举"（浏览器传不了路径）、体积上限、请求串行、临时目录用完即删、只绑回环。
- **Excel 进程绝不留下**（两条真实泄漏路径，都踩过）：
  1. 脚本 `-Probe` 分支里 `exit 0` 会**跳过 `Quit()`** → 每次健康探测漏一个；
  2. 中间件超时 `child.kill()` 杀 PowerShell → 脚本的 `catch { Quit }` 没机会跑 → 每次超时漏一个。
  两者正反馈（残留越多越慢 → 越容易超时 → 更多残留，实测攒到 57 个无窗口 `EXCEL.EXE`，一次转换从 5 秒退化到 60 秒）。
  修法：脚本**单一出口 + `finally` 收摊**；启动后把 Excel PID 作为 stdout 第一行报给中间件，**超时按 PID 精确 taskkill**；
  再加服务器退出清理与手工兜底 `npm run bridge:clean`（只杀命令行带 `-Embedding` 的 COM 实例）。
- 顺带：探测失败以前被永久缓存（一次瞬时失败让桥整个会话"不可用"），现在失败只缓存 5 秒；
  超时分层：转换 180s、探测 60s。

---

## 6. 测试与验证

| 命令 | 内容 |
| --- | --- |
| `npm run typecheck` | `tsc --noEmit` |
| `npm test` | 单元 467 例（纯逻辑：解析器/导出器/模型/几何） |
| `npm run e2e` | 端到端 **131 例**，**三段独立进程**：`main`（112，6 并发）→ `sensitive`（17，串行）→ `hot-reload`（2） |
| `npm run e2e:fast` / `:sensitive` / `:hot-reload` | 单独跑某一段 |

**为什么要分三段**（并发能把 8 分钟压到 3 分钟，但这几类天生不能和别人抢机器）：

- `sensitive`（串行）：`perf`（帧率断言）、`tabs-memory`（采堆）、`static-form`（启动预算）、
  `date-cells`（悬停时序）、`clipboard`（读写**真实系统剪贴板**）。
- `hot-reload`（**独立进程**）：它**改写 `src/App.tsx`** 触发真实 HMR；实测与 `perf` 同进程时
  用例全过但 Playwright **永不退出**，所以单独一次调用。
- 依赖机器的用例会自动跳过：转换桥相关（CI 无 Excel）、真实用户样本（`USER_EXCEL` 或 `tests/e2e/user-fixture.local`）。

**仍未解决**（如实记录）：`main` 全量偶发"用例全过但进程不返回"（可用日志/报告读取结果）；
一次 `workspace-undo ④` 在 6 并发下超时，单跑全绿、复跑全绿，属并发时序抖动，根因未定位。

---

## 7. 已知边界（诚实清单）

**能开的格式**：`.xlsx` / `.xlsm`（宏原样保留、**不执行**）/ `.xltx` / `.xltm`；`.csv` / `.tsv` / `.txt`（含 GBK 识别）、
`.ods`、`.xls`（BIFF8）—— 后三种自研解析后翻译成规范 xlsx 走全链路，**导出会另存为 .xlsx**（原文件不动）。
`.xlsb` 需要本机 Excel（经桥转 xlsx）；`.numbers` / `.et` 明确不支持（给"为什么 + 请另存为 .xlsx"）。

**解析降级**（都会记进"降级报告"，不静默丢）：
- `.xls`：加密工作簿、BIFF5 及更早、图表/宏/对话框工作表不解析；公式只搬缓存值；错误值如实丢弃并计数；
  条件格式/数据验证/批注/超链接/图片不解析。
- `.ods`：无法可靠翻译的公式**丢公式保值**（计数）；复杂样式不解析。
- `.csv`：日期/百分比/布尔一律按**文本**导入（CSV 无类型，不替用户猜）；单表。
- 表格配色是主题色的算法近似；行高列宽为估算。不支持：dxf 边框、超链接 tooltip、批注作者、
  `top10`/`containsErrors`、>3 色色阶、数据条渐变、`absoluteAnchor` 图片、iconSet 的 `gte` 边界。
- **图表 / 透视表 / 迷你图 / 形状 / VBA 不解析**，但列进 `report.unsupported`，**导出时字节原样保留**。
- 打印设置会解析成真值（列 `report.preserved`），但本工具**不做打印预览/输出**（屏幕预览工具）。

**交互边界**：属性面板类能力（字体/边框等）按约束**永久不提供**；自动填充永久关闭（选区右下角小方块已不绘制）。
多块选区下"追加类"操作作用于全部块，"两方类"（互换/粘贴）只支持单块并说明原因。

**性能与内存**：百万格 6.1 MB 实测解析 1180 ms / 端到端 1439 ms / 拖动 p50 4.2 ms；
单标签模型不常驻（堆 543→446 MB）；多标签常驻窗口 K=3（第 4 个标签让堆 347→194 MB）。
冷标签切回要重解析（百万格 ≈1.2 s，只有"正在重建…"提示）；撤销历史是**全局一条**（冷存重建会清零）；
同一文件开两个标签仍是两份模型。**未测**：100 万行 × 50 列这类极端规模；Chrome 单 renderer 堆上限。

---

## 8. 常用命令

```bash
npm install
npm run fixtures          # 生成功能夹具（合成样本）
npm run bench:fixture     # 生成性能夹具（大文件）
npm run dev               # 开发预览
npm run build:local       # 本地版（带转换桥）
npm run build:static      # 静态版（base './'，无桥）—— 部署用这个
npm test / npm run e2e    # 见 §6
npm run bridge:clean      # 收拾转换桥可能残留的无窗口 Excel（支持 -DryRun）
```

---

## 9. 部署

**部署在腾讯云 EdgeOne Pages**（静态托管，`npm run build:static` 的产物 `dist/`）。

- `base: './'`：产物用相对路径，可以挂在子路径下（`https://<域名>/<子路径>/`）。
- 静态形态**没有本机进程**：`.ods`/`.xls`/`.xlsb` 导出与 `.xlsb` 导入会在界面上置灰并写明"本地版可用"。
- 数据全部留在浏览器本地（IndexedDB），不上传任何服务器；仓库里**只有合成样本**，没有真实学生数据。
- **GitHub Pages 部署已停用**：`.github/workflows/static-pages.yml` 已删除；如需彻底关闭，
  到 GitHub 仓库 `Settings → Pages` 把 Source 设为 `None`。
- 缓存（实测口径见 README 的"部署与缓存"一节）：首次访问要下 ~6.8 MB（其中 Univer 引擎 ~5.3 MB），
  之后浏览器与 CDN 都会命中缓存，只有 `index.html` 需要校验，所以**后续访问明显更快**。
