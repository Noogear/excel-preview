# M4 多标签内存分析：现状 · 边界 · 可落地优化

> **状态更新（本文写成之后已按第 4 节的顺序① ② ③ 落地，实现见 `P3-内容锁定与工作区-交付说明.md` 的 ⑧ 章节）**
>
> 已实现：常驻窗口 K=3（`src/shell/resident-tabs.ts`）→ 非活动标签冷存 + 切回重建、
> 恢复现场只实体化活动标签、关闭路径清理（编辑桶/记账/撤销栈）与脏标签关闭确认。
> **实测结果**（`tests/e2e/tabs-memory.spec.ts`，1.2 MB 夹具开 4 个标签）：
> 启动 45 MB → 导入 1/2/3 个 = 111/310/347 MB（实体化 1/2/3）→ **导入第 4 个 = 194 MB（实体化仍为 3）**
> —— 第 4 个标签触发冷存后堆回落，**`disposeUnit` 确实把内存还回去了**（本文第 5 节里"未验证"的
> 头一条，现已实测）。下面的分析与裁定保持原样，作为决策记录。

> 回答的问题：**多表格打开支持到什么程度？工具栏能有标签页吗？能不能靠资源复用降内存？**
>
> 结论一句话：**多标签今天已经能用**（工具栏标签条 + 每标签一个 Univer 工作簿 + 跨表工作区共享），
> **但打开数量没有上限、也没有任何跨标签资源复用**——每多开一个百万格标签，浏览器堆多约 +480…+670 MB
> （P3 实测口径），恢复现场时更是**一次性把 N 个标签全部实体化**。
> 真正能把内存压下来的只有一件事：**让非活动标签不常驻 Univer 工作簿**（冷存 + 切回重建）；
> 其余"省字节/共享样式表"的路子收益都在 2% 以内。
>
> 口径声明：本文所有数字标注来源（`实测` = 仓库既有测量 / `文档` = P3 交付说明 / `估算` = 我按前两者推算）。
> 读代码读不出来的，一律写「未验证」并说明需要什么才能验。本文写作时**未重新跑**任何基准或 e2e
> （后续落地后的复测结果见上面的状态更新）。

---

## 0. 一屏摘要

| 问题 | 结论 | 证据 |
| --- | --- | --- |
| 能同时开多个表格吗 | **能**，标签数**无上限**（代码里没有任何 `MAX_TABS` 类常量） | `App.tsx:1209`、`Toolbar.tsx:221` |
| 工具栏有标签页吗 | **有**（工作簿级标签）；工作表级标签走 Univer 自带 footer | `Toolbar.tsx:221-273`、`setup.ts:70-75` |
| 一次能选多个文件吗 | **不能**，`<input type="file">` 没有 `multiple` | `Toolbar.tsx:123-131` |
| 每个标签常驻什么 | 一份原始字节 + 瘦身导出源 + **一个 Univer 工作簿（含渲染器）** | `App.tsx:130`、`sheets-ui/lib/es/index.js:8623-8640` |
| 有资源复用吗 | **没有**：样式表/工作表模型每次导入都重建；同一文件开两次 = 两份 | `to-univer.ts:79-111` |
| 非活动标签的工作簿会释放吗 | **不会**，只有「关标签」才 `disposeUnit` | `App.tsx:1092`（全仓唯一一处） |
| 恢复现场要付多少 | N 个标签 = N 次重解析 + N 个工作簿，**全部常驻** | `App.tsx:961-993` |
| 优化方向 | 非活动标签冷存（dispose + 切回重建）；恢复时只建活动标签；关闭路径清理 | 见第 3 节 |

---

## 1. 今天的多标签能力

### 1.1 标签模型：一个 ref 数组 + 单一 Univer 实例

标签注册表就是一个 ref 数组，没有任何数量约束（`App.tsx:130`）：

```ts
const tabsDataRef = useRef<Array<{ id: string; fileName: string; source: ExportSource; bytes: Uint8Array }>>([]);
```

- 每个标签 = `{ id, fileName, source, bytes }`。`id` 来自 `toUniverWorkbook` 的 `imported-${Date.now()}`（`to-univer.ts:96`），
  所以**同一个文件开两次会得到两个不同 id、两套完全独立的模型**（不合并、不共享）。
- 渲染侧只有一个 Univer 实例、多个"工作簿单元"：导入时 `api.createWorkbook(...)`（`App.tsx:1204`），
  切换靠 `instanceServiceRef.current?.setCurrentUnitForType(id)`（`App.tsx:1070`）。
- UI 快照 `tabs` 由 `syncTabs()` 从 ref 派生（`App.tsx:1055-1063`），标签条渲染在 `Toolbar.tsx:221-273`，
  横向滚动（`toolbar.css:203-211`，`overflow-x: auto`），单标签 `max-width: 180px`（`toolbar.css:248`）。
- 没有任何标签时显示文件名占位（`Toolbar.tsx:262-272`，`App.tsx:1813` 传的是 `fileName={null}`）。

### 1.2 每个标签里到底存了什么

`handleFile` 的落库片段（`App.tsx:1204-1212`）：

```ts
api.createWorkbook(outcome.workbookData);
const tabId = outcome.workbookData.id;
const tabEntrySource = slimForExport(parsed, bytes);
tabsDataRef.current = [...tabsDataRef.current, { id: tabId, fileName: file.name, source: tabEntrySource, bytes }];
```

- **完整解析模型不常驻**（这是 P3 已做的优化）：`parsed` 是 `handleFile` 的局部量，出栈即回收，
  只留 `slimForExport` 结果——`sheet.id` + `行号 → 每行第一个样式 s` 的小映射（`slim-source.ts:28-39`，
  百万格时约 501 条，文档 `P3:252`）。
- **原始字节每标签一份**：`bytes`（`App.tsx:1193`）与 `source.bytes` 是**同一个引用**
  （`slim-source.ts:38` 直接把它塞进返回对象），不是两份拷贝。
- **Univer 工作簿每标签一个，且常驻**：`createWorkbook` 后不 dispose，只有 `closeTab` 会
  `apiRef.current?.disposeUnit(id)`（`App.tsx:1092`）。Univer 侧每个 unit 都会建渲染器，
  只在 unit 被 dispose 时才 `removeRender`（`sheets-ui/lib/es/index.js:8623-8640`）。

### 1.3 切标签 / 关标签发生什么

- **切标签**（`activateTab`，`App.tsx:1066-1084`）：收掉黄色提醒框 → `setCurrentUnitForType(id)` →
  `parsedRef.current = tab.source` → `attachSheetDeps()`（重装内容锁 + 刷新表名/选区）→ 记日志。
  非活动标签的工作簿**原地不动**，不 dispose、不重建。切换耗时未验证（未做测量；按结构判断是"换当前单元"，不含重解析）。
- **关标签**（`closeTab`，`App.tsx:1086-1114`）：`disposeUnit(id)` → `clearWorkbookDirty(id)` →
  从数组移除 → 若关的是活动标签就激活邻居；**关掉最后一个标签**走 `App.tsx:1105-1109`：
  `activeTabIdRef.current = null`、`parsedRef.current = null`，标签条回到占位态。
  （启动时那份 `p0-workbook` 冒烟样本 `App.tsx:516` 不在 `tabsDataRef` 里、也**永远不会被 dispose**；
  关掉最后一个标签后画面表现未验证——代码路径是"没有当前单元"，实际渲染结果我没跑过。）
- **关标签不做脏检查**：`closeTab` 直接 dispose + 从注册表移除，下一次自动保存（`App.tsx:915-930`）
  写出的会话里就没有这个标签了 → **该标签的编辑静默丢失**（有 `dirty` 小红点，但没有确认弹窗）。

### 1.4 明确不支持的

| 项 | 现状 |
| --- | --- |
| 一次多选文件 | 不支持（`Toolbar.tsx:123-131` 无 `multiple`） |
| 重复文件去重 | 不做；同名文件开两次 = 两个独立标签、两套模型 |
| 标签数量上限 | 无 |
| 标签重排 / 拖拽 / 关闭其他 / 全部关闭 | 无（只有单标签关闭按钮，`Toolbar.tsx:242-257`） |
| 工作表级标签 vs 工作簿级标签 | 工作簿级 = 我们的标签条；工作表级 = Univer footer 的 `sheetBar`（`setup.ts:70-75`），两者互不感知 |
| 每标签独立的撤销历史 | 没有：`historyEntries` 是**全局一条**（`App.tsx:212`），`handleJumpTo` 对**当前**工作簿 `api.undo()`（`App.tsx:1377-1386`）→ 切表后跳步会作用到另一张表 |
| 导入中再开一个 | 被 `busy={status === 'importing'}` 挡住（`App.tsx:1819`），不会并发导入 |

---

## 2. 内存账本

### 2.1 每个标签的常驻项（按量级排序）

| 项 | 大小 | 来源 |
| --- | --- | --- |
| Univer 工作簿（cellData + skeleton + 引擎/画布） | **约 +480…+670 MB / 百万格标签** | 文档 `P3:242`（1 标签 459 MB、2 标签 1125 MB、3 标签 1607 MB，未强制 GC）；`P3:365` 写作"每多一个百万格标签约 +550 MB" |
| 原始 xlsx 字节（`tabsDataRef[i].bytes`） | ≈ 文件大小，夹具 6.1 MB | 文档 `P3:196` / `README:119` |
| 瘦身导出源（`rowStyles` 映射） | 约 501 条/表（几 MB 以内，未单独测） | 文档 `P3:252` |
| 会话写入时的瞬时拷贝 | 全部标签字节再克隆一份进 IndexedDB | `session.ts:265-270`（结构化克隆，不转 base64）、`App.tsx:1155-1161` |

**推论（估算）**：字节类常驻 ≈ 6.1 MB + 小映射，只占单标签 459 MB 的 **~1.5%**。
所以"释放字节"这条路的天花板就是 1~2%，而 Univer 工作簿占了 98%。

### 2.2 已有的实测数字（都来自 P3，本轮未重测）

- 解析 1180 ms、端到端导入 1439 ms、拖动帧间隔 p50 4.2 ms（`P3:203-217`，百万格 6.1 MB）。
- 模型瘦身：浏览器堆 **543 → 446 MB**（`P3:263`）；Node 常驻 **93 → 90 MB**（`P3:264`）。
- 导出路径：瘦身后每标签只留原始字节，导出时惰性解压，代价约 0.2 s（`P3:255`）。

### 2.3 会话恢复会把 N 个标签全部实体化（当前最大的内存风险点）

`App.tsx:961-993` 是一个**串行 for 循环**，对每个标签都做完整四步：

```ts
const parsed = await parseXlsx(tab.originalBytes);            // 962  重解析
const outcome = toUniverWorkbook(parsed, { name: tab.fileName, workbookId: tab.id });
boot?.univerAPI.createWorkbook(outcome.workbookData);          // 964  建工作簿（常驻）
await applyWorkbookFeatures(boot!.univerAPI, parsed);          // 970  重放特性
{ ..., source: slimForExport(parsed, tab.originalBytes), ... } // 978  瘦身入库
```

- `SessionTab.snapshot` 类型上允许存 Univer 快照，但**我们永远写 `null`**（`App.tsx:1159`），
  所以恢复 100% 走"重解析 + 重放特性 + 回放编辑"（`App.tsx:981-991`）。
- 结果：恢复 5 个百万格标签 ≈ 5 ×1.2 s 解析 + 约 2.5 GB 常驻（按 `P3:242` 口径估算）。
  没有任何上限或"只建活动标签"的短路——`activateTab(target)` 在循环**之后**才调用（`App.tsx:1000`）。
- 自动保存每 2 s 检查一次指纹，指纹含每个标签的脏格数（`App.tsx:1174-1181`），一旦变化就把
  **所有标签的字节**重新结构化克隆进 IndexedDB（`App.tsx:1154-1171` → `session.ts:474-485`）。
  多标签时的卡顿未实测，但克隆量 ≈ Σ文件大小，属可预期的主线程开销（估算）。

### 2.4 顺手发现的既有泄漏（与标签数正相关）

1. **`editsByTabRef` 从不删除**：`App.tsx:1118` 建 Map，只有 `set`/`get`（`992`、`1126-1129`、`1160`），
   `closeTab` 不清理 → 关掉的标签的编辑记录常驻。
2. **Univer 的撤销栈从不释放**：`_undoStacks` / `_redoStacks` 按 unitId 建 Map
   （`@univerjs/core/lib/es/index.js:18463-18464`），`disposeUnit` 只从实例表移除并 `unit.dispose()`
   （同文件 `17638-17654`），**全仓没有任何 `_undoStacks.delete`**；每栈上限 20 条（`18407`）。
   即"开→关"很多次会持续累积小对象；且**同一 unitId 重建会让旧撤销栈复用到新模型上**——这是第 3 节 (c) 的必须处理项。
3. **启动样本工作簿 `p0-workbook` 不参与标签管理**（`App.tsx:516`），也就永远不会被 dispose。

---

## 3. 可落地的优化选项

### (a) 导入成功后释放原始字节，导出时再读回 —— **不值得做**

- 事实：**导出确实需要这些字节**。`exportXlsx` 的唯一数据来源就是
  `source.raw?.entries ?? readZipEntries(source.bytes)`（`export-xlsx.ts:126-133`），
  且会话恢复也要靠它重解析（`App.tsx:962`，`session.ts:43-44` 注释写明"导出时做外科式修补要用"）。
- 收益（估算）：约 **6.1 MB/标签**，即单标签 459 MB 的 ~1.5%；10 个标签也就 60 MB。
- 风险：为了这 1.5% 要引入"重新读盘"通道（页面上只剩 `File` 句柄可用；`FileSystemFileHandle` 才能跨刷新），
  还要说服自己"导出永远来得及读"。**投入产出比明显为负，不做。**
- 唯一值得顺手做的是**不重复持有**：现在 `bytes` 与 `source.bytes` 已经是同一引用（不是两份），继续保持即可。

### (b) 非活动标签惰性创建 Univer 工作簿 —— 对"导入"无意义，对"恢复现场"高价值

- 对导入路径**不成立**：导入后立刻把新标签设为活动（`App.tsx:1213-1214`），
  所以"创建时就是非活动"的情况根本不存在；要真做就得先不建工作簿，那又会把完整模型留在内存里（与瘦身目标相反）。
- 对**会话恢复**成立且便宜：`App.tsx:961-993` 现在无条件建 N 个，改成"只实体化 `state.activeTabId`，
  其余只登记 `{id, fileName, bytes}` 冷条目，首次切过去时再走同一段构建代码"。
- 收益（估算，按 `P3:242` 口径）：恢复 5 个百万格标签，从 ≈2.5 GB / 6 s 降到 ≈0.55 GB / 1.2 s。
- 风险：**低**。改的是启动循环的边界条件，导出/编辑路径不变；坏情况只是"切到冷标签时慢 1.2 s"。
- 怎么验：`tests/e2e/persistence.spec.ts` 已有"重开页面后标签恢复"的用例（①，`persistence.spec.ts:44-130`）。
  再加一条：落盘 2 个标签 → 重开 → 断言 `__p0.getTabs()` 有 2 个、`performance.memory.usedJSHeapSize`
  只相当于 1 个标签的水位 → 点第二个标签 → 断言内容/表名正确。堆的口径沿用 `tests/e2e/perf.spec.ts:113`。

### (c) 非活动标签 dispose + 切回重建 —— **本轮最值得做的一件事**

机制（把现有的恢复循环抽成 `buildTabWorkbook(tab)` 复用即可，代码已经存在）：

```
dispose 前：snapshotActiveEdits()   // 必须！否则该标签的编辑值随模型一起没了
          api.disposeUnit(tab.id)   // 模型 + 渲染器一起释放（sheets-ui:8625-8640 会 removeRender）
切回时：   parseXlsx(bytes) → toUniverWorkbook({workbookId: tab.id}) → createWorkbook
          → applyWorkbookFeatures → 回放 editsByTabRef.get(tab.id)
```

- **释放什么**：`disposeUnit` 把 unit 从实例表移除并 `unit.dispose()`（`core/lib/es/index.js:17638-17654`），
  资源层在 `unitDisposed$` 上 `unloadResources`（同文件 `20046-20048`），
  渲染器被 `removeRender`（`sheets-ui/lib/es/index.js:8625,8637-8640`）。
  即"工作簿 + 画布 + 各插件资源"整体释放。**释放后 V8 是否真把几百 MB 还给系统未验证**——
  必须按 P3 的口径实测（`performance.memory.usedJSHeapSize`，注意 `P3:270` 明确说该口径**不含 Uint8Array 外部内存**）。
- **重建的代价（两条路，选重解析）**：
  - *重解析*（推荐）：字节已在内存，`parseXlsx` 百万格 ≈1.18 s（`P3:203`），另加特性应用 ≈11 ms（`P3:204`）；
    需要完整模型**瞬时**存在（Node 侧 ≈237 MB、`P3:206`），解析完即释放。**编辑不丢**：脏格在
    `dirty-tracker` 里（`dirty-tracker.ts:11-25`），`editsByTabRef` 里已有值，回放即可。
  - *从 Univer 快照重建*（不推荐）：`fWorkbook.save()` 确实能带上 CF/数据验证等插件数据
    （`@univerjs/sheets/lib/es/facade.js:7091-7093` → `core/lib/es/index.js:20053-20060`），省掉重解析；
    但它内部是 `Tools.deepClone(unit.getSnapshot())`，**快照本身 ≈ 活模型的体量**，留 N 份等于没省，
    且克隆瞬间还要翻倍。而且"快照能否完整还原图片/drawing/超链接"**未验证**（需要造一份带图片的夹具实测往返）。
- **撤销历史必须先清**：`IUndoRedoService.clearUndoRedo(unitId)` 存在（`core/lib/es/index.js:18491-18497`），
  但 Univer **不会**在 dispose 时自动清（同文件 `18463-18464`，全仓无 delete）。
  若沿用原 unitId 重建而不清栈，用户按 Ctrl+Z 会把"旧模型的 mutation"打到"重解析出来的干净模型"上——
  语义错误甚至覆盖回放结果。我们的自有历史账本（`App.tsx:212`、`1352-1368`）也要在重建处**切断**
  （或按标签分桶），否则跳步会作用到别的表（现状已经是全局一条，见 1.4）。
- 收益（估算，按 `P3:242` 的边际值）：每个被冷存的百万格标签 **≈480…670 MB**；
  若"最多常驻 K 个工作簿"，总内存从 O(N) 变成 O(K)。
- 风险：中。三个必须做对的点：① dispose 前 `snapshotActiveEdits()`；② 重建后清撤销栈/切历史；
  ③ 重建期间 `busy` 置位（避免用户在 1.2 s 里操作空模型）。
  另外**导出不受影响**：导出只读 `source.bytes` + 脏格（`App.tsx:1297-1327`），
  冷标签在导出前一定是活动的（导出按钮作用于当前标签），字节保真约束天然保持。
- 怎么验：
  - `npm test`：把"冷/热"记账抽成纯函数模块（照 `src/shell/history-model.ts`、`src/workspace/filter.ts` 的先例）加单测。
  - `npm run e2e`：新增用例"开 2 个夹具 → 在标签1 改一格并落盘 → 切到标签2 → 切回标签1"，
    断言：改的那格值还在（回放成功）、表名/活动表正确、`performance.memory` 不随标签数线性增长；
    再断言"切回后 Ctrl+Z 不会写出错误内容"（历史切断）。
  - `tests/e2e/perf.spec.ts:39-146` 的堆口径可直接复用（`113` 行读 `usedJSHeapSize`）。

### (d) 同一文件开两次时共享样式表/字符串表 —— **不值得做**

- 事实：`toUniverWorkbook` 每次调用都新建 `styles` 与 `sheets`（`to-univer.ts:79-111`），确实没有缓存。
  但真正的大头是 `cellData`（百万个可变单元格对象）——**它必须每标签一份**，因为编辑、脏格、撤销都按工作簿隔离。
- 可共享的只剩样式表/主题色（相对小），而共享引用会让两个 `Workbook` 指向同一 `styles` 对象；
  本产品虽然锁死格式（`univer/lock.ts` 的 `stripStyles` + 命令门禁），但 Univer 内部仍可能在
  `createWorkbook` 期间改写该对象。**未验证** Univer 是否深拷贝入参——要验需要做一次"共享 styles 开两个簿、改内容后比对两边样式"的实验。
- 结论：收益小、风险不对称（一旦污染就是"格式被改"，踩产品硬约束），**不做**。
  真要省"同一文件开两次"，正确做法是产品层面提示"该文件已在标签 N 打开"，而不是共享底层表。

### (e) 给"同时实体化的 Univer 工作簿数"设上限 —— 作为 (c) 的开关，而不是独立项

- 纯上限（不让开第 N+1 个标签）会伤体验，不能单独上。
- 与 (c) 合成一个机制最自然：**常驻窗口 K**（建议默认 2~3，可配置），超出时把最久未使用的非活动标签冷存。
  触发点只有两处：`handleFile` 追加标签后（`App.tsx:1209`）、`activateTab` 切走后（`App.tsx:1066`）。
- 收益同 (c)：内存从"随标签数线性增长"变成"封顶 ≈ K × 单标签"。
  按 `P3:242` 口径估算，K=2 时即便开 8 个百万格标签，堆仍稳定在 ≈1.1 GB，而今天是 ≈4 GB（必然 OOM）。
  Chrome 单 renderer 的堆上限本身**未验证**（要实测才能给出"今天最多能开几个"的确定数字；
  按边际 +500~670 MB 粗估是 5~6 个，属估算）。

### 另外两项"顺手就该做"的清理（低成本、与 (c) 互补）

- `closeTab` 里补 `editsByTabRef.current.delete(id)`、`IUndoRedoService.clearUndoRedo(id)`（需要根 injector，
  取法照 `setup.ts:155-166` / `App.tsx:2206-2210` 的先例）、以及首个真实标签导入后
  `disposeUnit('p0-workbook')`。
- 脏标签关闭前给一次确认（现在 `closeTab` 无脏检查，`App.tsx:1086-1114`）。

### 不建议本轮做的（记录理由）

- **自动保存改成分标签存储**：能省掉每次编辑后对所有标签字节的结构化克隆（`App.tsx:1154-1171`），
  但要动 `session.ts` 的 schema（`DB_VERSION = 1`，`session.ts:124-128`）并改
  `tests/unit/persistence.test.ts`、`tests/e2e/persistence.spec.ts` 的既有契约。
  它省的是"瞬时拷贝/卡顿"，不是常驻内存，**排在 (b)(c) 之后**。

---

## 4. 建议的落地顺序（按价值/风险比）

1. **恢复现场时只实体化活动标签**（(b)）：改 `App.tsx:961-993` 一处循环 + 抽一个
   `buildTabWorkbook()`；恢复 N 个标签从 O(N) 内存降到 O(1)，风险最低，还能顺带把
   "重建"这段代码准备好给第 2 步复用。验证：`persistence.spec.ts` 加堆断言。
2. **非活动标签冷存 + 切回重建（含常驻窗口 K）**（(c)+(e)）：复用第 1 步的 `buildTabWorkbook()`；
   必须一起做三件事——`snapshotActiveEdits()` 先落编辑、`clearUndoRedo(unitId)` 清撤销栈、
   历史账本按标签切断。收益最大（每冷存一个百万格标签 ≈480…670 MB，文档 `P3:242`）。
   验证：新增 e2e（改一格 → 切走 → 切回 → 值在、历史不串、堆不随标签数增长）。
3. **关闭/清理路径补齐 + 脏标签关闭确认**：`editsByTabRef` 删除、撤销栈清理、
   dispose 冒烟样本工作簿、脏标签二次确认。改动小、全在 `App.tsx:1086-1114` 附近，
   同时消除 (c) 最容易踩的三个坑，属"先修地基再上功能"。

---

## 5. 未验证清单（要验需要什么）

| 项 | 需要什么才能验 |
| --- | --- |
| 关掉最后一个标签后的实际画面 | 跑一次 e2e：开 1 个夹具 → 关标签 → 截图/断言 `__p0.getSheetNames()` |
| 切标签的墙钟耗时 | 在 `activateTab` 前后打点，或 e2e 用 `performance.now()` 包住点击 |
| `disposeUnit` 后堆是否真的回落 | e2e 读 `performance.memory.usedJSHeapSize`（`perf.spec.ts:113` 口径），注意不含外部内存（`P3:270`） |
| 从 `fWorkbook.save()` 快照重建是否等价 | 造带 CF/DV/批注/图片的夹具，比对重建前后 `getFeatureReport()` 与单元格值 |
| 共享 `styles` 是否会被 Univer 改写 | 实验：两个 `createWorkbook` 共享同一 `styles` 对象，改内容后比对 `getCellStyleData()` |
| Chrome 单 renderer 堆上限 / 今天最多能开几个百万格标签 | 循环导入 `fixtures/bench-large.xlsx` 直到崩溃，记录标签数与堆水位 |
| 多标签自动保存的卡顿 | 开 4~8 个标签后编辑一格，量 `collectSessionState()`+结构化克隆的主线程耗时 |
