# P1 保真与导出 — 交付说明

> 阶段目标：把"预览"从**单元格样式**扩展到 Excel 真正难还原的部分——
> 条件格式、数据验证、批注、超链接、浮动图片、Excel 表格斑马纹，并支持把编辑结果导出回 xlsx。
> 结果：**197 个单元测试全绿**；端到端覆盖见文末。

---

## 一、六类"必须 100% 保真"的能力，各自怎么实现的

| 能力 | 实现路径 | 验证方式 |
| --- | --- | --- |
| **边框/填充/字体/对齐/合并/行列尺寸** | 解析 `styles.xml`（含 `theme1.xml` 主题色 + tint）→ `IStyleData` | P0 起就有字段级断言 |
| **数字格式** | 解析 `numFmt`（内置 0–49 + 自定义）→ Univer `numfmt` 渲染 | 17 种格式显示文本逐一核对（含 `[h]:mm:ss`、`# ?/?`、格式内字面量） |
| **条件格式（高亮/色阶/数据条/图标集）** | 解析 `conditionalFormatting` + `cfRule` + `dxfs` → Facade `newConditionalFormattingRule()` 分支构建 → `addConditionalFormattingRule()` | 解析 4/4 逐字段核对真值；应用计数与视觉截图见 e2e |
| **数据验证** | 解析 `dataValidations` → `univerAPI.newDataValidation()` 的 `require*`/`setCriteria` → `fRange.setDataValidation()` | 解析 5/5（list/whole/decimal/date/textLength） |
| **批注** | 解析 `xl/comments*.xml`（多行文本保真）→ note facade | 解析 2/2，文本含换行 |
| **超链接** | 解析 `<hyperlinks>` + sheet rels（外链/表内跳转/邮件）→ hyper-link facade | 解析 3/3，目标逐一核对 |
| **浮动图片** | 解析 `xl/drawings/drawing*.xml`（oneCell/twoCell 锚点 + EMU）→ 原始字节转 Blob URL → 插入浮动图片 | 解析 2/2，锚点与尺寸核对 |
| **Excel 表格斑马纹与表头** | **样式实体化**（见第二节） | 适配层单测 + 端到端断言（表头底色/加粗/字色、斑马纹交替、汇总行上边框） |
| **自动换行的行高** | **行高估算**（见第三节） | 适配层单测 + 端到端实测 `row3=59px vs 常规 24px` |

---

## 二、Excel 表格：为什么必须"实体化"

实测结论：**表格的斑马纹与表头样式根本不在单元格里**。`xl/tables/tableN.xml` 只有
`tableStyleInfo@name`（如 `TableStyleMedium9`），而 Excel 的内置表格样式目录**不在文件中**
（`tableStyles.xml` 的 `count` 为 0）。所以想"看起来和 Excel 一样"，只能自己按样式名还原配色并写进单元格样式。

实现（`src/importer/table-style.ts`）：

- 强调色取自 `theme1.xml` 的 accent1..6，序号映射 `accent = floor((N-1)/7) + 1`（Excel 每 7 个变体共用一个强调色，`Medium9 → accent2`）
- Medium/Dark：表头 = 强调色实底 + **按亮度自动选黑/白字**；斑马纹 = 强调色极浅色调
- Light：表头 = 极浅底 + 强调色深字
- 汇总行加粗 + 上边框；首列/末列加粗
- 补丁**与原样式合并**（保留原有字体/对齐/数字格式），而不是覆盖

> **这是算法近似，不是 100% 复刻**。已在导入报告里显式记为降级项。
> 若要完全一致，需要引入 Excel 内置表格样式目录（约 60 个样式 × 若干配色）作为数据表——属于后续可选增强。
>
> 另有一条实测坑：ExcelJS 把"**没有**汇总行"写成 `totalsRowShown="1"`（字面语义相反）。
> 解析器按实际语义处理（`totalsRowCount = 显式值 ?? 0`）并保留原属性 + 记 warning，
> 否则 `SalesTable` 的数据行会被误当成汇总行渲染。

---

## 三、自动换行的行高

- 不处理的话，长文本会被硬裁切（P2 截图里肉眼可见）
- `src/importer/auto-height.ts`：半角 0.55em / 全角 1.0em 计宽 → 每行可容纳字数 → 行数 × 字号 × 1.36 + 余量
- 合并单元格按**整段合并宽度**计算；Excel 里手工设过高度的行**保持不动**（与 Excel 行为一致）
- 只增不减，且不低于默认行高

> 实测坑：最初把内边距按 8px 叠加，而 Excel 的默认行高**已经包含**内边距，
> 导致"单行文本"也被判定需要加高、整表行高被无谓撑大。已修正为 2px 余量。

---

## 四、外科式导出（导出回 xlsx）

常规做法是把内存里的工作簿"另存为"新 xlsx——那会**丢掉我们没解析的一切**（图表、透视表、形状、VBA…），
对一个"预览既有文件"的工具是致命的。本项目的做法是**外科式修补**：

```
原 .xlsx (zip)
  ├─ xl/worksheets/sheetN.xml   ← 只重写被编辑过的那张表（写回新的值/公式）
  ├─ xl/sharedStrings.xml       ← 只在需要新字符串时**追加**（复用已有索引，不重排）
  ├─ xl/workbook.xml            ← 只写 <calcPr fullCalcOnLoad="1"/>（让 Excel 打开时重算公式）
  └─ 其余全部条目（styles/theme/tables/drawings/charts/media/…）→ **逐字节原样保留**
```

- **只回写脏单元格**：`src/univer/lock.ts` 在 mutation 层记录每一次写入涉及的坐标
  （`sheetId → {row:col}`），导出时只把这些格子的当前值/公式交给导出器
- **单元格写入规则**：保留原 `r`/`s` 属性（**样式不动**）；已存在则替换 `<v>`/`<f>`；
  不存在则按列号/行号升序插入新的 `<c>`/`<row>`；清空内容时保留样式（无属性才整格移除）
- **字符串**：先复用 `sharedStrings` 中完全相同的条目，找不到才追加并同步 `count/uniqueCount`
- **公式**：不写缓存值，改由 `fullCalcOnLoad` 让 Excel 打开时重算，规避"缓存值过期"的经典坑
- 界面上是顶栏的 **`导出 xlsx`** 按钮，导出文件名形如 `原名-已编辑.xlsx`

单元测试覆盖（24 条）：往返正确性、**非白名单部件逐字节相等**、
`xl/tables/*.xml` 字节不变、共享字符串复用不新增 `<si>`、新单元格插入顺序、
`fullCalcOnLoad` 开关、值类型（数字去 `t`/布尔 `t="b"`/公式转义）、XML 转义与非法控制字符、
首尾空白 `xml:space="preserve"`、自闭合 `<sheetData/>`/`<row/>` 展开、不改动入参、找不到 sheetId 明确报错等。

---

## 四之二、实现用到的 Facade API（备查）

| 能力 | 入口与关键方法 | 出处 |
| --- | --- | --- |
| 条件格式 | `FWorksheet.newConditionalFormattingRule()` / `addConditionalFormattingRule()`；builder 的 `whenNumber*` / `whenText*` / `whenFormulaSatisfied` / `whenCellEmpty` / `whenDate` / `setDuplicateValues` / `setUniqueValues` + `setBackground/setFontColor/setBold/setItalic/setStrikethrough`；`setColorScale` / `setDataBar` / `setIconSet` | `@univerjs/sheets-conditional-formatting/lib/types/facade/` |
| 数据验证 | `univerAPI.newDataValidation()`；builder 的 `requireValueInList / requireValueInRange / requireNumber* / requireDate* / requireFormulaSatisfied / setAllowBlank / setOptions / build`；**`textLength`、`time` 等只能先 `build()` 再调用 `FDataValidation.setCriteria(type, [operator,f1,f2], allowBlank?)`**（builder 上没有该入口） | `@univerjs/sheets-data-validation/lib/types/facade/` |
| 超链接 | `FRange.setHyperLink(url, label?)`；表内跳转用 `FRange.getUrl()` 生成 payload | `@univerjs/sheets-hyper-link/lib/types/facade/f-range.d.ts` |
| 批注 | `FRange.createOrUpdateNote(ISheetNote)` | `@univerjs/sheets-note/lib/types/facade/f-range.d.ts` |
| 图片 | `FWorksheet.newOverGridImage()` → `setSource(url, ImageSourceType.URL)` / `setColumn/setRow/setColumnOffset/setRowOffset` / `setWidth/setHeight` / `buildAsync()` → `FWorksheet.insertImages([...])` | `@univerjs/sheets-drawing/lib/types/facade/` |

**iconSet 名称映射**：Univer 的 `IIconSetType` 枚举值**就是** Excel 的 OOXML 名，因此是**恒等映射**，
代码里显式列出全部 **20** 个（`3Arrows`…`5Boxes`）并做白名单校验；未识别名字（x14 自定义图标集等）记 issue 并跳过。
注意**图标顺序是反的**：Excel 下标 0 = 最低档，Univer `iconId` 0 = 图标组首项（最高档），
因此落库为阈值倒序 + `greaterThanOrEqual`（fixture 实测：`[{iconId:'0',percent:67},{iconId:'1',percent:33},{iconId:'2',percent:0}]`）。

**颜色落库形态**：条件格式高亮经 `ColorKit` 落库为 `rgb(r,g,b)`；色阶/数据条的 config 保持 `#RRGGBB`。

---

## 五、解析覆盖度（独立核验：与真值逐项对照）
用 `tests/unit/fixture-coverage.test.ts` 对 6 个真实样本做解析计数核验：

| 样本 | 真值 | 实测 |
| --- | --- | --- |
| `fixture-rules.xlsx` | 条件格式 4 条、数据验证 5 条、dxf 1 条 | ✅ 4 / 5 / 1 |
| `fixture-extras.xlsx` | 超链接 3、批注 2、图片 2 | ✅ 3 / 2 / 2 |
| `fixture-table.xlsx` | 表格 2 个 | ✅ 2 |
| `fixture-styles.xlsx` | 单元格 40、合并 1、冻结 {1,1}、样式 27 | ✅ 全部命中 |

`report.unsupported` 现在是**诚实的缺口清单**（按工作表/工作簿分组、按种类计数），仍未解析的主要有：
打印设置、自动筛选、迷你图、排序状态、条件格式/数据验证的 x14 扩展、工作表背景图、
OLE 嵌入对象、旧式 VML 绘图、数据透视表、切片器/日程表、图表、形状/文本框/组合图形、VBA 宏、外部链接缓存、ActiveX 等。

---

## 七、已知近似与无法还原（诚实清单）

**算法近似（看起来接近，但不是像素级复刻）**

| 项 | 说明 |
| --- | --- |
| Excel 表格配色 | 表头/斑马纹由样式名 + 主题强调色推算（Excel 内置表格样式目录不在文件里）。已计入导入报告的降级项 |
| 行高自适应 | 按字号与文本宽度估算（半角 0.55em / 全角 1.0em），非真实字体度量 |
| 列宽 | Excel 字符宽度 → 像素用经验公式换算 |

**能力缺口（已记入 `report.unsupported` / `issues`，不静默丢弃）**

- 条件格式：`dxf` 的**边框**（Univer CF 样式无边框位）、`top10`/`containsErrors` 等规则类型、色阶超过 3 色、数据条的渐变与负值配色、图标集的 `gte=0` 边界
- 数据验证：`textLength`/`time`/`dateNotEqual` 等没有专用 API 的类型走 `setCriteria` 兜底
- 超链接：`tooltip`（Facade 无对应参数）
- 批注：作者名（Facade 无对应字段）
- 图片：`absoluteAnchor` 锚点（只支持 oneCell / twoCell）
- 解析未覆盖：图表、数据透视表、迷你图、切片器、形状/文本框、OLE、VBA、打印设置、自动筛选状态、条件格式/数据验证的 x14 扩展、工作表背景图、排序状态等（完整清单见 `report.unsupported` 的实现处）
- 导出：带命名空间前缀的 `<x:c>` 会写成默认命名空间（Excel 不会这么写，但功能与回读都正常）

**样本自身的问题（已按"与 Excel 行为一致"处理）**
`fixture-extras.xlsx` 的 B2 超链接 `location` 指向本文件里**不存在**的工作表「条件格式」（生成样本时的失效引用）。
导入层保留原始 location 作为链接负载并记一条降级 issue——点击时 Univer 提示引用无效，与 Excel 打开该文件的表现一致。

---

## 八、视觉证据

`evidence/p1-cf-visual.png`（由 e2e 自动产出）：`fixture-rules.xlsx` 导入后，
**四类条件格式全部正确渲染**——

| 列 | 规则 | 画面表现 |
| --- | --- | --- |
| B「高亮>100」 | `cellIs greaterThan 100` + dxf | 120/210/175 呈**粉底 + 深红加粗**（`#FFC7CE` / `#9C0006`，与 `dxfs` 逐字段一致） |
| C「色阶」 | `colorScale` min→max | 红→绿渐变，按数值插值 |
| D「数据条」 | `dataBar` | 蓝色条按数值成比例 |
| E「图标集」 | `iconSet 3TrafficLights1` | 红/黄/绿圆点按分位阈值 |

同批产物还有 `evidence/p1-cf.png`、`evidence/p1-extras.png`（批注/超链接/浮动图片）。

---

## 七、测试

```bash
npm run typecheck   # tsc --noEmit（零报错）
npm test            # Vitest 202 passed（7 个文件）
npm run e2e         # Playwright 26 passed（导入渲染 / 手势 / 工作区 / 撤销 / 视觉基线 /
                    #   P1 六类特性 / 表格斑马纹 / 行高 / 导出）
```

最近一次完整验证的真实输出：

```
TYPECHECK  PASS（tsc 零报错，无任何诊断输出）
UNIT       Test Files 7 passed (7) | Tests 202 passed (202)
E2E        26 passed (1.0m)
           [p1-rules]  counts = {conditionalFormats:4, dataValidations:5, failed:0, skipped:0}
           [p1-extras] counts = {hyperlinks:3, notes:2, images:2, failed:0, skipped:0}
```

> 并发跑测试时请用 `PW_OUTPUT_DIR=test-results-b npm run e2e`：
> Playwright 每次运行会清空 outputDir，两个进程同目录会互删 trace 与截图
> （表现为 `browserContext.close: ENOENT ... traces/...`）。已做成可注入。

端到端覆盖：导入渲染、手势、工作区、撤销、视觉基线（P0/P2）+ P1 的表格斑马纹与行高、条件格式/数据验证/超链接/批注/图片的应用结果。
